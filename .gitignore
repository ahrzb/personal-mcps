design/personal-mcps-ui.html
node_modules/
.wrangler/
.dev.vars
__pycache__/
*.pyc
.secrets
.env

# operator-local token stash — never committed
.tokens

# publish-time build outputs (prepublishOnly), never committed
cli/dist/
clients/js/dist/
clients/rust/target/

# `nix build` drops this symlink into the tree; the store path it points at is the artifact
result
result-*

# live browser session cookies — never committed
cookies.txt

# Vite build output for the browser client — built on demand, never committed
web/dist/

# the visual-comparison report and its scratch shots (web/scripts/visual-compare.mts)
web/.visual/
