# Concepts — unreviewed exploration boards

Boards that explore a page or state the spec has not adopted. Nothing here is contract: no
§13 sentence, no fixture and no test points at them. They moved out of `design/` on
2026-09-02 so the canvas beside the code draws only what the code renders (the states
preview is the kept reference — D15 constraint 13). A board leaves this folder the day the
owner reviews it and §13 is amended (roadmap steps 8, 9, 10 and 11 in
`docs/superpowers/plans/2026-09-02-roadmap-after-d15.md`).

`canvas.json` here keeps their layout; seed them separately if they are reopened in the
design tool. `OauthConsent` itself stayed in `design/` — it draws the live §19.5 page; only
its states board is here, and of that only the DENIED confirmation panel is a concept (the
hub answers a deny with a redirect, never a page).

| Board | Explores |
|---|---|
| ~~`Agents`, `AgentDetail`, `GrantEditorStates`~~ | adopted 2026-09-03 (§13 `/agents`, `/agents/<slug>`, the grant editor) — moved back to `design/` |
| ~~`AuditDetailStates`~~ | adopted 2026-09-03 (§13's expanded row: the three no-bodies sentences, KB/MB stub sizes, the `event-<id>` anchor) — moved back to `design/` without its LOADING panel, the lazy fetch the spec never adopted |
| `OauthConsent` + `OauthConsentStates` | the §19.5 consent screen (self-registered marker, zero-agents empty state) — a step inside the authorize redirect, never navigated to, so it stays chromeless and gets no nav slot |
| ~~`AppNewProxiedStates`~~ | adopted 2026-09-03 (§13 pinned the connecting page, the endpoint URL rule and the field-scoped refusals) — moved back to `design/` with its CONNECTING panel redrawn as a link and a "Not now": decision 30 settled that a create may not depend on a tab a page cannot open, so there is no progress to show |
| `ReauthGate` | the recent-authentication challenge guarding `/settings` |
| ~~`AgentsV2`~~, `AgentDetailV2` | **`AgentsV2` adopted 2026-09-16 — moved to `design/Agents.dc.html`** (the plain list, row-as-link); `AgentDetailV2` superseded by the three-pane direction. Originally: `/agents` and `/agents/<slug>` redrawn around the whole data model (2026-09-16): grants shown per app as the editor's two lists, Allowed (grey) and Needs approval (amber), entries being roles or inline `family/pattern` items, with the reason an entry is dormant (archived app, undeclared role, item matching nothing today); per-app **reach** per §20 family computed by the gate's own matcher; credentials as tokens (Issue gains `expires_in`) plus bound OAuth clients; activity from `approval_list` and `audit_query { principal }`; **approved by the owner 2026-09-16** — the list is a plain list (after an in-place expansion was drawn twice and cut): one row per agent with one line of totals for access (apps, reach, how much needs approval, how much is dormant), credentials, activity, created, Delete; the whole row is the link to the agent page (stretched anchor, hover highlight, pointer, trailing chevron; Delete sits above it) — every per-app detail and every edit lives on the three-pane agent page |
| ~~`AppRecordingPane`~~ | **adopted 2026-09-17 into the app page's Recording pane** (`AppDetailPanes`, `MobileAppDetailStates`), by way of `AppRecordingDemo.html` and the three-pane grammar. Originally: a new **Recording** pane on `/apps/<slug>` (App group, dot marker lit when on): `log_bodies` as a switch, `redact` / `redact_results` as editable rows, what a recorded call keeps — the three `app_update` fields the page had no control for; Overview keeps identity only |
| `AgentsScaleOptions` | superseded 2026-09-16 by the agent page now in `design/` — an agent on 20 apps (2026-09-16, drawn for the owner to choose): the list row expansion four ways — A compact lines cut at eight with the rest on the agent page, B all twenty scrolling inside the row, C only dormant and approval-bearing apps as lines and the quiet majority as one line of names, D a pointer to the agent page past a threshold — and the agent page three ways — E one table with expandable rows and a filter, F the cards folded to a header line, G a paned page with a rail of apps; the row summary, the ordering (dormant, then approval-bearing, then by last call) and dimming of dormant rows are common to all |
| `AgentThreePane` | superseded 2026-09-16 by the agent page now in `design/` — G taken further (2026-09-16, exploration at the owner's request): the agent page as three panes — a rail of apps (amber dot: approval entries, dash: dormant) plus Credentials / Activity / Danger entries; a **listing** pane with everything the app advertises, the grant state read off each row (in Allowed / in Needs approval / allowed via role / direct, each with × to remove), Allow / Approval on hover, a Patterns group for entries that are not one item, and the unsaved bar at its foot; a **details** pane opening the selected item — the app page's expanded tool row plus a "For claude" block (standing, whether approval would apply, recent calls, the two actions). Folds the pair editor page and the picker palette into the agent page; the selection rides the URL; scripting-off is one round trip per click with the draft in hidden fields |
| `AgentThreePaneDemo.html` | **Rendered into `design/` on 2026-09-16 as `AgentDetail`, `AgentDetailPanes` and `AgentDetailStates` (headless capture of its states); it stays here as the clickable reference.** **chosen by the owner 2026-09-16 as the agent page's direction, and called polished after the rounds below.** Not a board: a clickable prototype of `AgentThreePane` with the gate's matcher, a per-app draft, the role three-way control, direct entries with ×, the "no effect while a role allows it" case, patterns typed into the filter, the unsaved marker in the rail and the save-or-discard prompt on switching apps; Save mimics `grant_set`, an emptied set removes the pair, and the app pane's foot carries an explicit **Remove from claude** button (owner, 2026-09-16: a link was too quiet; the filter also moved to its own full-width row under the title because the header read too compact), an inline confirm sentence in the same spot ("claude loses every entry on news. History stays; a waiting request expires."), the same empty `grant_set` underneath, landing on the grant step where the app is grantable again. The rail's Agent entries are live too: Credentials (tokens with an expiry select on Issue, the once-only reveal in the details pane, inline-confirmed Revoke — labelled **Remove** on an expired key, owner's call 2026-09-16: same `token_revoke` underneath, the row kept as history and dropped from the list, since the spec's one-verb rule read as a no-op on a dead key; clients read-only with Revoke pointed at Settings), Activity (waiting approvals with Approve / Reject, recent calls opening to their recorded bodies or the no-bodies sentence), Danger zone, and Grant another app as a step that uses panes 2 and 3 as one wide listing (owner's call: a picker with one choice per row does not need a details pane): each ungranted app as a card that says what the app does, with a plain **Grant** button (owner: "Start granting" was a process word) and a "show all N" that opens a scrollable list of its endpoints, each labelled with the roles that grant it and carrying an i marker whose hover shows what it does and its arguments; Grant opens that app's pane with an empty, unsaved set; a search bar at the top filters apps by name or description and by endpoint, opening the matching endpoints in place. Open it in a browser; it seeds nothing on the canvas. Wording (owner, 2026-09-16): the modes read **none · ask · allow**, in that order everywhere (the role control, the row actions, the details pane), and the lists **Allowed** / **Ask first**; every row — role, tool, prompt, resource, pattern — carries the same three-way control: solid when a direct entry sets the state, hollow when a role implies it with the highest-granting role named beside, and states below what a role grants disabled, since there is no deny; "disallow" was considered and rejected because the model has no deny entry and a row's third state cannot exclude an item a role already grants. The wire and the CLI keep `approval` |
| `AgentMobileDemo.html` | not a board: the agent page at 390 px as **three levels of navigation** (2026-09-16, owner's ask): level 1 is the rail (apps with their markers, Grant another app, Credentials, Activity, Danger zone, each row a chevroned tap target), level 2 the listing of what was tapped, level 3 the details of the row tapped there; a header with a back button names the level above (`‹ claude`, `‹ News MCP`) and carries the app's unsaved dot; the wide grant step has no third level (Grant lands on the app's listing); the top bar is the brand and a **hamburger** that slides in a **sidebar** from the right, the hamburger's own side, its close button at the same corner — the five nav entries, the Approvals count and Sign out — over a scrim (a drop-down was drawn first and replaced the same day at the owner's ask). Generated from `AgentThreePaneDemo.html` by a script, so rows, controls and details are byte-identical to the desktop prototype. Later the same day (owner): the level-1 header is the same back-plus-title bar as the other levels (no crumb, no repeated slug), a listing header never repeats the name the level header shows, and **Activity pages its calls** — twenty at a time with a *Load N more* row saying how many older calls the week still holds and that everything before is in Audit (the desktop demo carries the same, with a 64-call week as its fixture; on the real page the row is a plain link carrying `?calls=<n>` so it works with scripting off). **Approved by the owner 2026-09-16** as the phone rendering of the agent page and rendered into `design/MobileAgentDetail` and `MobileAgentDetailStates` (headless capture); it stays here as the clickable reference. |
| `BreadcrumbOptions.html` | the page breadcrumb five ways (2026-09-16, drawn after the owner found `Agents / claude / news` non-interactable and rejected bordered chips): A muted with chevrons and hover underline, B underlined ancestors, C blue links with slashes, D quiet pills, E the slugs as the page's own badges with the current one dark — **E chosen by the owner** for the desktop, then folded into the title line the same day — `Agents › claude` with the ancestor small and the slug big, the deeper path left to the rail and the listing — after the owner saw the crumb and the title naming the agent twice (the phone hides the crumb behind its level headers: a full path costs width a phone lacks); applied to `AgentThreePaneDemo.html` (the adopted boards in `design/` still show the old slashes until recaptured); the `agent` type pill beside the title went the same day — under "‹ Agents", the Agents crumb and the menu it said nothing the page did not, while the app page keeps its kind label (tunneled / proxied carries information) |
| `GrantEditorV2` | superseded 2026-09-16 by the agent page now in `design/` — the (agent × app) editor the other agent boards point at (2026-09-16): same page, same three-way choice per role, same `grant_set` — but each role row shows its patterns per §20 family and what they match today, and a reach card under the rows shows the resulting reach per family (allow wins over approval, chip names the winning role), recomputed on change; the proxied variant draws the undeclared-role refusal and the bare-list-grants-no-resources case; names the gap that the app page's Roles pane is read-only, so proxied roles still have no editor |
| `GrantEditorLists` | superseded 2026-09-16 by the agent page now in `design/` — the editor inverted (2026-09-16, owner's direction): two lists, **Allowed** and **Needs approval**, whose entries are role names or inline items `tool/<pattern>`, `prompt/<pattern>`, `resource/<uri-pattern>`, an **Add…** picker per list (a searchable palette over the catalog and roles, grouped by kind, each row saying where it already stands, staying open after an add so the next is one keystroke away; a native `<datalist>` without script), and the effective access per family beneath; the second panel spells the same set as the provider's `pmcp_grant` (its `allow` / `approval` sets already exist, §22), the CLI and the `grant_` rows — the YAML gets no spelling since §22 retires it — one data-model extension: a grant entry may be an inline family/pattern, which the gate takes as one more pattern in the per-family union. Competes with `GrantEditorV2`; one of the two gets retired |
| `AuditRedactStates` | masking from evidence (2026-09-16): every key in a recorded body on `/audit` and `/approvals/<id>` is a target; a popover adds its dotted path to the app's `redact` / `redact_results`, one literal-keyed entry per tool whose schema carries the path (named and pre-checked, never "every tool"), via `app_update`, forward-only and said so; the inverse popover on `‹redacted›` names the rule (schema `writeOnly` or config path) and offers Remove for the config kind |
| `AuditRedactLongList` | the mask popover when many tools carry the path: A collapses them to one pre-checked group row with a disclosure, filter and scroll region — **chosen by the owner 2026-09-16**; a B variant that offered the `.*` pattern when every tool takes the path was drawn, rejected and removed the same day, so the pattern stays a Recording-pane-only spelling; plus the Recording pane's map grouped by path so N literal entries read as one row |
| `RedactionEditorOptions` | the Recording pane's redaction editor driven by the schemas the page already holds (2026-09-16, drawn for the owner to choose), four ways: A pick a tool and tick fields in its input/output schema trees, with the audit popover's "also on N other tools" row; B every field of every tool in one table; C by path across tools, a tick masking it on every tool that takes it, expandable to per-tool ticks; D no editor, a Mask column on the Tools pane's existing Arguments table. In all four a tick writes a literal (tool, path) entry, writeOnly fields show pre-ticked and locked on tunneled apps, results are offered only where an output schema exists, and paths are never typed |
| `AppRecordingDemo.html` | **Adopted 2026-09-17: it is the Recording pane of `AppDetail`, rendered in the three-pane grammar on `AppDetailPanes` (desktop) and `MobileAppDetailStates` (phone); it stays here as the clickable reference for the editor's behaviour.** **approved by the owner 2026-09-16 (option C, with the scroll regions).** Not a board: a clickable prototype of the Recording pane in the app page shell with the by-path editor wired to real schema data — a tick on a path masks it on every tool that takes it (one literal entry per tool), the row goes indeterminate when only some are ticked, `writeOnly` fields on a tunneled app show locked, results are offered only where an output schema exists, the body logging switch, the proxied warning when logging is on with nothing masked, a draft with Save mimicking `app_update`, and a live preview of the map it would post. A select in the nav swaps between a proxied fixture, a tunneled one, and a 38-tool proxied one that shows each section becoming a scroll region past eight rows (sticky header, bottom fade, masked count, the filter to narrow it) |
| `AppThreePaneDemo.html` | **Rendered into `design/` on 2026-09-17 as `AppDetail`, `AppDetailPanes` and `AppDetailStates` (headless capture of its states), and transformed to `AppMobileDemo.html` for `MobileAppDetail` / `MobileAppDetailStates`; it stays here as the clickable reference.** **Adopted 2026-09-17 as the app page's direction** — the dispatch brief is `docs/superpowers/plans/2026-09-17-app-three-pane.md`, and where the two differ the brief wins. Not a board: a clickable prototype of the app page as three panes (2026-09-16, owner asked for it after the agent page) — rail: **Catalog** (Tools, Prompts and Resources folded into one listing with family groups and "none advertised" headers), Roles (owner, 2026-09-16: a role opens to the agent page's rows with one in-or-out mark at the right edge — locked for a role the app declared, live for one you defined; **New role** adds an owner-defined role by ticking items or adding patterns from the filter; the app's declaration wins on a name collision and the row says "app · replaced yours". This is a data-model extension: owner roles need storage beside the app's declaration, merged at filter time with the app's winning — a proxied app's roles are already all the owner's), Recording (the by-path editor as ordinary group rows — the body logging switch at the top of the pane beside the title, its state and unsaved badge on the summary line, the paths under sticky group headers as rows in the agent page's grammar — name on the left, status text beneath it, the unsaved badge and then the control at the right edge, per-tool rows the same — the filter in the header — with the details pane listing what ends up **masked before recording** per direction, each path with the tools it is masked on or declared writeOnly by, plus the what-is-kept facts; owner, 2026-09-16: the earlier wide form looked off, and the `app_update` JSON the details pane first showed under "What will be saved" read as the opposite of a mask in a pane about recording, so the raw op is gone), Overview (wide), Agents, Token, Danger zone (wide); Catalog rows open to arguments, output schema and the hub's four lines; a role opens to what it matches and who holds it; **Agents opens to the same grant editor the agent page has**, none · ask · allow per row, draft and Save, Remove at the foot. Granting a *new* agent is not started here (owner, 2026-09-16: it complicated the flow); the pane edits and removes existing grants, and a note points at the agent's page for a new one. One save-or-discard banner guards every draft, and the unsaved language is the agent page's (owner, 2026-09-16): an **amber dashed "unsaved" badge on every changed row, whatever the change** (owner, 2026-09-16: the earlier tint-by-state made a removal grey and read as nothing); a counted "N unsaved changes" in the foot; "was N of M tools" on the reach line; a blue dot on the rail entry, the listing row and the details-pane header that hold a draft — the rule being: a per-row change wears the badge, a container that holds a draft wears the dot. A nav select swaps a tunneled and a proxied fixture |
| `AppMobileDemo.html` | not a board: the app page at 390 px as **three levels of navigation** (2026-09-17), generated from `AppThreePaneDemo.html` by a script so rows, controls and details are byte-identical to the desktop prototype — level 1 the header and the rail as a chevroned list, level 2 a pane’s listing, level 3 a row’s details, each headed by a back button naming the level above; a wide pane (Overview, Danger zone, a proxied Token) has no level 3; the top bar is the brand and a **hamburger** sidebar over a scrim, as `AgentMobileDemo.html` has. Rendered into `design/MobileAppDetail` and `MobileAppDetailStates` (headless capture); it stays here as the clickable reference. |
| `AuditDirections.html` | not a board, and **the round behind the adopted one** (2026-09-20): `/audit` as **four whole-page alternatives** over the same fixture week, painted rather than interactive, differing in what the page is *for* — **A** the log with better rows ("what did my agents do · what was refused"), **B** summary first with the log on demand ("is anything abnormal · what changed"), **C** the session as the unit, **D** an investigation surface. Each names the questions it answers, and the research behind them is `docs/superpowers/reports/2026-09-20-audit-visualization-research.md`. Kept for the record: `AuditDemo.html` the next day is B, C and D made into one page with three views, and this is where the losing shapes are still visible |
| `AuditDemo.html` | **Adopted 2026-09-21 as the audit page's direction** ("this concept is insanely good" — owner) and **rendered into `design/` on 2026-09-21 as `Audit`, `AuditViews`, `AuditDetailStates` and `MobileAudit`** (headless capture, `docs/superpowers/plans/tools/capture-audit.mjs`); it stays here as the clickable reference. The dispatch brief is `docs/superpowers/plans/2026-09-21-audit-explorer.md`, and where the demo and the brief differ the brief wins. Not a board: a clickable prototype of `/audit` as an explorer — three readings of one filtered set (**Summary** with three tiles, three hand-written "worth a look" rules and top-N bars; **Sessions** as one row per `client.sessionId` opening a salience waterfall; **Events** as the log with related rows merged), a per-principal lane strip whose cell is the worst outcome in its hour, with a drag brush, **1h · 24h · 7d** presets, **Whole window**, a five-class legend and hatched not-loaded cells over the ceiling; a facet rail of 200 px with exhaustive counts and **Show all N**; the record as a 620 px drawer with search-within, the field table, JSON trees, the three no-bodies sentences and a chain's sibling timeline; **Export JSONL** carrying the selection; and the page states of the brief behind `?state=` (loading, failed, empty, nothing matching, over the ceiling, record loading, record gone). The phone rendering is a `@media (max-width:767px)` block in the **same file**, not a second demo. `AuditDemo.data.js` is the fixture week rewritten against the `record(` call sites in `server/src`, so every `detail` is one a writer actually records. Open it in a browser; it seeds nothing on the canvas |
| `SettingsRecording` | **not adopted (owner, 2026-09-16)**: with the per-app Recording pane approved, a cross-app switch table added a second place to flip one setting and no question the pane did not already answer; Settings stays sign-in and access. Kept for the record until the folder is pruned. Was: a third Settings group, **Records**, with one pane `/settings/recording`: every app's body logging as a per-row switch, masked-path and recorded-call counts, and the deploy-set limits (retention, body cap) read-only |

Known follow-ups: mobile variants for all of the above; §19.5's empty-state
copy points at `/apps` for creating an agent — the new IA puts that at
`/agents`, an amendment to make when the exploration lands.

**Unresolved behind the audit explorer** (raised by
`docs/superpowers/reports/2026-09-20-audit-visualization-research.md`, deliberately left
out of the 2026-09-21 dispatch, and both the owner's to rule on):

- **Does `outcome` split into a mechanism and a reason?** Today a `-32001` is one value
  with three sources (§7 keeps them indistinguishable on the wire, and the ledger copies
  that), so the page cannot say *why* a call was refused and the first "worth a look"
  sentence has to end at the outcome class. Splitting it would change what `record()`
  writes, what §7 discloses, and what the facet rail can group by — not a page decision.
- **Is there a weekly digest?** The explorer answers "what happened" when someone opens
  it; nothing in this round decides whether the hub also *tells* an owner once a week, or
  through what (push, mail, a page). The Summary view is where such a digest's content
  already lives, so the question is delivery, not drawing.
