# Orphan states of the web surface (2026-09-02)

> A state a page CAN render — a fixture, a component branch, a board panel — that no loader or route ever produces on a real request. Found by five tracers over every fixture and branch (192 states: 146 reachable), each orphan claim then put to a skeptic. Companion to `2026-09-02-roadmap-after-d15.md`; the register's G3/G4/G16/G9/G10/G30 already carry the two-factor, passkey-name and CLI-session ones — the rest are new. Fixture-quality notes (values fabricated for states that ARE reachable) are at the end.

## Confirmed orphans (40)

| # | Page | State | Selected by | Why no producer | Test that passes only by bypassing the loader |
|---|---|---|---|---|---|
| O1 | all panes | notice banner, tone `info` (`class="alert"`) | settings.tsx:111-116 `NOTICE_CLASS.info` | `ctx.notice` has exactly one producer: web.ts:1123 `notice: noticeOf(query)`. `noticeOf` (web.ts:1063-1077) returns only `tone:"success"` (line 1068 and passwordDone's 1089) or `tone:"danger"` (line 1073). grep for `tone: "info"` across server/src → zero hits outside the `Notice` type declaration (model.ts:111). No settings fixture sets it either. Nearest candidate — `passwordDone` — hard-codes success. | none |
| O2 | all panes | notice banner, tone `warning` (`class="alert alert--warning"`) | settings.tsx:111-116 `NOTICE_CLASS.warning` | Same single producer, same two tones. grep `tone: "warning"` in server/src → zero hits outside model.ts:111's type. No settings fixture draws it. | none |
| O3 | /settings/two-factor | TOTP mid-enrolment card, no error (QR image, base32 secret, six OTP boxes, Verify/Cancel) | settings.tsx:413-455 `if (enrollment)`; fixture totpEnrolling | KNOWN — adding facts. model.ts:2164 `enrollment: null`, unconditional; grep `enrollment` in server/src → only that literal, the type (model.ts:551-558) and settings.tsx's reads. THREE further reasons it could not work if it did render: (1) web.ts:844-845 `credential()` reads only `answered.ok`, so /two-factor/enable's `{totpURI, backupCodes}` body is discarded (two-factor/index.mjs:124-146 is where it is returned); (2) the card's Verify posts `paths.auth.totpVerify` = `/login/two-factor/verify-totp` (model.ts:421), mounted at web.ts:189-191 as `signInTranslation`, which redirects to /login or the landing page, never back to /settings/two-factor, and is outside the /settings prefix gate (web.ts:260-261); (3) that route reads `field(form,"code")` while the card's inputs are named `digit0`…`digit5` (settings.tsx:431-442), so it would submit an empty code. — skeptic: REFUTATION FAILED — confirmed orphan (known cause; I add facts). model.ts:2164 `enrollment: null` is an unconditional literal in the single returned object of `settingsProps`; I read the loader end to end (model.ts:2132-2173) — no earlier assignment, no spread that could carry it, no second return. `grep -rn enrollment server/src server/dev` → in src only model.ts:419-420 (a paths comment saying this card 'cannot render today'), model.ts:546/704 (the type), model.ts:2164 (the literal), settings.tsx:410-444 (the reads), identity.ts:1144/1291 (prose about TOTP enrolment surviving other ops, no props). I verified all three of the tracer's secondary facts myself and add a fourth. (1) `credential()` (web.ts:836-856) computes `const succeeded = answered !== null && answered.ok` and passes `answered` only to `redirectWith`, which copies `Set-Cookie` and nothing else (web.ts:862-866) — the JSON body IS discarded; better-auth returns `{method:"totp", totpURI, backupCodes}` at node_modules/better-auth/dist/plugins/two-factor/index.mjs:170-174, and `grep -rn "totpURI|backupCodes|otpauth" server/src server/dev` finds ZERO reads of either field in the hub. (2) the card's form posts `paths.auth.totpVerify` = `/login/two-factor/verify-totp` (model.ts:421), mounted web.ts:189-191 as `signInTranslation`, which ends in `redirectWith(outcome.twoFactorRedirect ? loginUrl(...) : landing, answered)` — and `landing = landingOf(form)` (web.ts:877-880) reads a `callbackURL` field the enrolment card does not render, so it defaults to `paths.apps`: Verify can only land on /login or /apps, never back on the pane. It is also outside the `/settings` prefix gate (web.ts:260-261). (3) that route sends `{ code: field(form,"code") ?? "" }` while the card's inputs are `name={`digit${i}`}` (settings.tsx:434) with no hidden `code` field — contrast login.tsx:123, which DOES render `<input type="hidden" name="code" data-otp-value />` for its script to fill; settings.tsx has exactly one inline script (ADD_PASSKEY_SCRIPT, settings.tsx:617) and it binds the Add-passkey button, so this form would submit an empty code. (4) NEW: `twoFactor()` is instantiated with no options (identity.ts:123), so `skipVerificationOnEnable` is falsy and `/two-factor/enable` does not even flip `twoFactorEnabled` (two-factor/index.mjs:135-148) — the enable POST leaves a `verified:false` row and a secret the owner is never shown. No test renders this state (web-pages.test.ts:3021 comments it as 'The one state no route reaches'), and no test imports server/dev/fixtures.ts at all (`grep -rn fixtures server/test` returns only harness prose). | none |
| O4 | /settings/two-factor | TOTP mid-enrolment card WITH `enrollment.error` (red sentence under the OTP boxes, aria-invalid on all six) | settings.tsx:440/444 `enrollment.error`; fixture totpEnrollError | Same unconditional `enrollment: null` (model.ts:2164). Additionally nothing anywhere writes an enrolment error string: grep `error:` inside any TotpEnrollment literal in server/src → zero. The only place a bad TOTP code produces a message is web.ts:803-811, which writes `/login?step=totp&error=…` — a query settingsProps never reads (model.ts:2178-2182 reads only `failed`/`field`, and only on the password pane). — skeptic: REFUTATION FAILED — confirmed orphan, strictly inside the previous one (both are guarded by the same `if (enrollment)` at settings.tsx:413, and `enrollment` is the unconditional `null` at model.ts:2164). I searched separately for anything that could ever write the error string: `grep -rn enrollment server/src` finds no object literal of type `TotpEnrollment` anywhere in src — the type is declared (model.ts:546-558) and read (settings.tsx), never constructed. The nearest real producer of a bad-TOTP-code sentence is `signInTranslation`'s refusal leg (web.ts:184-196), which redirects to `loginUrl({step:"totp", error: WRONG_CODE, ...})` — a `?error=` on /LOGIN — and `settingsProps` never reads `error`: its only query reads are `tokenKindOf` (`?kind=`), `settingsConfirm` (`?confirm=`) and `passwordErrorOf` (model.ts:2176-2182), which returns null unless `pane === "password"` AND `NOTICE_KEYS.failed` is present, and yields a `PasswordField`, not an enrolment error. Nothing bridges /login's query onto this pane. | none |
| O5 | /settings/two-factor | Backup-codes reveal card (the 8-chip grid, the amber "shown only once" line, Copy codes / Done) | settings.tsx:1051 `props.revealedBackupCodes ? <BackupCodesCard/> : null`, card at 546-570; fixture backupCodesRevealed | KNOWN — adding facts. model.ts:2165 `revealedBackupCodes: null`, unconditional; grep `revealedBackupCodes` in server/src → that literal, the type (model.ts:705) and settings.tsx:1051 only. Cause is the same discarded body (web.ts:844-845) for both producers, /two-factor/enable and /two-factor/generate-backup-codes. It also cannot ride the redirect: the whole flash protocol is NOTICE_KEYS (model.ts:614-622 — done/failed/reason/field/signedOut) and §15 keeps a secret out of a URL, so a reveal would need the render-not-redirect shape /apps/<slug>'s token_issue uses (web.ts:554-573). Third fact: even inside the orphan card, "Copy codes" (settings.tsx:562) is a dead control — the only clipboard script in the shell keys on `#copy-token` (layout.tsx:222) and this button has no id and no handler. — skeptic: REFUTATION FAILED — confirmed orphan (known cause; facts added). model.ts:2165 `revealedBackupCodes: null`, unconditional in the single return of `settingsProps`. `grep -rn revealedBackupCodes server/src server/dev` → model.ts:694-705 (prose + type), model.ts:2165 (the literal), settings.tsx:19 (comment) and settings.tsx:1051 (the read); nothing else. Same discarded-body cause as the enrolment card, and I verified it applies to BOTH producers: web.ts:296-300 mounts `paths.auth.backupCodesGenerate` through the same `credential()` helper, which reads only `answered.ok`; better-auth's regenerate returns its plaintext set in a body nothing here parses (`grep` for `backupCodes` in server/src → zero). It also cannot ride the redirect: the whole flash vocabulary is NOTICE_KEYS = done/failed/reason/field/signedOut (model.ts:614-622) and `noticeUrl` (web.ts:1042-1060) can set no other key, so a plaintext set would have to take the render-not-redirect shape /apps/<slug>/token_issue uses (web.ts:554-573) — the ONE route in the file built that way, with no twin under /settings. THIRD FACT CONFIRMED: 'Copy codes' (settings.tsx:562) is a dead control even inside the orphan card — the only clipboard code in the app is layout.tsx:222, `getElementById("copy-token")` + `getElementById("token-value")`, and this `<button type="button">` carries no id, no data attribute and no handler; settings.tsx's sole inline script (line 617) is the passkey one. | none |
| O6 | /settings/two-factor | backup-codes line, SINGULAR arm ("1 backup code remaining") | settings.tsx:495 `twoFactor.backupCodesRemaining === 1 ? "" : "s"`; fixture longNames (backupCodesRemaining: 1) | model.ts:2162 hard-codes `backupCodesRemaining: 0` on every enabled render, so the ternary can only ever take the plural arm. grep `backupCodesRemaining` in server/src → model.ts:543 (type), model.ts:2162 (the literal 0), settings.tsx:495. model.ts:2121-2131 states the ceiling itself: no endpoint reports the count, the codes live encrypted in a table §4 gives identity sole custody of. Consequence beyond the branch: an owner with 2FA on is always told "0 backup codes remaining · generated <today>", both halves false. — skeptic: REFUTATION FAILED — confirmed orphan. model.ts:2161-2162 is the only writer: `twoFactor: me?.user?.twoFactorEnabled ? { enabled: true, backupCodesRemaining: 0, generatedAt: ctx.now } : { enabled: false }` — the count is the literal `0` on every enabled render, so the ternary can only take the plural arm. `grep -rn backupCodesRemaining server/src server/dev` → model.ts:537/543 (type), model.ts:2122-2124 (the loader's own comment admitting both halves are invented), model.ts:2162 (the literal), settings.tsx:495 (the read), fixtures 345/534/591/603. TWO CORRECTIONS/ADDITIONS to the tracer. (a) The cited fixture does NOT actually draw this arm: `longNames` (fixtures.ts:595-603) sets `backupCodesRemaining: 1` but also `pane: "passkeys"`, and `TwoFactorCard` renders only under `case "two-factor"` (settings.tsx:1043-1053) — so the singular arm has no renderer anywhere, not a route and not even the states preview. (b) I checked whether the ENABLED arm itself is reachable, since that would be a bigger finding: it is, but only OFF-PAGE, so I do NOT call it an orphan. `twoFactor()` takes no options (identity.ts:123) so `/two-factor/enable` never sets `twoFactorEnabled` (two-factor/index.mjs:135-148); the only writer is verify-totp (two-factor/totp/index.mjs:206-208), whose code needs the secret the hub discards. BUT identity.ts:860 mounts better-auth as `app.all("/*")` under /api/auth (index.ts:389, AUTH_BASE_PATH identity.ts:1023), gated only by the Authorization-header rule — a cookie-bearing direct `POST /api/auth/two-factor/enable` returns the totpURI as JSON and a follow-up verify-totp does flip the column. The count staying `0` is unaffected either way. TEST BYPASS CONFIRMED as claimed: web-pages.test.ts:1170, case 27 ('§4/§13 · /settings's Enable two-factor and Regenerate backup codes render the password control the credential seam reads — each form, filled and submitted exactly as the page drew it, is ACCEPTED by better-auth instead of refused for a field no browser could send'), reaches the enabled card only via `enrollTwoFactor()` (web-pages.test.ts:576-580), a raw `UPDATE "user" SET "twoFactorEnabled" = 1` whose own comment says 'No hub route can'. It asserts the form is accepted and never reads the backup-codes sentence, so no test covers either arm of the ternary. | 27. §4/§13 · /settings's Enable two-factor and Regenerate backup codes render the password control the credential seam reads — each form, filled and submitted exactly as the page drew it, is ACCEPTED by better-auth instead of refused for a field no browser could send |
| O7 | /settings/passkeys | a passkey row named by its authenticator ("MacBook Touch ID", "YubiKey 5C") | settings.tsx:597 `{pk.name}`; fixtures passkeys / passkeysNeverUsed / longNames / confirmRemovePasskey | KNOWN — adding facts. model.ts:2220 `name: pk.name || "Passkey"`, and nothing ever supplies a name: settings.tsx:229-249's verify POST sends only `{response:{…}}` — no `name` key — so @better-auth/passkey stores null and every real row renders the literal "Passkey". grep `name` inside ADD_PASSKEY_SCRIPT → the string appears nowhere. Knock-on: the Remove dialog's title (settings.tsx:957 `Remove passkey “${confirm.name}”?`, fed by model.ts:2260 `name: row.name`) always reads “Passkey”, so an owner with two cannot tell which one the dialog is about. — skeptic: REFUTATION FAILED — confirmed, plus three new facts the tracer did not have. (1) The ceremony body is name-less as claimed (settings.tsx:229-249 posts `{response:{…}}` only; `grep -n "name" ADD_PASSKEY_SCRIPT` → nothing), and I read the plugin to see what it does with that: node_modules/@better-auth/passkey/dist/index.mjs:367 `let resolvedName = ctx.body.name || void 0;` then :390 `name: resolvedName` — so the column is written NULL unless the caller names it or an `options.registration.afterVerification` hook returns one, and identity.ts:130 mounts `passkey({ rpName, origin })` with neither hook. model.ts:2220 `name: pk.name || "Passkey"` then renders the literal for every real row. (2) 'Named by its authenticator' is not producible by ANY code, not just this code: WebAuthn hands back no authenticator name. What the DB does hold is `aaguid` — migrations/0001_auth.sql:108 declares the column, the plugin writes it (dist:399), `/passkey/list-user-passkeys` returns whole rows (dist:557 `adapter.findMany({model:"passkey"})` with no select), and the same package exports `getAuthenticatorName(aaguid)` (dist:768, exported at :804) whose own docstring is `passkey.name || getAuthenticatorName(passkey.aaguid) || "Passkey"`. model.ts:2216's `BetterAuthPasskey` type declares only id/name/createdAt, so the loader drops the one field that could produce this state — a one-line fix in passkeyRow, no ceremony change. (3) The knock-on the tracer names is confirmed: model.ts:2260 `name: row.name` → settings.tsx:957 dialog title, always “Passkey”. TESTS: two bypassers, not one — the named §13 test (web-pages.test.ts:2687) and `§13 · each passkey row carries its own added stamp: two passkeys with the same name and different createdAt render two different rows` (web-pages.test.ts:2718). Both call plantPasskey (helper at web-pages.test.ts:6229), a raw `INSERT INTO "passkey" ("id","name",…)` that writes the name the ceremony can never write; neither test would notice the loader printing "Passkey" if the column could only ever be NULL in production. | §13 · /settings/passkeys lists one row per passkey under the name the authenticator reported, and the rail's Passkeys marker is the number of rows the pane listed · with none, the pane renders "No passkeys yet. Add one to sign in without a password." and the marker reads 0 (the twin) |
| O8 | /settings/sessions | session row labelled "<client> · device flow" (a CLI session) | settings.tsx:626-628 `sessionLabel` — `session.source === "cli"`; fixture settingsBase.sessions[1] (`ses_4a77c0`, source "cli"), drawn by the sessions/default/longNames/confirmRevokeSession fixtures | NEW. model.ts:2290 `source: "web"` — a literal, unconditional, on every row. grep `source: "cli"` in server/src → zero hits (only server/dev/fixtures.ts:233 and the type at model.ts:579). model.ts:2114-2117 states the cause: nothing better-auth stores distinguishes a device-flow session from a browser one; the distinction identity enforces is the cookie's signature, not a column. Nearest candidate is `BetterAuthSession` (model.ts:2278-2283), which declares only id/createdAt/updatedAt/userAgent. Consequence: a real CLI session is listed as an ordinary web row, and the revoke dialog's title (settings.tsx:1001) says so too. Type-level note: `SessionRow.source` (model.ts:579) is a two-member union with one inhabitant in production. — skeptic: REFUTATION FAILED — confirmed independently. `grep -rn "source:" server/src/` returns exactly four page-relevant hits: model.ts:579 (the type), :2115 (the comment admitting it), :2290 `source: "web"` (an unconditional literal), and unrelated oauth.ts/gateway.ts lines. `grep -rn "SessionRow|sessionRow(" server/src/` shows model.ts:2285 `sessionRow()` is the SOLE constructor and model.ts:2152 its sole call site, inside settingsProps — no second loader, no mobile variant. The upstream shape genuinely carries nothing to switch on: `BetterAuthSession` (model.ts:2278-2283) is id/createdAt/updatedAt/userAgent, and the only thing a device-flow session could differ in is `userAgent`, which feeds `client`, not `source`. The refutation I most expected to land — that the suite exercises a genuine CLI session — actually reinforces the claim: web-pages.test.ts:613-635's `threeSessions` helper mints a real device-flow session via `deviceFlowToken(actor.cookie)` and asserts `whoami` works, so a true CLI session is rendered by the pane under test and still draws as an ordinary web row. `grep -n "device flow" server/test/worker/web-pages.test.ts` finds it only in that helper's prose comment — no assertion anywhere on the rendered label, which is why the suite is silent. `SessionRow.source` (model.ts:579) is a two-member union with one production inhabitant, and settings.tsx:626's whole function plus the revoke dialog's title (settings.tsx:1001) are dead weight behind it. | none |
| O9 | /settings/sessions?confirm=revoke-session&id=… | same dialog falling back to `confirm.client` for its title (the `: confirm.client` arm) | settings.tsx:1001 `matched ? sessionLabel(matched) : confirm.client` | NEW (benign/defensive). The only producer of a `revoke-session` confirm is model.ts:2250-2252, which builds it from a row it just found in `sessions`; settings.tsx:1119 passes that very same array into the dialog, so `matched` is always defined and the fallback is dead. There is no second construction site: grep `kind: "revoke-session"` in server/src → model.ts:2252 only. | none |
| O10 | /apps | notice with tone "info" — the bare `.alert` class and the info glyph | TONE_CLASS.info (apps.tsx:212) and NoticeIcon's fallback arm (apps.tsx:203-209) | noticeOf (web.ts:1063-1077) is the ONLY Notice constructor for a shell page and emits exactly two tones: "success" (1068) and "danger" (1073); passwordDone (1089) is "success" and /settings-only. `grep -rn "tone:" server/src/` outside pages/format.ts finds only web.ts:1068/1073/1089 and approvals.tsx's own badge Tone (a different type, never assigned to Notice). ctx.notice is set once, at web.ts:1122 `notice: noticeOf(query)`. No fixture selects it either. | none |
| O11 | /apps | notice with tone "warning" — `alert alert--warning` and the triangle glyph | TONE_CLASS.warning (apps.tsx:214) and NoticeIcon's warning arm (apps.tsx:184-194) | Same search as the info tone: noticeOf never returns "warning". The tone exists in the Notice union (model.ts:111) and in the two class tables (apps.tsx:211, format.ts:53) but has no writer. — skeptic: REFUTATION FAILED — confirmed by the same searches as the info tone (noticeOf has no warning return; shell()/context() are the only writers of the prop; a whole-tree `grep -rn "tone" server/src/` finds no fourth constructor). But the tracer's "has no writer" is incomplete in a way worth recording: a fixture DOES select this tone — fixtures.ts:1584-1590 `approvals.decideFailed`, `{tone:"warning", title:"That request is no longer pending", message:"It expired at 15:12:31…"}` — on /approvals rather than /apps, so the preview and the board both draw a warning banner nothing can write. Worse than unreachable, it is the WRONG tone for the event it depicts: a decision that raced and lost goes through web.ts:1008 `noticeUrl(back, name, outcome)` with a `reason`, which lands on noticeOf's failed arm (web.ts:1071-1076) and renders tone "danger" with title "<Op> failed". So the warning triangle in the preview is a fixture-quality defect on top of the orphan: fix the tone by writing one, or delete the branch and the fixture together. | none |
| O12 | /apps | danger notice with NO title | apps.tsx:224 falsy branch with tone==="danger" | web.ts:1072-1075 always sets `title: \`${humanize(failed)} failed\``; `failed` is non-null on that path by construction (1071). No writer produces a titleless danger notice. — skeptic: NOT REFUTED. Traced the whole plumbing: AppsProps.notice comes from shell() (model.ts:1363 `notice: ctx.notice`), ctx.notice from context() (web.ts:1123 `notice: noticeOf(query)`), and noticeOf is the ONLY reader of the flash. `grep -rn 'tone: *"danger"|Notice\b' server/src --include=*.ts --include=*.tsx` finds exactly two Notice constructors in the whole tree: web.ts:1063 noticeOf and web.ts:1086 passwordDone. noticeOf's danger arm (web.ts:1072-1076) unconditionally sets `title: `${humanize(failed)} failed``, which is truthy even if humanize returns "" (the string would be " failed"). passwordDone is tone success. approvals.tsx:69-73's `tone: "danger"` is a STATUS BADGE, not a Notice, and never reaches this prop. Every /apps notice writer routes through noticeUrl: web.ts:1008 (dispatch), :567 (token_issue), :1162 and :1166 (connectRedirect's `"No such app."` and beginConnect refusal) — all of them go back through noticeOf, so all of them carry a title. TWO CAVEATS THE TRACER DID NOT STATE. (a) The titleless ARM itself is live: noticeOf's success arm (web.ts:1067) returns `{tone:"success", message:...}` with no title, so `notice.title ? ... : null` is exercised on every successful /apps mutation. Only the danger x no-title CELL is dead — this is a dead combination, not dead code, so the fix is a type change (make title required on danger) rather than a deletion. (b) A larger orphan sits in the same component and was not claimed: TONE_CLASS/NoticeIcon carry `info` and `warning` arms (apps.tsx:211-215, and the same table in settings.tsx:111, approvals.tsx:78, audit.tsx:54, format.ts:53) and NOTHING in the codebase ever constructs a Notice with tone info or warning — the two constructors emit only success and danger. That is four copies of two dead palette arms. | none |
| O13 | /apps/new | name field error — `.field-error` under Name and aria-invalid="true" on #app-name | errors.name (app-new.tsx:94-95); fixture appNew.errors | errors is only ever {} (web.ts:457) or createErrors(reason) (web.ts:509). createErrors sets `name` only when the reason contains the literal `"name"` (web.ts:1247-1248). Enumerated every refusal app_create can reach: `grep -rn 'invalid(`|invalid("|RegistryRefusal(' server/src --exclude-dir=pages` gives admin.ts:216,222,229,239,246,251,265,362,379,618,920,1326 and registry.ts:893,897,906,1157,1168,1390,1396,1412,1415,1433,1465 — none contains `"name"`. `name` is optional (admin.ts:650) so `"name" is required` cannot fire, and web.ts always sends it as a string (appNewForm coerces, model.ts:1852) so `"name" has the wrong type` cannot either. Nearest candidate registry.ts:1168 spells `"roles" names "<role>"…` — the substring is `names "` , not `"name"`, and web.ts never sends `roles` on a create (web.ts:496-503). — skeptic: NOT REFUTED, and I found the tracer's argument slightly understated. `grep -n 'createErrors' server/src/web.ts` gives exactly two hits: the definition (1246) and the single call site (509, the app_create refusal arm); web.ts:457 is the only other AppNewErrors writer and it is the literal `{}`. So errors.name requires a reason string containing the literal `"name"` (web.ts:1247). Enumerated every message app_create can throw. (1) parseInput (admin.ts:213-234) names the OP's own field, so `"name" is required` and `"name" has the wrong type` are the two messages that WOULD match — and both are structurally unreachable from the browser: APP_FIELDS.name is `optional: true` (admin.ts:650), web.ts:503 always passes `name: draft.name`, and appNewForm (model.ts:1849) is `query.get("name") ?? ""` — always a string — because formFields (web.ts:1224) drops any non-string form value before formQuery builds the URLSearchParams. There is no route by which `name` arrives absent or non-string. (2) `coerce` for kind "text" (admin.ts:245-252) only adds `"name" is not one of the values this tool accepts`, and APP_FIELDS.name declares no `values`. (3) admin.ts's own throws: 362 and 379 ECHO THE SLUG VALUE in quotes (`the slug "${slug}" is reserved...`), which is a real value-echo vector the tracer missed — a create with slug literally `name` would emit a message containing `"name"` and be keyed under the Name field. It still cannot happen: assertSlugNotARoute only fires for RESERVED_APP_SLUGS, which app-routes.ts:43-45 derives from APP_STATIC_SEGMENTS = ["new","connect"], and assertSlugNotReserved only fires for PMCP_SLUG = "pmcp". (4) registry.ts refusals (grep `invalid(|RegistryRefusal(`): fields slug/upstreamUrl/roles/capabilities/redact/redactResults/kind — RegistryRefusal's message is `"${field}" ${reason}` (registry.ts:1487), so none spells `"name"`; the value-echoing ones (1157/1168 roles, 1415 capabilities, 1433 redact keys) echo values web.ts never sends on a create (web.ts:496-503 sends only slug/kind/name/endpoint/auth). No test touches it: `grep -n 'app_create' server/test/worker/web-pages.test.ts` has one hit (5567) and it only asserts the form's op name; nothing in server/test asserts a name/endpoint field error. | none |
| O14 | /apps/new | endpoint field error — `.field-error` under Endpoint and aria-invalid="true" on #app-endpoint | errors.endpoint (app-new.tsx:127-129); fixture appNew.errors; board panel AppNewProxiedStates.dc.html "ENDPOINT ERROR" ("Not a valid https URL.") | createErrors keys on the literal `"endpoint"` (web.ts:1247). The ONLY endpoint refusal is registry.ts:897 RegistryRefusal("upstreamUrl", "is required for a proxied app"), whose message is `"upstreamUrl" is required for a proxied app` — no match, so it lands in errors.form. There is no URL-format check at all: registry.createApp (registry.ts:889-935) only tests `!draft.upstreamUrl`, and admin's field is a bare optional text (admin.ts:652). `type="url"` in the markup is browser-side only. So both the fixture's message and the board's "Not a valid https URL." name a validation the hub does not perform. — skeptic: NOT REFUTED. Confirmed the tracer's central fact and its cause: the hub renames the field at the admin/registry seam. admin.ts:626 `proxyFields` maps `input.endpoint` -> `upstreamUrl`, so every refusal registry raises about it is `"upstreamUrl" ...` (registry.ts:897, message `"upstreamUrl" is required for a proxied app` via RegistryRefusal's `"${field}" ${reason}` at 1487) — no substring `"endpoint"`, so createErrors falls through to `{form}` (web.ts:1250). `grep -rn '"endpoint"' server/src --include=*.ts` outside pages/ returns exactly ONE hit — web.ts:1247, the createErrors key list itself. Nothing else in the tree writes that token into a message. Confirmed there is no URL-format validation anywhere on the create path: registry.createApp (registry.ts:889-935) tests only `proxied && !draft.upstreamUrl`, and admin.ts:652 declares endpoint as `{ kind: "text", optional: true }` with no `values` and no pattern, so the only `"endpoint"`-bearing message parseInput could emit is `"endpoint" has the wrong type` (admin.ts:239) — unreachable, because formFields (web.ts:1224) keeps only string values and appNewForm (model.ts:1850) defaults to "". `type="url"` on the input (app-new.tsx:124) is the browser's check and never reaches the server. So both the fixture's "Enter a full https:// URL." and the board's "Not a valid https URL." describe a validation the hub does not perform — the real refusal for a blank endpoint is the whole-form banner reading `"upstreamUrl" is required for a proxied app`. No test coverage (see the /apps/new name-error entry). | none |
| O15 | /apps/new | more than one error at once (the form banner plus name/slug/endpoint field errors together) | fixture appNew.errors — errors={name,slug,endpoint,form} | web.ts:1246-1251 createErrors returns a single-key object on every path: it `return`s inside the loop on the first matching field, else returns {form}. attempt (web.ts:1020) surfaces one HubError message, and app_create throws on the first violation. No code path can put two keys in AppNewErrors. — skeptic: NOT REFUTED. Read createErrors end to end (web.ts:1246-1251): `for (const key of ["slug","name","endpoint"]) if (reason.includes(`"${key}"`)) return { [key]: reason }; return { form: reason };` — it RETURNS inside the loop, so the result is a one-key object on every path, and `{form}` is the else, never an addition. The only other AppNewErrors value in the tree is the literal `{}` at web.ts:457 (`grep -n 'createErrors' server/src/web.ts` = 509 and 1246 only). Upstream it is single-valued too: `attempt` (web.ts:1027-1033) returns one `{reason}` carrying one HubError message, and app_create fails fast — parseInput throws on the first bad field (admin.ts:229/239) and registry.createApp throws on the first assert (registry.ts:891-908), so there is no accumulate-then-report path anywhere on this route (contrast registry.ts:1396, which DOES join violations, but only for `roles`, which web.ts never sends on a create). AppNewErrors' type (model.ts:1004) permits four keys; the producer can populate exactly one. | none |
| O16 | /apps/new | "CONNECTING" interstitial — "Connecting to Linear… Finish signing in, in the tab that just opened. This link expires in about 10 minutes." | board panel AppNewProxiedStates.dc.html | AppNewStep has exactly two members (model.ts:1013-1015) and app-new.tsx:251 renders FormCard or CreatedCard only — there is no third card. A proxy+oauth create never renders anything: web.ts:514-516 hands off to connectRedirect, which 303s straight to the provider (web.ts:1169). No prop, no template, no fixture. — skeptic: NOT REFUTED — this one is orphaned at the type level, the strongest of the six. AppNewStep is a two-member union (model.ts:1013-1015: `{kind:"form",...} | {kind:"created",...}`) and app-new.tsx:251-255 is a binary ternary rendering FormCard or CreatedCard; a third card would not type-check without a schema change. Read the board panel (design/AppNewProxiedStates.dc.html:157-172) — its copy ("Connecting to Linear…", "This link expires in about 10 minutes") appears nowhere in server/src: `grep -rn 'CONNECTING|Connecting to' design/*.dc.html` finds it only in that board, and the strings are absent from the page and from fixtures.ts. Confirmed the real flow renders nothing at all for a proxy+oauth create: web.ts:514-516 returns connectRedirect(), which resolves the app row, calls beginConnect and 303s straight to the provider (web.ts:1169 `return c.redirect(String(started.value), 303)`) — the browser never sees an /apps/new body on that path. Its two failure arms redirect to /apps with a notice (web.ts:1162, :1166), not to an interstitial. The "about 10 minutes" is the OAUTH_STATE_TTL the board narrates but no page ever shows. | none |
| O17 | /apps/<slug> | Overview "Created" rendered as the em dash | app-detail.tsx:666 `props.overview.createdAt === null ? "—"` | The only writer is model.ts:1626 `createdAt: row.kind === "builtin" ? null : row.createdAt`, and `row.kind==="builtin"` cannot happen here: appDetailProps returns null at model.ts:1527 when registry.getApp is null, and registry.ts:845 `if (slug === PMCP_SLUG) return null` — so the builtin never reaches app_get on this page (web.ts:483 then answers the 404). Both real variants carry a concrete createdAt (admin.ts:543/551). No fixture selects it either — every appDetail fixture sets a literal date. | none |
| O18 | /apps/<slug> | a grant chip built from an UNPARSEABLE suffix (`<role>:something-else`) | grantChip's `at >= 0 && slice !== "approval"` path (model.ts:1647-1657) | agent_list is the only source (model.ts:1531) and admin.ts:598 emits exactly `entry.role` or `${entry.role}:approval`; grantEntries (admin.ts:613-621) refuses anything else on the way in with `"roles" takes a role name or "<role>:approval"`. Dead branch — and its doc comment ("shown as it was stored rather than silently read as an allow grant") describes the opposite of what the code does: it would strip the suffix and render the chip as allow. — skeptic: CONFIRMED, and double-locked — the tracer only found the outer lock. Searches I ran: `grep -rn "INSERT INTO grant_" server/src server/migrations` → ONE writer, registry.ts:1181 inside setGrants; `grep -rn "setGrants" server/src` → one non-test caller, admin.ts:1060, which builds its entries with grantEntries (admin.ts:613-621) and throws `"roles" takes a role name or "<role>:approval"` on any other suffix; `grep -rn "grant_" server/migrations/*.sql` → only the 0007 column renames, no seeded rows. SECOND lock the tracer missed: setGrants itself calls `assertRoles({ [entry.role]: [] })` (registry.ts:1163) → validateRoles → `ROLE_NAME_CHARSET` = `[a-z0-9_-]{1,N}` (registry.ts:351), so a role name containing ':' cannot be stored even by a direct setGrants call (the only such callers are server/test/harness/seed.ts:396 and three tunnel/registry tests). Read path is likewise single-sourced: model.ts:1572 `chips: held.map(grantChip)` where `held = agent.grants[slug]` from agent_list, whose only emitter is admin.ts:598 `entry.mode === "approval" ? `${entry.role}:approval` : entry.role`. So the string is always `role` or `role:approval`. The tracer's doc-comment finding also holds: model.ts:1644-1646 says the suffix is "shown as it was stored", but the code sets `role = spelled.slice(0, at)` and `mode = "allow"` — it would strip the suffix and render the chip as a plain allow grant, the opposite of the comment. | none |
| O19 | /apps/<slug> | app-detail notice with tone "info" or "warning" | alertClass (format.ts:52-62) via app-detail.tsx:999 | Same absence as on /apps: noticeOf (web.ts:1063-1077) emits only success and danger. The info and warning rows of ALERT_CLASS have no writer. — skeptic: CONFIRMED, with one narrowing the tracer should carry. The page's notice has exactly one source: shell() copies `notice: ctx.notice` (model.ts:1363), and ctx.notice is assigned once, web.ts:1123 `notice: noticeOf(query)`. noticeOf (web.ts:1063-1077) returns `{tone:"success"}`, `passwordDone(...)` (also success, web.ts:1089) or `{tone:"danger"}` — nothing else. Searches: `grep -rn "tone:" server/src` → web.ts:1068/1073/1089 only (approvals.tsx:69-73 sets a different type, the OutcomeBadge `Tone`, not Notice["tone"]); `grep -rn "notice" server/src/pages/model.ts` → the type at 130/1330 and the one assignment at 1363; `grep -rn "warning" server/src/web.ts` → 0 hits, so setGrants's returned warnings (registry.ts:1170) never become a notice either. NARROWING: the `alert--warning` CLASS is not dead on this page — app-detail.tsx:990-995 hardcodes `class="alert alert--warning"` for the archived banner. What is dead is the info and warning rows of ALERT_CLASS (format.ts:53,55), i.e. a Notice ever carrying those tones. Report it as "Notice.tone's info/warning members have no writer", not as "the warning palette is unreachable". | none |
| O20 | /apps/<slug> | board control **Edit grants** on the Agents pane (drawn three times in design/AppDetailPanes.dc.html) | board panel only | AppAgentRow (model.ts:927) carries no control and AgentsPane (app-detail.tsx:713-752) renders none — the code comment at app-detail.tsx:706-712 records the removal as deliberate (§13, the agent×app editor is deferred, as is design/GrantEditorStates.dc.html as a whole). Documented, not a defect; listed so the board is not read as a spec. — skeptic: CONFIRMED as board-only, with a count correction. AppAgentRow (model.ts:927) is `{slug, description, chips}` — no control field; AgentsPane (app-detail.tsx:713-752) renders two <td>s (Agent, Granted roles) and no third; the removal is documented at app-detail.tsx:706-712 ("there is no **Edit grants** control because the (agent × app) editor is deferred too"). CORRECTION: `grep -o "Edit grants" design/AppDetailPanes.dc.html | wc -l` → 2, not 3 (one per agent row: claude, pi); `grep -o "Edit grants" design/AppDetail.dc.html | wc -l` → 0; `grep -rln "Edit grants" design/*.dc.html` → AppDetailPanes only. The board does draw an empty third header cell for the control column, which is probably what the "three times" in the code comment counted — so app-detail.tsx:711's own count is off by one. Documented deferral, not a defect; the correction is worth carrying so the comment isn't trusted as an inventory. | none |
| O21 | /apps/<slug> | board top-level nav item **Agents** (design/AppDetail.dc.html's header) | board panel only | NavSection is four values (model.ts:101) and layout.tsx:38-43 NAV lists Apps/Audit/Approvals/Settings. No /agents route exists in web.ts; app-detail.tsx:737 renders an agent slug as text "because /agents/<slug> is deferred". — skeptic: CONFIRMED. NavSection is exactly four members (model.ts:101 `"apps" | "audit" | "approvals" | "settings"`), and layout.tsx:39-44 NAV lists Apps/Audit/Approvals/Settings — a fifth item is a type error, so no props can select it. No /agents route: the full GET/POST table in web.ts (165-673) mounts /login, /device, /settings*, /audit, /audit/export.jsonl, /approvals*, /apps*, /oauth*, /manifest.webmanifest, /sw.js, /styles.css and nothing under /agents; `grep -rn "agents" server/src/web.ts` returns no route. app-detail.tsx:737 renders the agent slug as plain text with the reason inline. The board draws it at design/AppDetail.dc.html header line 27, between Apps and Audit. Board-only, deferred by design. | none |
| O22 | /audit | bodyStubs — ‹blob image/png · 4.2 MB› / ‹oversize · 2.1 MB› placeholders (fixture `bodyStubs`) | AuditEventRow.args/result carrying a BodyStub; audit.tsx:117-129 stubLabel/renderBody | The DATA is real: audit.ts:248-256 stores an over-cap body as one `oversize` stub and an unstructured content block as a `blob` stub, and the JSONL export renders them (test 14's export half passes honestly). But on the PAGE renderBody is called only from EventDetail (audit.tsx:289-291) — same ?expand= chain. The rows themselves are reachable; the stub rendering is not. — skeptic: CONFIRMED, and the tracer UNDERSTATED it. Searches I ran: `grep -rn "expand" server/src` → the only functional hit is model.ts:1980 `expandedId: positive(ctx.query.get("expand")) ?? null`; `grep -rn "expand=" --include=*.ts --include=*.tsx --include=*.js --include=*.md .` (repo-wide) → two hits, both in docs/superpowers/plans/2026-09-02-roadmap-after-d15.md (line 243 and ledger row G1: "chevron drawn, nothing sets ?expand=; auditWith cannot spell it"). Structurally impossible to emit: AuditLinkQuery (model.ts:154-157) is `Pick<AuditQuery, principal|app|event|tool|session|since|until|limit|offset>` — no `expand` member — and every href on the page routes through auditLink (audit.tsx:243-245) → paths.auditWith (model.ts:244). audit.tsx ships no <script> (`grep -rn "<script" server/src/pages/*.tsx` → app-new, approvals, apps, layout, login, settings only); layout.tsx's only always-present script registers the service worker, which has no fetch handler (web.ts:668). CORRECTION to the tracer: the JSONL export does NOT render these placeholders. audit.ts:400 enqueues `JSON.stringify(row)` — the raw `{stub:"blob",contentType,bytes}` object — and test 14 asserts exactly that raw shape. So stubLabel/renderBody (audit.tsx:117-129) has exactly ONE call chain in the entire product: EventDetail (289-291) ← EventRow's `isOpen` (353) ← expandedId. The placeholder STRING is wholly orphan, not half-honest. ADDED FACT: even the `bodyStubs` fixture never draws the oversize half — `expandedId: 41290` (fixtures.ts:2069) opens the blob row, while the `{stub:"oversize",bytes:2202009}` args live on row 41289 (fixtures.ts:2039), which stays collapsed; only one row expands per render, so ‹oversize · 2.1 MB› is unrendered in the states preview too. Strict-definition nuance: model.ts:1980 IS a loader line that produces the prop from a real GET /audit?expand=<id>; what is absent is anything that emits such a URL. TESTS BYPASSING: 'a row's client session id links back to this same view as ?session=…' (test 15) is ALSO on this chain — the tracer missed it; both it and test 14 synthesize the URL through the `auditPath` helper (server/test/worker/web-pages.test.ts:5838), whose comment 'spelled the way a link on the page spells it' is false for `expand`. | 14. §15 · an exported row carries its recorded bodies post-redaction… (its page half is the synthesized ?expand= URL; its export half is genuine) |
| O23 | /audit | empty histogram — "No events in this window." and the suppressed day axis (fixture `empty`, histogram.buckets: []) | audit.tsx:523 `histogram.buckets.length === 0`; axis gated at audit.tsx:537 | model.ts:2072-2090 auditHistogram starts from `new Array(AUDIT_BUCKETS).fill(0)` (AUDIT_BUCKETS = 24, model.ts:1944) and returns `counts.map(...)` — 24 buckets on every path, with no early return; grep `histogram:` in model.ts shows auditProps:1978 as the only producer of the prop. A real empty window therefore draws 24 zero-height bars AND the day axis, never this copy. — skeptic: CONFIRMED. auditHistogram (model.ts:2088-2104) reads end to end: `const counts = new Array<number>(AUDIT_BUCKETS).fill(0)` with AUDIT_BUCKETS = 24 (model.ts:1930), no early return and no filtering of the array, returning `counts.map((count, at) => …)` — length is unconditionally 24 on every path, including a zero-row scan. `grep -rn "AuditProps" server/src server/dev` → auditProps (model.ts:1951) is the sole loader and fixtures.ts:2071 the sole other constructor; `grep -n "histogram" server/src/pages/model.ts` → auditProps:1979 is the only place the prop is set. A genuinely empty window therefore renders 24 bars at `height:0%` (peak 0 → audit.tsx:531) AND the full uniqueDays axis (537-546), never the empty copy. ADDED FIXTURE-QUALITY NOTE on the same fixture: `empty` declares `bucketMs: 6 * HOUR` under a 1h window (since 2026-08-24T13:47Z → until NOW 14:47Z, fixtures.ts:1974-1975, 65-66), but the loader computes `Math.max(60_000, Math.ceil(3_600_000/24))` = 150 000 ms — so the chart caption '6-hour buckets' (audit.tsx:519) is fabricated in that fixture as well. | none |
| O24 | /audit | custom range — segmented control with no aria-current, tiles reading "… vs previous period" | AuditFilters.range === "custom" (rangeOf, model.ts:2035-2041); audit.tsx:261 aria-current, audit.tsx:82-84 rangeNoun default. No fixture. | range is computed only by `rangeOf(until - since)` (model.ts:2003) and every navigable link carries an exact preset span: RangeSegment emits since = now - RANGE_MS[key], until = now (audit.tsx:260); the pager, Load more, session link, Export and the limit select all forward since/until unchanged (baseQuery audit.tsx:229-241; hidden fields audit.tsx:409-410). The one control that could produce another span — the date field at audit.tsx:423 — is `readonly` and has no `name`, so it is neither editable nor submitted (roadmap G11). Only a hand-typed since/until pair reaches "custom". — skeptic: CONFIRMED. `grep -rn "auditWith|paths\.audit|auditExport" server/src` → every hit is inside audit.tsx, plus layout.tsx:41's bare nav href and model.ts's own definitions; `grep -rn '"/audit|`/audit' server/src` → only model.ts:221/245/263. No other page or module links into /audit with a window at all. Inside the page: auditLink → baseQuery (229-241) forwards filters.since/filters.until verbatim, and the ONLY override that touches them is RangeSegment (258-265), which writes `since = nowMs - RANGE_MS[key], until = nowMs` — an exact RANGE_SPAN_MS span. The GET form re-posts the same pair as hidden inputs (409-410); the limit select is `form="audit-filters"` (608) so it re-posts them too; the date field (423) is `readonly` with no `name`, so it is neither editable nor submitted (roadmap G11). rangeOf (model.ts:2024-2029) yields "custom" only for a span matching no preset, and auditFilters (1994-1997) falls back to the 24h preset unless BOTH since and until survive `positive()`. Route table check (`grep "app\.(get|post)" server/src/web.ts`, 35 mounts): /audit has two GETs, no POST, and nothing anywhere redirects to it — so no server-built URL can carry an odd span either. Hand-edited query string only. No fixture, no test. | none |
| O25 | /audit | notice (any tone) — the alert block at audit.tsx:383-390 | AuditProps.notice via shell(ctx,"audit") → ctx.notice (model.ts:1363). No audit fixture sets one. | ctx.notice is only ever `noticeOf(query)` (web.ts:1122), which reads the `done`/`failed` keys; those keys are written only by noticeUrl (web.ts:1042-1060), whose `back` is always a /settings pane, /apps, an /apps/<slug> pane or /approvals — no call site passes paths.audit. /audit has no POST route at all (web.ts:398-408 is the GET page plus the GET export), and its filter form is a GET to /audit carrying no done/failed field. Only a hand-typed /audit?done=… draws one. — skeptic: CONFIRMED, with the chain re-walked. shell (model.ts:1350-1365) copies `notice: ctx.notice` into every signed-in page's props, and context (web.ts:1116-1128) sets `notice: noticeOf(query)` at web.ts:1123 — so /audit is wired to render one; only nothing ever writes the key. noticeOf (web.ts:1063-1076) reads only NOTICE_KEYS.done/failed, written solely by noticeUrl (web.ts:1042-1060). I enumerated EVERY noticeUrl call site (`grep -n "noticeUrl(" server/src/web.ts` → 351, 372, 379, 567, 849, 1008, 1162, 1166) and every dispatch() mount (`grep -n "dispatch("` → 393 settingsTokens, 394 settingsClients, 434 approvals, 547 apps, 580 app-pane function): the `back` values are paths.settingsPane("password"), the pane argument of credential() (web.ts:840-856, always a /settings pane), paths.settingsTokens, paths.settingsClients, paths.approvals, paths.apps, paths.appPane(slug,…) and paths.appDetail-derived panes. paths.audit appears in NO back position. /audit has no POST route and no dispatch mount, so no redirect-back can land there; its filter form is a GET carrying since/until/session/offset/tool/principal/app/event/limit and no done/failed field. The service worker's notificationclick opens /approvals/<id> (web.ts:145-146). ADDED FACT: all five audit fixtures are `...shell("audit")` / `shell("audit", 0)` (fixtures.ts:1883, 1909, 1935, 1969, 2006) and the fixture shell defaults `notice = null` (fixtures.ts:73-77) — so the alert block on /audit is absent from the states preview as well as from production, which is a states-coverage gap on top of the orphan. | none |
| O26 | /audit | board panel AuditDetailStates "EXPANDED — LOADING" ("Loading call detail…") | design/AuditDetailStates.dc.html panel 2 | The page is one server-rendered response; AuditProps has no loading member, audit.tsx ships no client script and makes no fetch. grep "Loading call detail" over server/src → 0 hits. — skeptic: CONFIRMED. `grep -rn "Loading call detail" server/src` → 0 hits; `grep -rn "Loading " server/src` → 0 hits anywhere in the source. AuditProps (model.ts:1201-1220) declares no loading/pending member, so no loader could select it even if the copy existed. `grep -rn "<script" server/src/pages/*.tsx` → audit.tsx has none, and none of the six templates that do ship one fetches anything (they register the service worker, re-open a <dialog>, copy a token, run the OTP/passkey ceremonies, and register push). The service worker itself is push + notificationclick with no fetch handler at all (web.ts:668, and the no-SPA pin at web.ts:145-146), so the page is one server-rendered response with no second request to be pending on. Board-only state (design/AuditDetailStates.dc.html panel 2, lines 63-82). | none |
| O27 | /audit | board panel AuditDetailStates "BODIES NOT RECORDED" ("Call bodies aren't recorded for this app (body logging is off).") | design/AuditDetailStates.dc.html panel 3 | grep "aren't recorded" / "body logging" over server/src → 0 hits. EventDetail (audit.tsx:288-313) renders nothing at all for absent bodies, so a bodies-off ok tools/call row (gateway passes `detail` only on failure classes, gateway.ts:1164-1176) draws the eyebrow plus a bare "Client: …" line — the roadmap's G24. Reported here only to add that the same code path also yields a completely empty panel for a row with neither client nor detail, because audit.tsx:353 gates the panel on isOpen, not on hasDetail. — skeptic: CONFIRMED. `grep -rn "Loading call detail" server/src` → 0 hits; `grep -rn "Loading " server/src` → 0 hits anywhere in the source. AuditProps (model.ts:1201-1220) declares no loading/pending member, so no loader could select it even if the copy existed. `grep -rn "<script" server/src/pages/*.tsx` → audit.tsx has none, and none of the six templates that do ship one fetches anything (they register the service worker, re-open a <dialog>, copy a token, run the OTP/passkey ceremonies, and register push). The service worker itself is push + notificationclick with no fetch handler at all (web.ts:668, and the no-SPA pin at web.ts:145-146), so the page is one server-rendered response with no second request to be pending on. Board-only state (design/AuditDetailStates.dc.html panel 2, lines 63-82). | none |
| O28 | /approvals | decideFailed — a WARNING-tone titled notice ("That request is no longer pending" / "It expired at 15:12:31. The agent's next attempt opens a fresh one.") (fixture `decideFailed`) | ApprovalsProps.notice.tone === "warning"; approvals.tsx:78-87 ALERT_CLASS + NoticeIcon | grep `tone: "` over server/src returns exactly three Notice producers: web.ts:1068 {tone:"success", message:"<Op> done."}, web.ts:1071-1075 {tone:"danger", title:"<Op> failed", message: reason}, and web.ts:1087-1096 passwordDone (success, /settings only) — the rest are approvals.tsx's own badge tones. A decide that loses the race throws HubError(CODES.notPermitted, "no decidable approval request") (approvals.ts:376-381) through ops.approval_decide (admin.ts:1102-1117) and dispatch (web.ts:1001-1009), landing on /approvals?failed=approval_decide&reason=no+decidable+approval+request → the page really draws tone DANGER, title "Approval decide failed", message "no decidable approval request". Both the tone and every word of the fixture's copy are unproducible; `tone: "info"` is likewise unproducible on every shell page. — skeptic: CONFIRMED. My own search agrees. `PageContext.notice` has exactly ONE assignment in the whole worker: web.ts:1123 `notice: noticeOf(query)` inside `context()` (grep -n 'notice' server/src/web.ts -> 1063 def, 1115 context, 1123 the only assignment; grep -rn 'notice' server/src/pages/model.ts -> 130/1330 type decls, 1363 `notice: ctx.notice` in shell(), nothing else). `noticeOf` (web.ts:1063-1078) returns exactly three shapes: passwordDone (tone success, only for CHANGE_PASSWORD, only landed on /settings panes by web.ts:339-379), {tone:"success", message:`${humanize(done)} done.`}, and {tone:"danger", title:`${humanize(failed)} failed`, message: reason}. grep -rn 'tone:' server/src returns no fourth producer — web.ts:1068/1073/1089 are those three; every other hit is a page's own badge/alert mapping (approvals.tsx:69-73, format.ts:60, settings.tsx:118, apps.tsx:175). So `warning` (and `info`) are unproducible on /approvals and on every other shell page. I also confirmed the tracer's account of the real race: approvals.ts:376-381 throws HubError(notPermitted, "no decidable approval request") -> dispatch (web.ts:1001-1009) -> noticeUrl -> /approvals?failed=approval_decide&reason=… -> tone DANGER, title "Approval decide failed". Neither the tone nor a word of the fixture's copy is producible. Tests: no test asserts any /approvals page state (grep 'Older|No pending requests|No decisions yet|Showing last' over server/test -> 0 hits); the only /approvals page cases are web-pages.test.ts:878 (case 8, owner scoping of /approvals/<id>) and :989 (case 16, forms name real ops). Nothing bypasses the loader because nothing tests it. | none — no test asserts any /approvals page state (grep "Older"/"No pending requests"/"No decisions yet"/"Showing last" over server/test → 0 hits; the only /approvals page tests are 8 (owner scoping of /approvals/<id>) and 16 (forms name real ops)) |
| O29 | /approvals | history row badged "pending" (approvals.tsx:72) | outcome(status) with status === "pending" inside HistoryRow | model.ts:1884 builds history as `listed.approvals.filter(row => row.status !== "pending")`, so HistoryRow is never handed a pending row and that arm of `outcome` never runs; PendingCard hardcodes its own badge (approvals.tsx:153). Dead branch. — skeptic: CONFIRMED. `outcome()` has exactly one caller: grep -n 'outcome(' server/src/pages/approvals.tsx -> 68 (definition) and 179 (`const out = outcome(row.status)` inside HistoryRow). HistoryRow is rendered only from `props.history` (approvals.tsx:333-334, inside the `history.length === 0 ? … :` arm at :315). approvalsProps (model.ts:1878) builds that prop as `listed.approvals.filter((row) => row.status !== "pending")` and then `history: decided.slice(0, historyLimit)` (:1883) — a pending row can never enter it. The pending cards draw their own hardcoded `<span class="badge badge--warning">pending</span>` (approvals.tsx:153) and never call `outcome`. Dead branch confirmed; no fixture plants a pending row in `history` either (the nearest, decideFailed's first history row, is `status: "expired"`, fixtures.ts:1594). Tests: none — see claim 1's grep. | none |
| O30 | /approvals/<id> | "Decided —" (the `approval.decidedAt ? absolute(...) : "—"` arm of timeRows for a rejected/used row, approval-detail.tsx:110) | status rejected|used with decidedAt === null. No fixture. | Both writers of those two statuses stamp decided_at in the same UPDATE — approvals.ts:309-311 (claim → used) and approvals.ts:386-387 (decide → approved/rejected). The only status that leaves decided_at null is `expired` (flipExpired, approvals.ts:568-572), which takes the other arm of the switch. Defensive fallback with no producer. — skeptic: CONFIRMED, and I checked one writer the tracer did not mention. grep -rn 'UPDATE approval|INSERT INTO approval|decided_at' over server/src + server/migrations gives every writer of the column: approvals.ts:246-260 INSERT (status 'pending', decided_at null), :309-311 claim (`SET status = 'used', decided_at = ?` — same statement, so `used` always carries one), :386 decide (`SET status = ?, decided_at = ? WHERE … status = 'pending'` — so `approved`/`rejected` always carry one), :570 flipExpired (`SET status = 'expired'` only, and only on a `pending` row — the one path that leaves decided_at null, and it selects timeRows' `expired` arm, approval-detail.tsx:105-108). The extra writer is approvals.ts:342, settle's MRTR restore `SET status = 'approved' WHERE id = ? AND status = 'used'` — it does not clear decided_at, and the row it targets was already stamped by decide, so it produces no null either. migrations/0003_approval.sql:33 declares `decided_at INTEGER` nullable but no seed or migration backfills a rejected/used row. approvals.ts:442 maps null->null faithfully, so the page's fallback is a defensive arm with no producer. Tests: none. | none |
| O31 | /device | relativeTime's "N min(s) ago" and "N hour(s) ago" arms on the confirm card | device.tsx:77-80 — the seconds>=45 and minutes>=60 arms of relativeTime(request.requestedAt, now) | model.ts:2401 `requestedAt: ctx.now` is the sole assignment of that field on a real request (grep requestedAt over server/src → model.ts:2401 and device.tsx:74/157 only), and ctx.now is the same instant device.tsx compares against (web.ts:1122). Delta is always 0, so device.tsx:75-76 returns "Just now" every time. better-auth's /device response carries no created-at to use instead (routes.mjs:551-559 returns user_code, status, client_id, scope and nothing more), which model.ts:2396-2400 already documents for `ip`. Even the fixture (4s before NOW) renders "Just now", so the preview does not expose it either. — skeptic: REFUTATION FAILED — confirmed. `grep -rn requestedAt server/src server/dev server/test` returns exactly four hits: model.ts:503 (the DeviceRequest field), model.ts:2401 `requestedAt: ctx.now`, device.tsx:157 (the only relativeTime call site), fixtures.ts:153. deviceProps (model.ts:2359-2365) sets `now: ctx.now` and deviceStep (model.ts:2401) sets `requestedAt: ctx.now` from the SAME PageContext object, and `now` is built once per request at web.ts:1122 `now: new Date().toISOString()` inside `context()` — one string, compared against itself, so `deltaMs` is exactly 0 and device.tsx:75-76 returns "Just now" on every real render. I also checked the two escape hatches the tracer did not name: (a) `grep -rn relativeTime server/src server/dev server/test` finds no second caller — no other page has this helper, so no other props feed it; (b) the sibling field `expiresAt` (model.ts:2404) is likewise ctx.now-derived and device.tsx never renders it at all, so there is no third clock in the card. The only Device call site is web.ts:221 `render(Device(await deviceProps(ctx, c.req.raw)))` — no second construction anywhere. The 4-second fixture (fixtures.ts:153 requestedAt 14:46:56 vs NOW 14:47:00, fixtures.ts:65) also lands under the 45s threshold, so the states preview does not draw it either. TESTS: web-pages.test.ts imports no fixtures and never calls a page component (its header pins "pages are driven through exports.default.fetch, never by calling web.ts internals"); cases 28 and 29 POST /device's own rendered form but assert the redirect and the CLI redemption, never the rendered time. No test bypasses the loader. Adding one fact the tracer left out: the model.ts:2396-2400 KNOWN CEILING comment already spells the same root cause for `ip` — better-auth's deviceCode record carries no requesting-device columns — so requestedAt shares that documented ceiling rather than being an oversight; fixing it needs a created-at on better-auth's record, not a page change. | none |
| O32 | /oauth/consent | the DENIED confirmation panel (design/OauthConsentStates.dc.html:68-72 — "Request denied · Claude was told access was denied. You can close this tab.") | a board panel with no corresponding prop: ConsentProps (model.ts:1238-1259) has no decision/decided arm at all | consent.tsx renders exactly one card (:99-148) and no decided variant; no fixture exists (oauthConsent has 4 keys, none decided); and web.ts:604-645's deny leg calls /oauth2/consent with accept:false and then `c.redirect(redirectUri, 303)` straight to the CLIENT's redirect_uri — the hub renders nothing after a denial. Searched model.ts for a consent decided/denied type (grep "decided"/"decision" in the ConsentProps block → nothing; /device is the only page with a `decided` arm). The board panel cannot be produced. — skeptic: REFUTATION FAILED — confirmed, and the suite actively pins the opposite. Read the board (design/OauthConsentStates.dc.html, the last panel, labelled DENIED) and consent.tsx end to end: ConsentPage (consent.tsx:67-153) renders ONE `.auth-card` with no decision/decided branch anywhere — the only conditional is `clientSelfRegistered` (the warning alert) and `agents.length === 0` (NoAgents vs AgentPicker). ConsentProps (model.ts, consentProps at 2432-2461) returns nine fields and none of them is a decision. `grep -rn "denied|decided|decision" server/src/pages/model.ts` finds a `decided` arm only on DeviceStep (model.ts:518, produced at model.ts:2375-2377 from `?decided=`), never on consent; `grep -rn decided server/src/web.ts` finds only device.ts's two lines (web.ts:236, 238) — there is no `?decided=` leg on paths.oauthConsent. I re-read the POST handler (web.ts:606-645): both legs converge on `return c.redirect(redirectUri, 303)` to the CLIENT's redirect_uri; the only non-redirect exit is `new Response("Bad Request", { status: 400 })` when the provider returned no url — plain text, not this panel. And GET paths.oauthConsent (web.ts:594-602) either renders the single card or answers 400. Corroborating rather than bypassing: web-pages.test.ts:1507 "§19.5 · denying writes no binding and redirects to the client with access_denied" asserts precisely that a denial produces a redirect, i.e. the hub renders nothing. No fixture exists (oauthConsent has 4 keys, none decided). No test bypasses a loader here. | none |
| O33 | /login (design board only) | the recent-auth challenge card — design/ReauthGate.dc.html: "Confirm it's you / Managing two-factor, passkeys and sessions needs a recent sign-in", a password-only form, "Use a passkey", and the footnote "Sessions signed in with a CLI token can't manage credentials" | a whole board with no props arm: LoginStep (model.ts:459-467) has three kinds and none is a re-auth challenge | §4's recent-auth refusal is identity.ts:632 `if (opts?.recent && !(await isRecentAuth(...))) throw loginRedirect(req)`, and loginRedirect (identity.ts:654-660) emits `/login?next=…` and nothing else — so the browser gets the ORDINARY credentials card (login.tsx:60, with a username field, the "Use your username and password." subtitle and no explanation of why it was bounced). Same for the bearer-sourced-session refusal at identity.ts:629. Searched: grep -rn "ReauthGate|Confirm it's you|reauth" over server/src, server/dev and docs/specs → only design/README.md:65,83 and design/canvas.json:41 (gateway.ts's `reauthorize` is the tunnel door, unrelated). No fixture, no page, no props. — skeptic: REFUTATION FAILED — confirmed. I read identity.ts:604-660 in full. BOTH refusals the board illustrates throw the SAME response: the bearer-sourced/no-session arm (`if (!session || !user?.username) throw loginRedirect(req)`) and the recent-auth arm (identity.ts:630 `if (opts?.recent && !(await isRecentAuth(session.session.createdAt))) throw loginRedirect(req)`). loginRedirect (identity.ts:654-660) builds exactly `Location: /login?next=${encodeURIComponent(pathname + search)}` — no `error`, no `reason`, no `step`, no discriminator of any kind. loginStep (model.ts, the `loginStep(query)` reader) keys only off `error` + `step`/`method` and returns one of LoginStep's three kinds (model.ts:459-467), so a bounced browser lands on CredentialsCard (login.tsx:60-111) with the literal title "Sign in", subtitle "Use your username and password.", a USERNAME field the board does not have, and nothing explaining the bounce; `redirectTo` is consumed only as the hidden `callbackURL` (login.tsx:70/109) and changes no copy. My own greps: `grep -rn "reauth|Reauth|Confirm it's you|recent-auth|recentAuth|isRecentAuth" server/src server/dev docs` — inside server/src the only hits are gateway.ts's `reauthorize` (the tunnel door's per-tick principal re-check, unrelated), identity.ts:630/643, index.ts:118 and web.ts:10/247/470, all comments or the gate itself; nothing in server/dev; the board is named only in design/README.md:65,83 and design/canvas.json:41. No LoginStep member, no fixture, no props path. Worth flagging beyond the tracer's note: docs/superpowers/plans/2026-09-02-roadmap-after-d15.md:340 records "the recent-auth refusal is served (identity.ts:658)" as REFUTED-not-a-gap — that line is about the refusal being SERVED (the 302 happens), which is true and is not a claim that this card renders; it does not contradict the orphan finding. | none |
| O34 | shell | no nav item current (a shelled page naming no section) | n/a — NavSection (model.ts:101) has no "none" member | Not representable. Every Layout caller names one of the four (grep `active=` over server/src/pages/*.tsx → 5 hits, all literal or props.section). The signed-in URLs that draw NO shell at all are /apps/new (app-new.tsx:6, chromeless) and /approvals/<id> (approval-detail.tsx:9, chromeless) — reachable by design, and the consequence is that the badge and Sign out disappear on both. — skeptic: REFUTATION FAILED — confirmed, and it is unrepresentable at the type level, not merely unproduced. NavSection (model.ts:101) is `"apps" | "audit" | "approvals" | "settings"` with a comment that already states the rule ("Pages outside the shell — /login, /device, /apps/new, /approvals/<id> — are chromeless card layouts and carry no section at all, which is why this never has a 'none' member"), and LayoutProps.active (layout.tsx:22) is a required NavSection. My greps: `grep -rn "<Layout|Layout(" server/src server/dev` → five call sites and the definition, no sixth; `grep -rn "active=" server/src/pages/*.tsx` → apps.tsx:295 `active="apps"`, audit.tsx:381 `active="audit"`, approvals.tsx:266 `active="approvals"`, app-detail.tsx:964 `active={props.section}`, settings.tsx:1091 `active={props.section}`. I chased the two props.section cases to the loader rather than stopping at the template: `grep -n "shell(ctx" server/src/pages/model.ts` → 1398 "apps", 1610 "apps", 1880 "approvals", 1973 "audit", 2158 "settings" — five literals, no computed or optional value, and shell() (model.ts:1352-1364) returns `section` as a required field of ShellProps (model.ts:126). So no request can produce an unhighlighted nav. The reachable consequence the tracer names is right and is the substantive part: /apps/new and /approvals/<id> draw no shell at all (chromeless), so on those two the approvals badge and Sign out are simply absent. | none |
| O35 | shell | pendingApprovals absent — the `?` on LayoutProps.pendingApprovals | layout.tsx:26 `pendingApprovals?: number` | All five Layout call sites pass it from ShellProps, where it is a required `number` (model.ts:128): apps.tsx:295, approvals.tsx:266, audit.tsx:381, settings.tsx:1093, app-detail.tsx:966. Grep `<Layout` over server/src and server/dev finds no sixth caller. The optional marker buys nothing — undefined renders identically to 0. — skeptic: REFUTATION FAILED — confirmed, with one sharpening. `grep -rn pendingApprovals server/src server/dev` gives every producer and consumer: model.ts:128 declares it a REQUIRED `number` on ShellProps, model.ts:1362 `pendingApprovals: rows.length` is the sole assignment (inside shell(), whose five call sites are model.ts:1398/1610/1880/1973/2158), and all five Layout callers forward it (apps.tsx:295, audit.tsx:381, approvals.tsx:266, app-detail.tsx:966, settings.tsx:1093). `grep -rn "<Layout" server/src server/dev` finds no caller that omits it. So `undefined` never reaches layout.tsx. SHARPENING (matters for how this is written up): this is dead OPTIONALITY, not a distinct drawable state — layout.tsx:261 renders the badge under `pendingApprovals ? ... : null`, so undefined and 0 produce byte-identical HTML. There is no orphan PIXEL here, only an orphan type marker; the fix is deleting the `?` on layout.tsx:26, which is a one-character change with no rendering consequence. TESTS: nothing bypasses a loader — web-pages.test.ts imports no fixtures and constructs no Layout; the badge-adjacent cases drive real requests. | none |
| O36 | every page that draws a notice | Notice tone "warning" | ALERT_CLASS["warning"] → `alert alert--warning`; approvals.tsx:101 / apps.tsx:184 NoticeIcon warning arm | No producer. `ctx.notice` is set in exactly one place — web.ts:1122 `notice: noticeOf(query)` — and noticeOf (web.ts:1063-1078) returns only success or danger; passwordDone (web.ts:1086) returns success. `grep -rn "tone:" server/src` finds only web.ts:1068/1073/1089 as Notice constructors (approvals.tsx:69-73 is a badge Tone, an unrelated type). Nearest candidate, the approvals decision refusal, goes through dispatch(paths.approvals) → noticeUrl `?failed=approval_decide` → tone danger. Drawn only by fixtures.ts:1586 (approvals.decideFailed). | none |
| O37 | every page that draws a notice | Notice tone "info" (the bare `.alert`, styles.css:1418) | ALERT_CLASS["info"] → `alert`; approvals.tsx:110 / apps.tsx:200 NoticeIcon info arm | Same searches as "warning": nothing constructs a Notice with tone "info" anywhere in server/src, and no fixture draws it either (grep `tone:` over fixtures.ts → 11 hits, all success/danger except the one warning at 1587). The info arm of all five class tables and both info icons is dead in the app AND in the preview. | none |
| O38 | audit | the notice banner on /audit | audit.tsx:383-390 `{notice && …}` with its own ALERT_CLASS at audit.tsx:54 | Every noticeUrl call site (web.ts:351, 372, 379, 567, 849, 1008, 1162, 1166) targets paths.settingsPane(...), paths.settingsTokens, paths.settingsClients, paths.approvals, paths.apps, or paths.appPane(slug, pane) — never paths.audit. /audit is mounted only as a GET (web.ts:398) and its export (web.ts:405); nothing redirects there. Its filter form is method="get" and rewrites the whole query string, so a `done=`/`failed=` cannot survive a filter submission either. No audit fixture sets a notice. — skeptic: REFUTATION FAILED — orphan confirmed, but the claim needs one precision. A producer for the PROP does exist and does run for /audit: web.ts:398's GET calls context() (web.ts:1113), which sets `notice: noticeOf(query)` at :1123 for every page alike, and auditProps (model.ts:1972) spreads `shell(ctx,"audit")` (model.ts:1363) which copies it. So `/audit?done=app_archive` typed by hand WOULD draw the banner. What has no producer is the NAVIGATION: nothing in the app ever puts done=/failed= on an /audit URL. My searches: `grep -rn 'noticeUrl(' server/src/web.ts` → 8 call sites (351, 372, 379 → paths.settingsPane("password"); 567 → paths.appPane(slug,"token"); 849 → credential()'s `pane`, and `grep -n 'credential(' server/src/web.ts` shows those panes are only settingsTwoFactor/settingsPasskeys/settingsSessions; 1008 → dispatch()'s back, whose 5 registrations are settingsTokens (393), settingsClients (394), approvals (434), apps (547), and the app-pane function (580) that returns paths.apps or paths.appPane; 1162, 1166 → paths.apps). None is paths.audit. `grep -rn 'paths.audit' server/src` → layout.tsx:41 (plain nav href, no query), model.ts:221/244/263 (the builders). The only /audit link builder is auditWith (model.ts:244), called once, at audit.tsx:244, from `{...baseQuery(filters), ...overrides}` — filters only, so an unrecognized param is dropped rather than carried. The filter form (audit.tsx:408) is method="get" with hidden inputs since/until/session/offset only, so a GET submit erases any flash. All five audit fixtures use `shell("audit")` / `shell("audit", 0)` (fixtures.ts:1883, 1909, 1935, 1969, 2006) and fixtures.ts:73-77 defaults notice to null. No test hits /audit with done=/failed=. | none |
| O39 | settings | the revoke-session dialog's `confirm.client` fallback title | settings.tsx:1001 `matched ? sessionLabel(matched) : confirm.client` | The false arm cannot fire: model.ts:2249 only emits `{kind:"revoke-session"}` when the id is found in the very `sessions` array that settings.tsx:1001 then searches, and both come from the same SettingsProps. Dead fallback, not a rendered state. — skeptic: REFUTATION FAILED — confirmed dead branch, and it is dead even in the preview. settingsConfirm (model.ts:2249-2252) returns `{kind:"revoke-session", id, client: row.client}` ONLY when `sessions.find(s => s.id === id && !s.current)` matched, and the array it searches is `rows` — the very same array handed to the page as props.sessions (model.ts:2168 `sessions: rows`, :2171 `confirm: settingsConfirm(ctx.query, pane, rows, …)`). settings.tsx:1000 then re-searches props.sessions for the same id, so `matched` is defined by construction; nothing between the two can drop a row (both come off one SettingsProps object, no re-fetch). I also checked the preview: fixtures.ts:561 sets `confirm: {kind:"revoke-session", id:"ses_4a77c0", client:"pmcp CLI"}` and settingsBase's session list contains ses_4a77c0 (fixtures.ts:228), so the fixture takes the matched arm too — the fallback renders nowhere at all. No test bypasses it (web-pages.test.ts:2881 and :3007 drive the dialog through real `?confirm=revoke-session&id=…` requests on real sessions). ADJACENT ORPHAN worth adding, not in the claim: sessionLabel (settings.tsx:626-628) has a `session.source === "cli"` arm appending " · device flow", but model.ts's sessionRow (:2287) hardcodes `source: "web"` for every row better-auth lists — grep `source:` over model.ts finds no other writer — so every real session renders the bare client string and the cli label is unreachable. | none |
| O40 | service worker | push fallbacks — "Approval needed" with no app, "A request is waiting for your decision", tag "approval", data.url "/approvals", and the malformed-JSON catch | web.ts:1294 catch, 1296 title fallback, 1297 body fallback, 1301 tag fallback, 1302 url fallback (and 1311 in notificationclick) | The only push producer in the worker is approvals.ts:531 (grep `config.push` / `send(` over server/src → approvals.ts:539 is the sole call), and it always sends all four fields as valid JSON. No path sends a partial payload or a non-JSON body. Defensive, but no producer. — skeptic: REFUTATION FAILED — orphan confirmed; I attacked each field and none can arrive falsy. Sole payload producer: approvals.ts:531 in notifyOwner, whose only caller is approvals.ts:288 (`grep -n 'notifyOwner' server/src/approvals.ts` → 137 comment, 198 deps comment, 288 call, 519 definition) and whose only transport is `this.config.push` (approvals.ts:526), absent = nothing sent. It always JSON.stringify's all four keys. Field by field: `approvalId` = crypto.randomUUID() (approvals.ts:230), always truthy → the tag "approval" fallback (web.ts:1301) is dead. `app` = app.slug — registry-validated non-empty, or the virtual "pmcp" slug (gateway.ts:942) → the "Approval needed" title fallback (web.ts:1296) is dead. `url` = `${publicOrigin}/approvals/${id}` (approvals.ts:609-610), truthy even with an empty origin → the data.url "/approvals" fallback (web.ts:1302) and, consequently, the notificationclick `|| "/approvals"` at :1311 are both dead. `tool` was my best refutation candidate — an empty tool name would fire the body fallback (web.ts:1297) — and it fails twice over: splitAggregatedName (gateway.ts:550-554) returns null when `at <= 0 || at === name.length - 1`, so "news_" never yields tool ""; and even a well-formed but unknown tool is refused before the gate, because redactionMapFor null → notPermitted at gateway.ts:973-974 runs BEFORE passGate at :979, which is the only caller of approvals.check. The `catch` at web.ts:1294 and the `event.data ? … : {}` guard would need a non-JSON or payload-less push, and no code path sends one. Purely defensive. No test exercises the worker's body — web-pages.test.ts:890 only asserts /sw.js is served unauthenticated and leaks no username. | none |

## Claimed, then refuted by the skeptic (6)

- **/apps — success notice carrying a title — the bold `.alert-title` line over a green alert (apps.tsx:224 with tone "success").** Producer: web.ts:1066-1067 (noticeOf's `done === CHANGE_PASSWORD` arm) → web.ts:1086-1096 passwordDone, reached for /apps via web.ts:1122 `notice: noticeOf(query)` in context(), on `GET /apps?done=change_password` (optionally `&signedOut=<n>` for the second sentence). — REFUTED. The tracer traced the redirect and not the branch. noticeOf is route-blind: web.ts:1064-1069 keys on the `done` query value alone — `return done === CHANGE_PASSWORD ? passwordDone(query) : {tone:"success", …}` — with no route, pane or referrer guard, and it runs inside `context()` (web.ts:1115-1125, `notice: noticeOf(query)` at :1122), which every page route builds, /apps included (web.ts:449-452 `const ctx = await context(c.req.raw, …); return render(AppsPage(await appsProps(ctx)))`). So `GET /apps?done=change_password` renders passwordDone (web.ts:1086-1096): tone "success", title "Password updated.", message "App and agent tokens keep working: they do not derive from the password." — the exact state claimed orphan, on a real request, from a real code path. The contrast that proves the omission is deliberate elsewhere: the Password pane's sibling flash IS guarded — passwordErrorOf (model.ts:2176) returns null unless `pane === "password"` — while noticeOf carries no such check. Caveat, and it is the tracer's real finding: no link, form or redirect in the hub ever puts `done=change_password` on an /apps URL (the three CHANGE_PASSWORD redirects, web.ts:351/372/379, all target `paths.settingsPane("password")`), so this is query-selected reachability — the same mechanism that makes `?confirm=…` dialogs (model.ts:2231) and `?kind=agent|app` (model.ts:2203) first-class page states — reached by a hand-edited, shared or bookmarked URL rather than by navigation. Not an orphan by the stated definition; arguably a small bug, since a notice about a password change can be made to appear on a page that has nothing to do with one.
- **/audit — expanded row detail — the .row-detail <tr>, the EVENT DETAIL panel, the recorded bodies and the session link.** Producer: model.ts:1980 `expandedId: positive(ctx.query.get("expand")) ?? null`, reached by the real route web.ts:398 `app.get(paths.audit, …)` — request: GET /audit?expand=<id of any row in the current window> (add any filter and it still resolves; the id comes from audit_query's own rows). — REFUTED as an orphan; it is a NAVIGATION gap, which is a different finding. The loader line the tracer cites IS the producer — it reads a real query param on a real request and returns 200 with the panel. The proof is in the tests the tracer called bypasses: `page()` (web-pages.test.ts:386-390) does `get(path)` through the worker and asserts status 200, so tests 14 (line 965) and 15 (line 975) drive the FULL stack — route → auditProps → audit_query against seeded D1 → render. Nothing is planted, no fixture is used, no props object is passed directly; `auditPath` (web-pages.test.ts:5838-5842) is only a URLSearchParams helper, so "NOT paths.auditWith" is true but immaterial — a browser hitting that URL gets the same page. What the tracer actually found, and it is real, is that NOTHING RENDERED SPELLS THE URL: AuditLinkQuery (model.ts:154-157) omits `expand`, so paths.auditWith (model.ts:244) cannot emit it and baseQuery/auditLink (audit.tsx:235-245) strip it from every href (range presets 260, session link 307, export 402/476, clear 462, pager 622/631, Load more 648); the summary row is a bare `<tr>` (audit.tsx:326) with no <a>, no onclick; `grep -rln "<script" server/src/pages/*.tsx` → audit.tsx is NOT among them, and layout.tsx's only script registers the service worker (which has no fetch handler and opens only /approvals/<id> on notificationclick, web.ts:1305-1320). Repo-wide `grep -rn "expand" server/src server/dev server/test design/*.dc.html cli clients docs/specs` finds no link builder. Both NEW facts confirmed: model.ts:1207-1211's "the row whose <details> is rendered open" is stale (EventRow emits two sibling <tr>s, audit.tsx:326-360; the only <details> on the surface is app-detail's Tools row), and audit.tsx:353 gates the detail row on `isOpen` alone while `expandable` (322) only drives the Chevron — so ?expand= on a row with no client/detail/args/result draws a bare "Event detail" eyebrow and nothing else. Correct framing: reachable-but-unlinked (roadmap G1), plus a latent empty-panel bug at audit.tsx:353.
- **/audit — session-filtered view — "Filtered to session <id> · Clear session filter" + narrowed rows.** Producer: model.ts:2004 `...text(ctx.query, "session")` inside auditFilters, on GET /audit?session=<id> via web.ts:398; the same filters feed web.ts:405 `app.get(paths.auditExport({}))`, so /audit/export.jsonl?session=<id> is a second real request that produces the identical filter object through auditQueryOf (model.ts:2016). — REFUTED, and the tracer's own text concedes the producer ("model.ts:2005 does produce it from ?session=") before voting orphan — that is a discoverability argument, not an absence-of-producer one. Beyond the producer, TWO rendered controls already operate on this state: the filter form (audit.tsx:408 `method="get" action={paths.audit}`) re-submits the hidden session field (audit.tsx:411) on every Apply, so the filter survives every other filter change through a real control, and "Clear session filter" (audit.tsx:472) is a rendered link out of it. Test 13 (web-pages.test.ts:947-956) exercises `session` end to end through the export route without touching the expand panel at all, which is a live producer independent of the alleged orphan. My own searches agree with the tracer on the ENTRY point only: `grep -rn "paths.audit" server/src/pages/*.tsx` → audit.tsx (223/244/402/408/462/476/598) and layout.tsx:41's bare nav href, nothing else; `grep -rn "session" server/src/pages/audit.tsx` → the sole `?session=` emitter is line 307 inside EventDetail. So the state is reachable and its producer is live; what has no in-app entry point is the FIRST hop into it — same G1 gap as the row above, and it should be reported as one gap ("no rendered link opens a row, so nothing rendered offers a session id to filter by"), not two orphans.
- **/approvals/<id> — formatArgs' top-level `null` / array arms (approval-detail.tsx:63-67 -> "null" / "[ … ]").** Producer: server/src/gateway.ts:1083-1086 (argumentsOf admits arrays) -> server/src/approvals.ts:204 applyRedaction -> :238 args_json -> :439 JSON.parse -> server/src/pages/model.ts:1904 approvalDetailProps -> approval-detail.tsx:64-66 — REFUTED for the ARRAY arm — the tracer trusted a TypeScript annotation that does not hold at runtime. gateway.ts:1083-1086 `argumentsOf` is the whole args binding: `const args = msg.params?.arguments; return typeof args === "object" && args !== null ? (args as Record<string, unknown>) : undefined;` — `typeof [] === "object"` and an array is not null, so a JSON-RPC `tools/call` whose `params.arguments` is an ARRAY passes the guard and is cast, not rejected. Nothing validates it structurally: gateway.ts:304-311's tools/call case reads only `params.name`; the only invalidParams() calls in gateway.ts are 1907/1920 on resources/read URIs; JsonRpcRequest (gateway.ts:85-90) is a bare type, no zod/valibot. From there: passGate (gateway.ts:1035-1052) -> approvals.check(…, args, …) -> approvals.ts:204 `applyRedaction(args ?? {}, redactPaths)` -> registry.ts:774-782 `structuredClone(args)` returns an ARRAY (maskPath at :790-801 even has an explicit Array.isArray branch, so redaction survives it) -> canonicalJson (approvals.ts:651-676) handles arrays -> approvals.ts:238 `args_json: JSON.stringify(masked)` stores `"[…]"` -> the row is INSERTed (:246-262) and -32003 approvalRequired is thrown BEFORE prepareForward, so the row exists even though the backend never sees the call -> approvals.ts:439 `args: JSON.parse(row.args_json)` hands the array back -> approvalDetailProps (model.ts:1897-1906) passes it through -> formatArgs' `Array.isArray(value)` arm renders `[ … ]`. Request that gets there: any agent-principal `tools/call` on an approval-gated tool (filter.check(tool) === "approval", gateway.ts:975-978) with `"arguments": [ … ]`, then the owner opens /approvals/<id>. The `null` arm IS dead: `args ?? {}` turns undefined into `{}` and argumentsOf already collapsed `null`/scalars to undefined, so args_json is never `"null"`. Tests: none touch either arm.
- **/login — the TOTP challenge as the DESTINATION OF A SIGN-IN (?step=totp with no error, web.ts:818's twoFactorRedirect arm).** Producer: server/src/web.ts:813-818 (reads better-auth's twoFactorRedirect, emits ?step=totp), reachable because server/src/identity.ts:855-877 + server/src/index.ts:389 mount better-auth's whole surface at /api/auth/* with no cookie-side filter, so /two-factor/get-totp-uri + /two-factor/verify-totp set user.twoFactorEnabled (better-auth totp/index.mjs:206-208) — REFUTED. The tracer's better-auth reading is right and its hub reading is wrong: it searched only the hub's DRAWN forms and missed that better-auth's entire route table is publicly mounted. index.ts:389 `app.route("/api/auth", authRoutes() as Hono)`, and identity.ts:855-877 authRoutes() is a CATCH-ALL — `app.all("/*", … auth().handler(c.req.raw))`, whose own comment says "which endpoints exist under here is better-auth's plugin list to decide, not a route table of ours". The only gate is `admitsBearer` (identity.ts:939-946), and identity.ts:861-863 fires it ONLY when `c.req.raw.headers.get("Authorization") !== null` — a browser/curl request carrying just the owner's session cookie reaches every /two-factor/* endpoint unguarded (that is deliberate: BEARER_ADMITTED's doc at :890-925 says the guard exists to stop a stolen CLI BEARER, and that cookie-only callers are the intended users of these routes). Reachable chain, every leg a live route of this worker: (1) the hub's OWN /settings TOTP pane form, web.ts:281-286 credential(paths.settingsTwoFactor, "/two-factor/enable", …), creates the twoFactor row with an encrypted secret (two-factor/index.mjs:124-160) — the hub discards the totpURI body, which is the known enrolment orphan, but the SECRET is now in D1; (2) the owner POSTs /api/auth/two-factor/get-totp-uri with {password} and their cookie — totp/index.mjs:90-131, `use: [sessionMiddleware]`, returns `{ totpURI }` containing that secret; (3) the owner POSTs /api/auth/two-factor/verify-totp {code} with the same cookie — totp/index.mjs:150+, `isSignIn = !session.session` is false, `twoFactor.verified !== true` and `!user.twoFactorEnabled`, so :206-208 `updateUser(user.id, { twoFactorEnabled: true })` and verified:true; (4) next sign-in through /login passes two-factor/index.mjs:250's `if (!data?.user.twoFactorEnabled) return;`, the after-hook returns twoFactorRedirect, and web.ts:813-818 reads it and redirects to `/login?step=totp` (loginStep, model.ts:2347-2352, then draws the challenge card). This is the brief's "rare-but-real condition", not an orphan. The suite's own note at web-pages.test.ts:568-581 ("No hub route can") is true only of hub-DRAWN pages — enrollTwoFactor plants the column because the test cannot compute a TOTP code, not because no route flips it. NOTE this does not rehabilitate the two known orphans: the enrolment card and the backup-codes reveal stay orphaned, because credential() (web.ts:836-855) still throws the response body away — the owner has to leave the hub's UI to finish enrolment.
- **/device — the EXPIRED-specific refusal copy (fixture device.expiredCode's message; AuthStates.dc.html panel "DEVICE — EXPIRED CODE").** Producer: server/src/pages/model.ts:2384 (the verify-failed literal on GET /device?user_code=…), and server/src/web.ts:236 -> model.ts:2380 (the decide-failed ?error= redirect) — both select the same single branch at server/src/pages/device.tsx:107-108 — RECLASSIFIED as a FIXTURE-NOTE, not an orphan. The tracer is right that no code emits that sentence, but it treated a copy variant as a distinct renderable state. device.tsx has exactly ONE error branch — grep -n 'error' server/src/pages/device.tsx gives :105 `aria-invalid`, :107 `{step.error ? (`, :108 `<div class="field-error">{step.error}</div>` — and no branch anywhere on the error's TEXT. So the board's "DEVICE — EXPIRED CODE" panel and the `expiredCode` fixture render the exact same code path as the "invalid code" one, with a different string. The state itself (kind "enter-code", non-empty userCode, non-null error) has a producer: model.ts:2384 returns `{ kind: "enter-code", userCode, error: "That code is not valid. Check it and try again." }` on a real GET /device?user_code=<dead or unknown code>, and the tracer's diagnosis of WHY the wording cannot differentiate is correct — identity.ts callAuth (:1037-1049) returns null on any non-ok status, discarding better-auth's EXPIRED_USER_CODE vs INVALID_USER_CODE distinction. A second producer of the same branch the tracer missed: web.ts:229-236, the deviceDecide POST, redirects to `/device?error=That%20code%20could%20not%20be%20decided.` when callAuth fails, which model.ts:2380 reads back as `error: ctx.query.get("error")` — also not the fixture's sentence, and with userCode "". Verdict: the fixture and the board panel carry fabricated copy in a reachable branch, which is a fixture-quality note by the brief's own definition. Tests: none assert either device error string.

## Fixture notes (55)

- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] COUNT: server/dev/fixtures.ts declares 31 settings fixtures (fixtures.ts:359-628), not the 34 the task brief states. Enumerated keys: default, passwordWrongCurrent, passwordTooShort, passwordMismatch, passwordRefused, passwordUpdated, passwordUpdatedSignedOut, twoFactor, passkeys, sessions, tokens, clients, bare, passkeysEmpty, passkeysNeverUsed, tokensEmpty, clientsEmpty, tokensAgents, tokensApps, tokensFilteredEmpty, clientsActiveOnly, totpEnrolling, totpEnrollError, backupCodesRevealed, confirmDisableTwoFactor, confirmRemovePasskey, confirmRevokeSession, confirmRevokeOtherSessions, confirmRevokeConnection, error, longNames.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] THE BIGGEST ONE — settingsBase.twoFactor (fixtures.ts:343-347) is `{enabled:true, backupCodesRemaining:8, generatedAt:"2026-03-02T11:20:00.000Z"}`. The loader can only ever emit `{enabled:true, backupCodesRemaining:0, generatedAt:ctx.now}` (model.ts:2162). Every fixture that does not override it — 27 of the 31 — draws a backup-codes sentence with a count and a generation date no render can produce. The three overrides are no better: `error` uses 7 and `longNames` uses 1 (the singular arm, itself an orphan branch); only `backupCodesRevealed` (generatedAt: NOW) matches the loader's date half.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] settingsBase.sessions (fixtures.ts:215-243) carries `ses_4a77c0` with `source: "cli"`. model.ts:2290 hard-codes `source: "web"` on every row, so the "pmcp CLI · device flow" label the sessions/default/longNames/confirmRevokeSession fixtures draw — and the revoke dialog title in confirmRevokeSession — is a label no request produces. This fixture is the only reason the `source` union looks inhabited on both sides.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] The passkey fixtures (fixtures.ts:200-213, plus longNames' Windows-Hello row) name their rows "MacBook Touch ID" / "YubiKey 5C". model.ts:2220 renders `pk.name || "Passkey"` and the registration ceremony sends no name (settings.tsx:229-249), so every real row reads the literal "Passkey". The board copy and the fixtures agree with each other and with nothing the server can emit.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] backupCodesRevealed (fixtures.ts:527-538) carries a notice `{tone:"success", message:"Two-factor authentication is on."}`. noticeOf (web.ts:1063-1068) can only write `"<Humanized op> done."` — the real string after that POST would be "Two factor enable done." or "Backup codes generate done.". Fabricated copy on an already-orphan state.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] The `error` fixture (fixtures.ts:581-594) carries `{title:"Could not disable two-factor", message:"That password was not accepted. Nothing was changed."}`. The producer (web.ts:849-853 → 1070-1076) writes `title: "Two factor disable failed"` and the message is better-auth's own line verbatim. Both halves are invented.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] COUNTER-EXAMPLE, worth keeping: the six Password fixtures are the accurate ones. passwordWrongCurrent's "Invalid password" and passwordTooShort's "Password too short" are @better-auth/core's BASE_ERROR_CODES messages verbatim; passwordMismatch's "The change was refused." is web.ts:1075's default for the hub's own reason-less refusal; passwordRefused's "Password too long" is a real unmapped code (update-user.mjs:170); passwordUpdated / passwordUpdatedSignedOut match passwordDone (web.ts:1086-1098) word for word. These are what every other settings fixture's copy should be checked against.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] Three fixtures exist ONLY to draw states no route reaches — totpEnrolling, totpEnrollError, backupCodesRevealed. Worth keeping as the design record of the intended flow, but the states-preview index should mark them (and the `longNames` singular-backup-code line) as unreachable rather than showing them beside the 27 that render on a real request.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] No settings fixture uses `tone: "info"` or `tone: "warning"` — consistent with the loader, which cannot produce either. The two arms of settings.tsx's NOTICE_CLASS are dead in both the fixture set and production; if the tone union is meant to stay four-wide, it needs a producer, not a fixture.
- [/settings and its five sub-panes (settings.tsx · settingsProps + settingsConfirm in model.ts · the /settings/* routes, credential() and dispatch() in web.ts)] GAP, not an inaccuracy: no fixture draws an empty `sessions` list (the loader reaches it when /list-sessions fails — model.ts:2152), no fixture draws "Unknown client" (model.ts:2289), and no fixture draws a passkey row named "Passkey", which is the ONLY name production emits. The last is the one to add: it is the real steady state of the pane and nothing in the preview shows it.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] COUNT DRIFT vs the brief: the file actually holds 7 /apps fixtures (not 8), 28 app-detail (not 27) and 9 app-new. Keys: apps = default, empty, allArchived, confirmDelete, connectFailed, archivedNotice, longNames; appDetailFixtures = default, offline, neverConnected, archived, proxied, needsReconnect, unreachable, dimmedProxy, prompts, promptsUndeclared, resources, templates, roles, rolesTunneled, rolesEmpty, overview, overviewProxied, overviewProxiedDefault, agents, agentsEmpty, token, tokenRevealed, tokenProxied, danger, dangerArchived, confirmRevokeToken, confirmArchive, confirmDelete.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] apps.connectFailed — the notice's TEXT is invented. noticeOf (web.ts:1071-1076) always renders title = `${humanize(failed)} failed` and message = the refusal's own reason, so the real render is "Connect failed" / e.g. "this app does not connect through OAuth", never "Could not connect GitHub" / "The provider did not complete the sign-in…". The fixture's stated CAUSE is also wrong: a callback that comes back from the provider without a credential does not redirect to /apps at all — upstream.ts:760-765 connectRefused answers a text/plain 400. The reachable producers of ?failed=connect are web.ts:1162 (no such app) and web.ts:1166 (beginConnect throwing during discovery/DCR), including via the oauth create at web.ts:514-516.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] apps.archivedNotice — same class: the real success notice is "App archive done." (web.ts:1068 humanize), not "home is archived. Its roles, grants, and tokens are kept." The SHAPE (success, no title) is right.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] apps fixture row `notion` sets upstreamAuthMode:"headers" together with upstream:"connected". The loader cannot produce that pair: admin.ts:555 attaches `connection` only when auth==="oauth", so model.ts:1440 `upstream: row.connection ?? null` is always null for a headers proxy. Harmless in the render (both statusBadge and connectAction gate on upstreamAuthMode==="oauth") but it teaches a shape that does not exist.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] appNew.slugReserved — the STATE (a slug field error) is reachable, but not from a reserved slug. `pmcp` throws admin.ts:362 and a route segment throws admin.ts:379; neither message contains the literal `"slug"`, so createErrors (web.ts:1247) routes both to errors.form and the page draws the whole-form banner, not a field error. Also, "This slug is reserved." is prose the hub never emits.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] appNew.slugInvalid / appNew.errors slug text — the real strings are the refusal messages verbatim: `"slug" is not a valid slug` (admin.ts:246) and `"slug" already exists in this namespace` (registry.ts:906). The fixtures' "Slugs are lowercase letters, digits, and dashes." / "You already have an app called notion." are hand-written and drift from what a browser would see. createErrors puts the RAW refusal into the field, so every field-error string on this page is machine prose.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] appNew.blank exposes a real defect the preview already draws: app-new.tsx:61 does paths.mcpScoped(username, form.slug || "…") and mcpScoped percent-encodes its segment (model.ts:386), so an empty slug renders the hint as "served at /ahrzb/mcp/%E2%80%A6". Verified: encodeURIComponent("…") === "%E2%80%A6".
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] AppNewStep.created carries `name` (model.ts:1014) and web.ts:529 computes it (`draft.name === "" ? draft.slug : draft.name`), but CreatedCard (app-new.tsx:200-219) renders only step.slug and step.token. Dead field on both sides; fixtures tokenReveal/createdProxy set it and nothing reads it.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] No /apps/<slug> fixture carries a notice at all — the appDetail helper spreads shell("apps") (notice null) and no fixture overrides it — although the pane dispatcher (web.ts:579-585 with APP_OP_PANE, web.ts:940) reliably lands notices on /apps/<slug>/token and /apps/<slug>/danger. This is the largest coverage hole in the family and matches the memory note that a fixture should accompany every page/pane/state.
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] Reachable app-detail states with no fixture, worth adding: (a) an oauth proxy at connection "not connected" — the only render of the **Connect** label and of the "not connected" status word; (b) a tunnel Token pane with tokens: [] ("No live key…"); (c) a headers proxy on the Overview pane, where UpstreamCard renders nothing (app-detail.tsx:113); (d) the Resources pane under UNREAD_CATALOG (blank tab counts) and a mixed resources-listed/templates-unread render; (e) a tunnel with logBodies false → the bare "Off"; (f) a tool with only redactedArgs or only redactedResults; (g) a tunnel whose DO is unreachable → the no-control Unread arm; (h) an ArgumentRow with type "" (catalog-view.ts:58 — a property declaring no `type`); (i) a prompt argument with required:false (every fixture prompt arg is required).
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] Design boards vs the shipped page (divergences, not orphan props): AppDetailStates.dc.html's "PROXIED · OAUTH" panel draws Disconnect alone, while appHeader (model.ts:1743-1757) gives an oauth app BOTH Reconnect and Disconnect on every pane; its "APP TOKEN REVEAL" panel draws a standalone card with "New app token / Issued for news just now." and a **Done** button, while the shipped TokenPane (app-detail.tsx:780-784) renders TokenReveal inline above the live-key table with no such heading and no Done; its "ARCHIVED" panel puts Unarchive in the header, where the page puts it in the Danger zone only (app-detail.tsx:864).
- [/apps · /apps/new · /apps/&lt;slug&gt; (apps.tsx, app-new.tsx, app-detail.tsx; appsProps/appNewProps/appDetailProps in model.ts; the /apps routes in web.ts)] Tests: no test in server/test/ imports server/dev/fixtures.ts (only server/dev/preview.ts does) and none calls appsProps / appDetailProps / appNewProps directly — every apps-family case in web-pages.test.ts drives the router over seeded D1 and real dialled tunnels (e.g. the tools-undeclared case at line 4480 dials with capabilities:["prompts"]). So no test in this family passes by bypassing the loader. The corollary is the real gap: /apps/new has NO test beyond line 5564 asserting the form renders — createErrors (web.ts:1246) is never exercised, which is how the errors.name and errors.endpoint orphans survived.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] Scope note first: server/dev/fixtures.ts is preview-only — grep over server/test for "dev/fixtures" returns 0 hits, so no fixture can make a test pass. For this family the only loader-bypassing in the suite is URL synthesis: web-pages.test.ts:5838 `auditPath()` builds an arbitrary query string (including `expand`) rather than going through paths.auditWith, which is what keeps tests 14 and 15 green over an unreachable panel. Approval rows are opened honestly through Approvals.check (web-pages.test.ts:260-278), never planted.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] audit `default` and `middlePage` declare `range: "7d"` over since = 2026-08-18T00:00:00Z / until = NOW (2026-08-24T14:47:00Z) — a 6 d 14 h 47 m span. rangeOf (model.ts:2035-2041) calls that "custom", so no real request highlights the 7d segment over that window; the real 7d preset sets since = 2026-08-17T14:47:00Z.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] Every audit histogram in the file is unproducible. auditHistogram (model.ts:2072-2090) always emits exactly AUDIT_BUCKETS = 24 buckets with bucketMs = max(60_000, ceil(span/24)). The fixtures use 27 buckets at a round 6 h (`default`, `middlePage`, `bodyStubs`), 4 buckets at 6 h (`filteredBySession`) and 0 (`empty`). Consequences for the previews: the chart caption "{bucketHours}-hour buckets" and the day axis are fabricated; bodyStubs' and filteredBySession's declared 24 h windows would really draw 24 one-hour buckets captioned "1-hour buckets"; and default's real caption would be "6.615972222222222-hour buckets".
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] audit `empty` is the only fixture that draws "No events in this window." — an unreachable branch (see the orphan row). A real empty 1 h window draws 24 zero-height bars, the day axis, and the caption "0.041666666666666664-hour buckets", none of which any fixture shows.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] audit `default` row 41284 pairs `event: "approval.requested"` with `outcome: "-32003"`, a `client` block and `detail: { approval, status, expires_in }`. approvals.ts:279-286 records that event with `outcome: "ok"`, no client and `detail: { approvalId }`; the -32003 belongs to the sibling tools/call row the gateway writes (gateway.ts:999 + 1164-1176). Row 41280's `detail: { approval: "apr_7c1a" }` is `{ approvalId: <uuid> }` in reality. So the amber "approval" badge on an approval.requested row, and the detail line under it, are both shapes the recorder never produces.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] Every approval id in the fixtures is a short handle (`apr_8f2k`, `apr_7c1a`, `apr_6h4p`). approvals.ts:230 mints `crypto.randomUUID()`, so real /approvals/<id> URLs, history-row links and audit detail lines carry 36-character UUIDs — the id chips and column widths in the previews are sized for a value the hub never issues.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] All five approvals fixtures set `historyLimit: 25`. HISTORY_LIMIT is 20 (model.ts:1858) and "Older →" doubles it (40, 80, …), so 25 sits on no navigation path — a first visit really reads "Showing last 20 decisions".
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] `default`, `decideFailed` and `bulkyArgs` set `hasMoreHistory: true` beside 6–7 history rows. model.ts:1886-1888 sets hasMoreHistory only when decided.length > historyLimit, at which point history is exactly historyLimit rows — so "Showing last 25 decisions · Older →" above a 6-row table is a combination the loader cannot emit. `noPending` (6 rows, hasMoreHistory false) is consistent apart from the 25.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] approvals `decideFailed`'s history head — `{ ...pendingCreatePage, status: "expired", decidedAt: null }` — is faithful (flipExpired never stamps decided_at). Only the notice above it is fabricated.
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] auditOptions' vocabulary is fine in principle (filterOptions scans the unfiltered read, so options may name principals absent from the page): "bootstrap" (identity.ts:1233) and "app:<slug>" (tunnel.ts:1590) are both real spellings alongside user:/agent:. The one real principal missing from the list is index.ts:270's HUB_PRINCIPAL (cron rows).
- [/audit, /approvals, /approvals/&lt;id&gt; (audit.tsx, approvals.tsx, approval-detail.tsx; auditProps/approvalsProps/approvalDetailProps in server/src/pages/model.ts; routes at server/src/web.ts:396-446)] `bodyStubs` exists to show §15's stub placeholders, but stubLabel is only called from EventDetail — the preview shows them only because the fixture hand-sets `expandedId: 41290`. Same for `default`'s expandedId 41284. Both are the fixture standing in for a link the product does not have.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] HEADLINE (structural, not a fixture note): the /login two-factor challenge cannot be entered from a sign-in. web.ts:818's `outcome.twoFactorRedirect` arm is the only producer a real sign-in reaches, and it needs user.twoFactorEnabled — a column nothing in server/src writes (grep twoFactorEnabled → model.ts:2141/2161, both reads). The suite's own helper says so: web-pages.test.ts:568-581 enrollTwoFactor, "No hub route can". Root cause is the SAME already-known orphan as the TOTP enrolment card: web.ts:836-856 credential() discards better-auth's response body, so /settings/two-factor/enable's secret never reaches the owner, so no code can be derived, so /two-factor/verify-totp (two-factor/totp/index.mjs:208) never flips the column. Fixing the enrolment orphan un-orphans this flow too — one cause, four fixtures (login.totp, totpError, backupCode, backupCodeError) and the whole TwoFactor/MobileTwoFactor board family downstream of it.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] SECURITY: open redirect on /login's passkey arm. model.ts:2321 takes `query.get("next")` raw; web.ts:875 landingOf clamps it to a hub-relative path for the FORM post, but the passkey ceremony redirects client-side — login.tsx:56 landingUrl(redirectTo) → :109 passkeySignInScript(...) → :291 `location.assign(LANDING)` with no check. `/login?next=https://evil.example` (or a javascript: URL) is honoured on a successful assertion. The password arm is defended; this one is not.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] REFUSAL-CODE MAPPING (the brief's explicit ask): this family maps ZERO better-auth error codes. web.ts:911-921 refusalOf — the only reader of better-auth's `code`/`message` — is called only from credential() (web.ts:857, the /settings family), never from signInTranslation (web.ts:788) or deviceStep. /login collapses everything into two literals at web.ts:978-980. WRONG_CREDENTIALS swallows, on /sign-in/username: INVALID_USERNAME_OR_PASSWORD (401), USERNAME_TOO_SHORT / USERNAME_TOO_LONG / INVALID_USERNAME (422), EMAIL_NOT_VERIFIED (403), FAILED_TO_CREATE_SESSION (500), plus zod 400s and any WAF 429 (better-auth/dist/plugins/username/error-codes.mjs and index.mjs:155-209). Collapsing the first is §15's deliberate anti-enumeration rule; collapsing the 422s and the 500 is not — a validator rejection and a broken session store both read as "wrong password". WRONG_CODE swallows, on both two-factor legs: INVALID_TWO_FACTOR_COOKIE, TOTP_NOT_ENABLED, INVALID_CODE, TOO_MANY_ATTEMPTS_REQUEST_NEW_CODE, ACCOUNT_TEMPORARILY_LOCKED, BACKUP_CODES_NOT_ENABLED, INVALID_BACKUP_CODE, OTP_HAS_EXPIRED (two-factor/error-code.mjs). ACCOUNT_TEMPORARILY_LOCKED reading as "That code did not work. Try again." is the one that costs an owner real time.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] REFUSAL-CODE MAPPING, /device: model.ts:2388 collapses INVALID_USER_CODE and EXPIRED_USER_CODE into one sentence (that IS the orphan row above); web.ts:236 collapses INVALID_USER_CODE, EXPIRED_USER_CODE, DEVICE_CODE_ALREADY_PROCESSED, DEVICE_CODE_NOT_CLAIMED, AUTHENTICATION_REQUIRED and access_denied (device-authorization/error-codes.mjs:4-16) into "That code could not be decided." /oauth/consent maps nothing at all: consentProps null and a provider refusal both become a bare 400 "Bad Request" with no page and no message (web.ts:600, web.ts:640).
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] FIXTURE COUNT: login has SIX fixtures (default, credentialsError, totp, totpError, backupCode, backupCodeError), not the 7 the brief states. device is 5 and oauth-consent is 4 as stated.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] FIXTURE VALUES, device.default (fixtures.ts:143-157): all three request fields are fabricated. `ip: "203.0.113.42"` can never render — model.ts:2399 hardcodes the literal "unknown" (its own comment says why: better-auth's deviceCode record has no address column). `client: "pmcp CLI on Windows"` can never render — model.ts:2400 is `verified.client_id ?? "unknown"`, and the only real client sends the literal "pmcp-cli" (cli/src/main.ts:415 DEVICE_CLIENT_ID). `requestedAt: "2026-08-24T14:46:56.000Z"` (4s before NOW) is not wrong so much as inert — model.ts:2401 always sets it to ctx.now, and 4s renders "Just now" anyway, which is why the preview never shows relativeTime's other two arms.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] DEAD PROP: DeviceRequest.expiresAt is computed at model.ts:2404 (and typed at model.ts:504) and rendered NOWHERE — device.tsx's ConfirmCard shows userCode, ip, client and requestedAt only. The card's expiry statement is the static footer at device.tsx:234, "Codes expire after 10 minutes."
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] FIXTURE MESSAGES that the loader cannot produce (fixture-quality, not orphan states): login.credentialsError says "Wrong username or password." but web.ts:978 emits "That username and password did not match."; login.totpError says "That code didn't work. Codes rotate every 30 seconds." and login.backupCodeError says "That backup code has already been used." but web.ts:979 emits "That code did not work. Try again." for both. Three fixtures show copy that no request can render.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] COVERAGE GAPS in fixtures.ts (states the loader CAN produce and no fixture draws): consent with namespace "" (absent resource) and with scopes [] (absent scope) — both render a labelled row or a sentence with a hole in it; device enter-code carrying the query error "That code could not be decided." (device.expiredCode carries a code AND an error, which is the other producer); login credentials with an error and NO username echoed (a direct POST omitting the field).
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] TEST GAPS in server/test/worker/web-pages.test.ts for this family: nothing GETs /device (bare), /device?user_code=<bad>, /device?error=… or /device?decided=… — tests 28/29 assert the redirect Location and stop, so the approved and denied cards' RENDER is never exercised. Nothing registers a nameless DCR client, so consent's "An application" fallback is untested. No test in this family bypasses the loader (nothing imports server/dev/fixtures.ts or calls loginProps/deviceProps/consentProps directly; every case goes through the real route). The nearest thing is the two-factor pair: test 24's credentialPages helper (web-pages.test.ts:659-662) and the passkey test at :2835 hand-build `?step=totp` / `?step=backup-code`, a query no product flow emits.
- [/login, /device, /oauth/consent (login.tsx, device.tsx, consent.tsx; loginProps/deviceProps/consentProps in model.ts; the /login/* + /device/* translation routes in web.ts; identity.ts's better-auth mount)] BEHAVIOUR NOTES worth a spec line, not orphans: (1) login.tsx:52 switchMethod builds `${paths.login}?method=${method}` with no `next`, so switching between the TOTP and backup-code cards silently drops the deep link and a verified code lands on /apps instead of where the owner was going. (2) model.ts:2375-2377 trusts `?decided=` alone — GET /device?decided=approved draws the success card for any signed-in visitor who decided nothing. (3) deviceStep ignores better-auth's `status` field (device-authorization/routes.mjs:551-559 returns pending|approved|denied), so revisiting an already-approved code redraws the Approve/Deny card; pressing either then yields DEVICE_CODE_ALREADY_PROCESSED and the generic "could not be decided" error. (4) model.ts:2352 renders `query.get("error")` verbatim (JSX-escaped, so no XSS) on both /login and /device — the hub only ever writes three fixed strings, but a crafted link puts arbitrary copy under the sign-in password field. (5) model.ts:2468-2474 originOf's catch arm is defensive only: the provider validates redirect_uri long before this page renders, so the "show the raw string" fallback is a dead branch by design (the loader's own comment says so).
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] Preview registration is airtight and needs no audit: fixtures.ts:2152 is typed `{ [K in keyof PagePropsByName]: Record<string, PagePropsByName[K]> }`, preview.ts:37 is `Record<PageName, FC<any>>`, preview.ts:65 walks `Object.keys(fixtures)` rather than a hand-written index, and tsconfig.json includes server/dev. So an unregistered fixture and a fixture-less PageName are both compile errors. All 10 page records are non-empty.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] The per-page fixture counts in the brief are stale. Actual (top-level keys per record): login 6, device 5, settings 31, apps 7, app-detail 28, app-new 9, approvals 5, approval-detail 6, audit 5, oauth-consent 4 = 106. The brief's login 7 / settings 34 / apps 8 / app-detail 27 sum to 110; the total 106 is the number that is right.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] fixtures.ts:1586 approvals.decideFailed is the only fixture drawing tone "warning", and it draws copy no route can produce. Real: tone danger, title "Approval decide failed", message = approval_decide's own HubError text. Fixture: tone warning, title "That request is no longer pending", message "It expired at 15:12:31." — a fabricated literal clock as well as a fabricated tone.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] Four more fixtures fabricate notice copy that noticeOf cannot build. web.ts:1068 makes every success `${humanize(op)} done.` with NO title, and web.ts:1073 makes every failure title `${humanize(op)} failed`. (a) fixtures.ts:771 apps.connectFailed — "Could not connect GitHub" / "The provider did not complete the sign-in…"; real is "Connect failed" + upstream's own sentence or "No such app." (b) fixtures.ts:786 apps.archivedNotice — "home is archived. Its roles, grants, and tokens are kept."; real is "App archive done." (c) fixtures.ts:582 settings.error — "Could not disable two-factor" / "That password was not accepted…"; real is "Two factor disable failed" + better-auth's message. (d) fixtures.ts:528 settings.backupCodesRevealed — "Two-factor authentication is on."; real is "Backup codes generate done." The seven Password-pane fixtures (fixtures.ts:367-427) are the exception: they match noticeOf and passwordDone verbatim.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] All 28 app-detail fixtures go through the `appDetail(...)` factory at fixtures.ts:997, which hardcodes `...shell("apps")` (fixtures.ts:1004) with no override for notice or pendingApprovals. So the app page never demos the badge-hidden state and never demos either notice tone — even though its notice banner (app-detail.tsx:998) is genuinely reachable from token_issue refusals and the danger-pane ops. That is the one real preview gap in this family.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] No fixture on any page sets tone "info", and outside approvals.decideFailed none sets "warning" — so the two orphan tones are half-visible in the preview and half-invisible, which is the worst of both.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] On tests: nothing in server/test touches the shell chrome. `grep -rn "nav-badge|nav-link|app-header|header-user|brand" server/test` returns nothing. The approvals badge, the four-item nav, the aria-current on the top nav, the brand link and the username are entirely unasserted — web-pages.test.ts:1607 even notes in passing that the shell renders a second aria-current anchor, and scopes around it rather than pinning it. The pane rail and pill row ARE well covered (web-pages.test.ts:1617, 2010 for settings; 4092, 4102-4127, 4435 for app-detail), including the unread-vs-none marker collision at 4272.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] No test posts to paths.approvalsPush (grep `approvalsPush` over server/test → nothing). approvals.test.ts:1204 exercises `subscribePush` at the module level, below the route — so the web route's CSRF gate, its subscriptionOf shape check, its 400 and its 204 are untested, as is the ordering that keeps `/approvals/push` from being dispatched as an op named "push".
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] web-pages.test.ts:1869 pins "a mutation posted from a pane redirects back to that pane" for the six /settings panes only (it walks settingsPostTargets). There is no equivalent walk for /apps/<slug>, which is why the Connect/Disconnect controls drawn on the app-detail header (model.ts:1743, 1749) landing their notice on /apps instead of the pane go unnoticed.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] No orphan in this family is propped up by a test that bypasses the loader — every `test_bypassing` above is genuinely "none". The orphan tones are propped up by fixtures.ts alone; the /audit notice banner and the service-worker payload fallbacks have neither a test nor a fixture holding them up. The failure mode here is absence of coverage, not fixture-flattered coverage.
- [Shell and shared components — layout.tsx (nav highlight, approvals badge, sign-out, mobile nav), the notice component and format.ts tones, ConfirmShell, PaneRail/PanePills, and the PWA manifest / service worker / push-subscribe flow] Not orphans, but worth one line each while the family is open: the notice component exists in FIVE independent copies of the same four-row tone table (format.ts:53, apps.tsx:213, approvals.tsx:78, settings.tsx:113, audit.tsx:54) and three copies of the icon set, and they already disagree — apps.tsx:222 and app-detail.tsx:999 set role={danger ? "alert" : "status"}, settings.tsx:123 sets role="alert" unconditionally, and approvals.tsx:269 sets no role at all. Same story for ConfirmShell: apps.tsx:242 hand-copies layout.tsx:169's markup and its showModal() script rather than importing it.
