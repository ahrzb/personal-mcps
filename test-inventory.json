{
  "cli/test/plan.test.ts": {
    "parseDesired · defaults and grammar (§9, §15) §15 · log_bodies defaults by kind (tunnel true, proxy false), and an explicit value overrides the kind default in both directions": "todo",
    "parseDesired · defaults and grammar (§9, §15) §9 · `reader:approval` splits into approval mode; bare `reader` is allow — role names carry no colon, so the split is unambiguous": "todo",
    "parseDesired · defaults and grammar (§9, §15) §9 · an unrecognized key (`rols:`) and a wrong-typed field (redact values not string arrays) each throw naming the offending path — a typo never silently plans a role wipe; twin: the correctly spelled file parses": "todo",
    "parseDesired · defaults and grammar (§9, §15) §9 · every default applied — absent kind → tunnel, absent name → slug, description \"\", archived false, redact/redact_results {} — so two files that mean the same thing normalize equal": "todo",
    "parseDesired · defaults and grammar (§9, §15) §9 · proxy defaults: auth \"headers\", forward_identity false": "todo",
    "parseDesired · defaults and grammar (§9, §15) §9 · semantic problems (reserved slug, undeclared role, dual-mode grant) do NOT throw here — they are planChanges' errors, so diff reports every problem in one pass": "todo",
    "planChanges · order and laws (§9) §9 · one fixture exercising all four phases pins the order deletes → creates → updates and archive transitions → grant_set, and a grant_set naming a service created in the same plan sorts after that create": "todo",
    "planChanges · order and laws (§9) §9 · planChanges is total: every semantic problem lands in `errors`, no input throws, and a plan carrying hard errors still returns best-effort steps so diff can show everything at once": "todo",
    "planChanges · order and laws (§9) §9 · the empty-plan law — desired derived from an arbitrary current state plans nothing: no steps, no warnings, no errors. This is the file's churn insurance: it holds across every future field, so adding one to the config language costs one case above, not a rewrite here": "todo",
    "planChanges · severities, every refusal beside its allow-twin (§9) §8 · a kind change on an existing slug is a hard error: kind is immutable and the planner never invents a delete-and-recreate the file did not ask for": "todo",
    "planChanges · severities, every refusal beside its allow-twin (§9) §8/§9 · the reserved `pmcp` slug is a hard error as a `services:` key and inside a `grants:` block alike — the reservation is uniform": "todo",
    "planChanges · severities, every refusal beside its allow-twin (§9) §9 · a grant naming a role a TUNNELED service has not declared warns and still plans the grant_set (the file may be ahead of the first connection); twin: a declared role plans with no warning": "todo",
    "planChanges · severities, every refusal beside its allow-twin (§9) §9 · one role granted in both modes for one (account, service) is a hard error; twin: the same role in a single mode is accepted": "todo",
    "planChanges · severities, every refusal beside its allow-twin (§9) §9 · the built-in `all` is never \"undeclared\": granted on either kind it neither warns nor errors": "todo",
    "planChanges · severities, every refusal beside its allow-twin (§9) §9 · the same undeclared role on a PROXIED service is a hard error — its roles live in this very file; twin: a role the file declares is accepted": "todo",
    "planChanges · the steps a difference produces (§8, §9) §6/§9 · an `archived` difference plans service_archive / service_unarchive, never an update carrying an archived field": "todo",
    "planChanges · the steps a difference produces (§8, §9) §8 · an `auth` mode flip plans a service_update flagged destructive — it wipes the stored upstream credentials; twin: any other update is not": "todo",
    "planChanges · the steps a difference produces (§8, §9) §8 · the builtin `pmcp` row is excluded from the delete computation": "todo",
    "planChanges · the steps a difference produces (§8, §9) §9 · a (account, service) pair absent from `grants:` plans grant_set with an empty role list — absence is desired state, not silence": "todo",
    "planChanges · the steps a difference produces (§8, §9) §9 · file-only service and file-only account → service_create / account_create carrying the normalized fields": "todo",
    "planChanges · the steps a difference produces (§8, §9) §9 · proxy-only fields (endpoint, auth, forward_identity, roles) are diffed like any other field": "todo",
    "planChanges · the steps a difference produces (§8, §9) §9 · server-only service and server-only account → service_delete /account_delete, both flagged destructive (grants cascade, tokens deleted)": "todo",
    "planChanges · the steps a difference produces (§8, §9) §9/§15 · a change in redact, redact_results, or log_bodies alone plans a service_update — either kind": "todo"
  },
  "clients/js/test/api.test.ts": {
    "backoffDelay() · §6 \"Reconnect\" the schedule is the table below — one case per row, no bespoke assertions": "todo",
    "caller() · §7 \"Caller identity forwarding\" §7 · a consumer-shaped forgery is not this library's problem to detect: what arrives under hub/* is read verbatim, because the hub strips inbound copies before injecting its own (the trust argument lives server-side)": "todo",
    "caller() · §7 \"Caller identity forwarding\" §7 · a request that never passed through the hub — _meta absent, or present without hub/* keys — yields principal \"\", empty roles, and a uniformly false hasRole: no error for the author to handle": "todo",
    "caller() · §7 \"Caller identity forwarding\" §7 · hasRole(anything) is true when roles contains \"all\", so an owner ([\"all\"]) and an all-granted account behave identically in service code": "todo",
    "caller() · §7 \"Caller identity forwarding\" §7 · hasRole(x) is true when roles contains x, false for a role not granted — the twin pair in one case": "todo",
    "caller() · §7 \"Caller identity forwarding\" §7 · hub/principal and hub/roles are read off _meta into principal and roles, with roles kept exactly as granted (never expanded)": "todo",
    "secret() and sensitive() · §7 \"Sensitive-field redaction\", §11 §7 · a path naming no property in the schema is a TypeError: a silent typo would quietly persist a secret; twin: the correctly spelled path in the same schema marks it": "todo",
    "secret() and sensitive() · §7 \"Sensitive-field redaction\", §11 §7 · secret() is schema-only — a marked field validates and serializes exactly as the wrapped schema does, so real values still cross the wire and the HUB does the masking (§15)": "todo",
    "secret() and sensitive() · §7 \"Sensitive-field redaction\", §11 §7 · secret() returns a derived schema: the schema passed in is unchanged, and a second call on the same input is independent": "todo",
    "secret() and sensitive() · §7 \"Sensitive-field redaction\", §11 §7 · secret(schema) emits writeOnly: true at that node — inside a tool's INPUT shape and its OUTPUT shape alike (the hub reads both directions)": "todo",
    "secret() and sensitive() · §7 \"Sensitive-field redaction\", §11 §7 · sensitive() copies — the original object is not mutated at any depth": "todo",
    "secret() and sensitive() · §7 \"Sensitive-field redaction\", §11 §7 · sensitive(schema, [\"password\", \"credentials.token\"]) sets writeOnly at a top-level property and at a dot-path, on an input schema and an output schema alike": "todo"
  },
  "clients/js/test/contracts-consumer.test.ts": {
    "close codes → client behavior · §4, §6 §4 · each row's required behavior agrees with the fixture's behavior word for that entry — the fixture's vocabulary is the vocabulary, so a fifth behavior cannot appear on either side unnoticed": "todo",
    "close codes → client behavior · §4, §6 §4 · every entry in contracts/close-codes.json — close codes 4000–4004 and the upgrade statuses 401/403 alike — has exactly one row in transport.test.ts's reconnectPolicyRows": "todo",
    "close codes → client behavior · §4, §6 §4 · every reconnect-policy row names an entry the fixture defines: the table transcribes the oracle and never invents an ending of its own": "todo",
    "close codes → client behavior · §4, §6 §4 · the fixture's behavior vocabulary is closed: every word it uses is one the policy table can express, and no row needs a word it lacks — so a new behavior is a spec change, not a silent addition (finding RESOLVED 2026-08-25: contracts/README.md named FOUR behaviors while the library headers named three, folding max backoff into \"reconnect\". The pinned vocabulary is THREE — stop_fatal, stop_quiet, reconnect — with the fourth word split off as a separate `schedule` attribute, exponential or max_only, which is the shape both row types already carried. README, both client headers and both tables now spell it that one way)": "todo",
    "close codes → client behavior · §4, §6 §4 · the mapping is total in both directions — stop_quiet ↔ resolve, stop_fatal ↔ reject, reconnect ↔ pending — so neither the fixture nor ReconnectPolicyRow can grow an ending the other cannot say": "todo",
    "close codes → client behavior · §4, §6 §4 · the two axes stay separate: a fixture entry's behavior is one of the three words and never a schedule, and only reconnect entries carry a schedule — so \"retry at max backoff\" cannot re-enter as a behavior": "todo",
    "tunnel frames · §4, §6 §6 · the fixture's hub/replaced notification, replayed verbatim, drives the stop-quietly path: the library recognizes the pinned frame, not a paraphrase of it": "todo",
    "tunnel frames · §4, §6 §6 · the fixture's ok reply, replayed verbatim, completes registration; the fixture's rejection reply, replayed verbatim, produces RegistrationError — the refusal beside its allow-twin, both read from the fixture": "todo",
    "tunnel frames · §4, §6 §6 · the hub/register frame the library emits deep-equals the fixture's request shape: the method name and the params keys clientVersion, protocolVersion, roles — and no service or slug field, ever, because identity comes exclusively from the token": "todo",
    "tunnel frames · §4, §6 §6 · the protocolVersion the library sends equals the fixture's pinned revision — a revision bump is a fixture commit, owner-run, and this case is what makes the client's copy follow it": "todo",
    "tunnel frames · §4, §6 §6 · the set of hub/* control methods in the fixture equals the set the transport consumes internally — a method outside that set reaches the SDK session untouched, so a new control frame cannot be swallowed silently": "todo"
  },
  "clients/js/test/transport.test.ts": {
    "handshake · §6 \"Transport\", \"Framing\", \"Handshake\" §6 · close() is idempotent: it stops reconnecting and resolves `closed`, and a second call changes nothing": "todo",
    "handshake · §6 \"Transport\", \"Framing\", \"Handshake\" §6 · hub/* control frames are consumed internally and never surface on onmessage; ordinary MCP traffic does, one JSON-RPC message per text frame": "todo",
    "handshake · §6 \"Transport\", \"Framing\", \"Handshake\" §6 · send() while the socket is down drops the message and does not throw or queue it — the hub re-lists after every registration, so a dropped notifications/tools/list_changed heals itself": "todo",
    "handshake · §6 \"Transport\", \"Framing\", \"Handshake\" §6 · start() resolves only after hub/register is accepted, and the frame sent is the declaration handed to the constructor (roles verbatim, `{}` when omitted)": "todo",
    "handshake · §6 \"Transport\", \"Framing\", \"Handshake\" §6 · the constructor rejects anything but a bare https origin (a path, a wss URL) with a TypeError before any I/O; twin: a bare origin constructs and dials wss://<host>/connect, derived — never passed in": "todo",
    "handshake · §6 \"Transport\", \"Framing\", \"Handshake\" §6 · the dial carries the service token as `Authorization: Bearer`, and carries no service or slug anywhere: identity rides the token alone": "todo",
    "reconnection is invisible to the SDK session · §6, §11 §11 · serve()'s resolution mirrors the transport's terminal state — resolves after a replacement, rejects with the same error class otherwise — so the policy is decided in HubTransport and nowhere else": "todo",
    "reconnection is invisible to the SDK session · §6, §11 §6 · a mid-life network drop reconnects and re-sends hub/register, while onclose never fires and `closed` never settles — one transport is one service lifetime, not one socket": "todo",
    "reconnection is invisible to the SDK session · §6, §11 §6 · onclose fires exactly once, and only at a terminal state": "todo",
    "the policy itself · §6 upgrade matrix + close codes the table below — one case per row, plus its allow-twin row: every refusal ending (401, 4001, a rejected registration) is authored beside the ending that keeps the connection alive, so a transport that gives up on everything cannot pass (strategy §9 rule 2)": "todo"
  },
  "scripts/test/bootstrap-contract.test.ts": {
    "main(), the printing contract · §12 §12 · `create` and `reset-password` write the generated password to stdout exactly once and the rotate-the-master-key reminder to stderr; the secret itself is never printed on either stream": "todo",
    "main(), the printing contract · §12 §12 · exit 0 on success and 1 on any failure, and a missing BOOTSTRAP_SECRET or PMCP_URL fails before any request is made — the master key is read from the environment, never from a flag": "todo",
    "the copied wire shapes · §4 §12 · `delete` of an absent username is an ordinary success shape — the postcondition is absence, not existence; twin: `delete` of a live username produces the same shape, so the caller cannot use the response to probe which usernames exist": "todo",
    "the copied wire shapes · §4 §4 · each BootstrapRequest variant serializes deep-equal to its request shape in the fixture — the client copy is checked against the oracle, not against itself": "todo",
    "the copied wire shapes · §4 §4 · each fixture response shape parses into the BootstrapResponse variant echoing its `op`, including `list` with an empty `usernames` array": "todo",
    "the request · §12 §12 · bootstrap never retries on its own — the stub sees exactly one call per invocation, for a 5xx as much as for a 200: `create` is not idempotent and every accepted invocation is audited server-side": "todo",
    "the request · §12 §12 · one op per request: the serialized body is exactly the BootstrapRequest for that invocation, with no extra fields and no password in either direction of a `create`": "todo",
    "the request · §12 §12 · the POST targets <origin>/internal/users and carries the secret as `Authorization: Bearer` — never in the body, never in the URL or query string, so it stays out of logs and process listings": "todo",
    "the status mapping · §12 the table below — one case per row, refusals and the 2xx twin in one list": "todo"
  },
  "server/test/tunnel/approval-e2e.test.ts": {
    "§7 availability-first, both directions 12. §7 · a known-offline tunnel is refused -32000 BEFORE the gate reads anything: no pending row, no audit approval.requested, no push — twin: the same call against an online service opens the pending": "todo",
    "§7 availability-first, both directions 13. §7 · an approved pass survives a -32000 for an offline service untouched: the row still reads `approved` and the later retry executes once": "todo",
    "§7 availability-first, both directions 14. §7 · unavailability discovered BETWEEN check and claim yields -32000 with the pass unconsumed — twin: availability holding through the same window consumes it and executes": "todo",
    "§7 availability-first, both directions 15. §7 · a dispatch failure AFTER a successful claim leaves the pass consumed (a call may already have reached the service, §15) and the retry gets a fresh -32003": "todo",
    "§7 MRTR — one approval, many legs 16. §7 · an approved call whose relayed result is input_required restores the row to `approved`, and the consumer receives that result verbatim": "todo",
    "§7 MRTR — one approval, many legs 17. §7 · the follow-up leg carrying inputResponses/requestState rides the original approval and reaches the service (the counter advances per leg, no new -32003)": "todo",
    "§7 MRTR — one approval, many legs 18. §7 · a complete result consumes the pass: the next identical call opens a fresh pending": "todo",
    "§7 MRTR — one approval, many legs 19. §7 · inputResponses/requestState never enter the binding or the stored args_json — the follow-up leg matches on params.arguments alone": "todo",
    "§7 the approval loop over a real tunnel 1. §7 step 2 · a gated call answers -32003 carrying approvalId, approvalUrl and expiresAt, and the service's invocation counter stays at zero": "todo",
    "§7 the approval loop over a real tunnel 2. §7 step 2 · a retry while still pending returns the SAME approvalId, with no second row, no second approval.requested audit row, and no second push": "todo",
    "§7 the approval loop over a real tunnel 3. §7 step 4 · after approve, the identical retry executes exactly once — counter 1 (the allow-twin every refusal in this file is measured against)": "todo",
    "§7 the approval loop over a real tunnel 4. §7 step 4 · a second identical call after consumption opens a fresh pending and the counter stays at 1": "todo",
    "§7 the approval loop over a real tunnel 5. §7 · arguments differing in a visible field do not match the pass: fresh -32003, counter unchanged": "todo",
    "§7 the approval loop over a real tunnel 6. §7 step 4 · a rejected request answers -32003 with a fresh pending row, and the rejected row stays rejected": "todo",
    "§7 the approval loop over a real tunnel 7. §7 · a pass past expires_at reads as expired everywhere and emits approval.expired exactly once — driven through the injected clock against limits.APPROVAL_WINDOW_MS, never slept": "todo",
    "§7 the approval loop over a real tunnel 8. §7 · owners are never approval-gated: the same tool called by the owner executes with no approval row created at all": "todo",
    "§7 the CAS claim under deterministic interleavings 10. §7 step 1 · the initial check never authorizes dispatch: the leg whose claim changes no rows falls through to a fresh -32003, and the counter still reads 1 (the row that goes red against a SELECT-then-dispatch implementation)": "todo",
    "§7 the CAS claim under deterministic interleavings 11. §7 step 1 · a claim lost to a concurrent identical call leaves that caller with a NEW pending row rather than a second execution": "todo",
    "§7 the CAS claim under deterministic interleavings 9. §7 step 1 · interleaving table — one case per row of `casInterleavings`, driven by runCasInterleaving(row, casInterleavings)": "todo",
    "§7 the CAS claim under deterministic interleavings 9b. §9 rule 2 · every row either executes a leg itself or its `twin` resolves to a row of this table that does — the invariant a hub dispatching nothing at all cannot satisfy, and the only thing standing between this table and a perfect exactly-once score for doing nothing": "todo",
    "§7 the redaction union and the catalog 20. §7 · the stored args_json masks the UNION over the live catalog: schema `writeOnly` paths from the DO's cached inputSchema plus config `redact` paths, while the service still receives the real values": "todo",
    "§7 the redaction union and the catalog 21. §7 · args_hash is post-redaction: two calls differing only in a redacted field share one approval row — twin: differing in a visible field, they do not (case 5)": "todo",
    "§7 the redaction union and the catalog 22. §7 · a tool absent from the cached catalog is refused -32001 — the same code as ungranted and unknown, with no pending row created; twin: the catalogued tool opens one (case 1)": "todo",
    "§7 the redaction union and the catalog 23. §7 · a never-connected service refuses the same way and heals: after the service registers, the identical call opens a pending instead of -32001": "todo"
  },
  "server/test/tunnel/hibernation.test.ts": {
    "§6 across an evictDurableObject boundary 1. §6 · survival table — one case per row of `evictionSurvivalRows`, driven by runEvictionSurvivalCase": "todo",
    "§6 across an evictDurableObject boundary 2. §6 · a registered socket still round-trips a tools/call after eviction — this file's premise, and the protocol-level twin of smoke.test.ts's A2": "todo",
    "§6 across an evictDurableObject boundary 3. §6 · identity survives: the frame forwarded after eviction still carries the hub's own hub/principal and hub/roles, resolved from the attachment": "todo",
    "§6 across an evictDurableObject boundary 4. §6 · status() reads online after eviction — `registered` rides the attachment, not an instance field, so availability-first refusals stay correct across a hibernation": "todo",
    "§6 across an evictDurableObject boundary 5. §6 · the cached catalog is served after eviction, and after eviction *while offline* too (DO SQLite, not memory)": "todo",
    "§6 across an evictDurableObject boundary 6. §6 · the pending map is EMPTY after eviction — §6's hibernation-safety assumption, validated rather than assumed; nothing here asserts it survives": "todo",
    "§6 across an evictDurableObject boundary 7. §6 · a forward issued after eviction correlates on a fresh wire id and resolves normally: the empty map costs the next caller nothing (the allow-twin of case 6)": "todo",
    "§6 owner actions across the boundary 12. §6 · sever() after eviction closes with the right code and still honors onlyIfTokenId — the opening token id rides the attachment": "todo",
    "§6 owner actions across the boundary 13. §6 · wipe() after eviction still returns the DO to never-connected (the catalog it erases is the one case 5 proved survived)": "todo",
    "§6 the deadline alarm across hibernation 8. §6 · an accepted-but-unregistered socket evicted, then runDurableObjectAlarm → close 4004: the deadline is a storage alarm precisely so it outlives hibernation": "todo",
    "§6 the deadline alarm across hibernation 9. §6 · a registered socket evicted, then the same alarm → a no-op, socket still open and still online (the allow-twin of case 8)": "todo",
    "§6/§10 attachment versioning 10. §6 · a wake reading an attachment with an unknown or absent `v` closes 4004, so deploy version-skew heals as a routine reconnect": "todo",
    "§6/§10 attachment versioning 11. §6 · a wake reading the current `v: 1` attachment proceeds normally — the allow-twin of case 10, and what keeps case 10 from being satisfied by closing every woken socket": "todo"
  },
  "server/test/tunnel/lifecycle.test.ts": {
    "§15 ordering, observed live 10. §6 · at the moment the client observes 4002, the archived flag is already set — a reconnect racing the close can only be refused 403": "todo",
    "§15 ordering, observed live 11. §6 · a re-register arriving after the row is gone closes 4003 instead of resurrecting the service (twin of case 9's ordering)": "todo",
    "§15 ordering, observed live 12. §6 · a re-register arriving after an archive is refused at the upgrade, never at registration (twin of case 10)": "todo",
    "§15 ordering, observed live 9. §15 · at the moment the client observes 4001, D1 already has no service row — the cascade is D1-first, so a racing re-register finds nothing": "todo",
    "§6 catalog and wipe 13. §6 · the cached catalog survives an ordinary disconnect: tools/list still serves it while the service is offline — deploy-induced flapping never churns an agent's tool list": "todo",
    "§6 catalog and wipe 14. §6 · archival keeps the catalog: unarchive restores a service that lists exactly what it listed before (the allow-twin of case 15)": "todo",
    "§6 catalog and wipe 15. §6 · wipe() returns the DO to never-connected: listTools is empty afterwards": "todo",
    "§6 catalog and wipe 16. §6 · wipe() is idempotent, and safe against a DO that was never woken": "todo",
    "§6 catalog and wipe 17. §6 · wipe() leaves a live socket alone — severing first is admin's ordering, not the DO's business": "todo",
    "§6 catalog and wipe 18. §6 · status() reads offline for an accepted-but-unregistered socket and online only after hub/register — the twin pair the approval gate's availability-first refusal depends on": "todo",
    "§6 the upgrade matrix 1. §6 · upgrade table — one case per row of `upgradeRows`, driven by runUpgradeCase(row, upgradeRows)": "todo",
    "§6 the upgrade matrix 1b. §9 rule 2 · every 401/403 row's `twin` resolves to a 101 row present in this table, differing by one column — without it, a handleConnect that accepts nothing passes the whole matrix": "todo",
    "§6 the upgrade matrix 2. §6 · 403 means exactly archived: over the whole table, the archived rows are the only 403s — a totality check on the oracle itself, so a new refusal can't quietly borrow the archived code": "todo",
    "§6 the upgrade matrix 3. §6 · an accepted upgrade leaves the DO holding exactly one socket, and every refusal leaves it holding none": "todo",
    "§6 the upgrade matrix 4. §6 · an unarchive heals with no bot involvement: the same credential refused 403 is accepted 101 once the flag clears (the allow-twin of the 403 rows)": "todo",
    "§6/§8 severing a live connection 5. §6 · sever table — one case per row of `severRows`, driven by runSeverCase(row, severRows)": "todo",
    "§6/§8 severing a live connection 5b. §9 rule 2 · every closing row's `twin` resolves to a `close: \"none\"` row present in this table — the survivor that a sever() closing everything it can reach cannot produce": "todo",
    "§6/§8 severing a live connection 6. §8 · token_revoke closes only the socket that token opened; a socket opened with the service's other live token stays up and still forwards": "todo",
    "§6/§8 severing a live connection 7. §6 · sever against an offline service is a no-op and never throws": "todo",
    "§6/§8 severing a live connection 8. §6 · in-flight consumers are failed fast when the socket goes: a call awaiting a response gets -32000 at the close, not at limits.CALL_TIMEOUT_MS": "todo"
  },
  "server/test/tunnel/pipeline-tunnel.test.ts": {
    "§15 deadline, disconnect, and the audit chokepoint 13. §15 · a service that never answers fails -32000 at limits.CALL_TIMEOUT_MS — asserted against the constant with the run's value shrunk, never waited out": "todo",
    "§15 deadline, disconnect, and the audit chokepoint 14. §15 · a disconnect mid-call fails the waiting consumer -32000 immediately rather than at the deadline (the allow-twin: a service that answers in time resolves)": "todo",
    "§15 deadline, disconnect, and the audit chokepoint 15. §15 · an offline service fails -32000 on call while its cached tools/list still lists tools — the pair that keeps \"unavailable\" from meaning \"invisible\"": "todo",
    "§15 deadline, disconnect, and the audit chokepoint 16. §15 · every resolved tools/call writes exactly one audit row carrying a hub-measured duration_ms": "todo",
    "§15 deadline, disconnect, and the audit chokepoint 17. §15 · a denied tools/call writes one row too, with its refusal code as outcome — denials are just fast (the twin of case 16)": "todo",
    "§15 deadline, disconnect, and the audit chokepoint 18. §15 · tools/list writes no audit row on either endpoint shape — kept out by vocabulary, not by filtering": "todo",
    "§15 deadline, disconnect, and the audit chokepoint 19. §15 · a failed audit write fails the call: the ledger is chosen over availability": "todo",
    "§15 deadline, disconnect, and the audit chokepoint 20. §7 · the consumer's relayed result is never masked, while the audit row written for that same call is — masking exists for persistence, not for the wire": "todo",
    "§7 `_meta` hygiene, observed at the service 10. §7 · the consumer's clientCapabilities are mirrored onto the forwarded request, and a consumer that declared none forwards `{}` so the service refrains from elicitation": "todo",
    "§7 `_meta` hygiene, observed at the service 11. §7 · an owner's call forwards hub/roles [\"all\"] literally, never expanded into the service's declared role names": "todo",
    "§7 `_meta` hygiene, observed at the service 12. §6 · ids never cross: the consumer's JSON-RPC id never appears on the socket, and the response the consumer receives bears the consumer's id, not the wire id": "todo",
    "§7 `_meta` hygiene, observed at the service 7. §7 · hygiene table — one case per row of `metaHygieneRows`, driven by runMetaHygieneCase": "todo",
    "§7 `_meta` hygiene, observed at the service 8. §7 · a forged consumer `hub/roles` never reaches the service; the hub's own resolution arrives in its place (strip-then-set, overwrite never merge)": "todo",
    "§7 `_meta` hygiene, observed at the service 9. §7 · progressToken and unrecognized vendor `_meta` keys arrive untouched — the allow-twin of case 8": "todo",
    "§7 both endpoint shapes, one pipeline 1. §7 · scoped tools/list serves the caller's filtered catalog with unprefixed names": "todo",
    "§7 both endpoint shapes, one pipeline 2. §7 · aggregated tools/list prefixes `<slug>_` and spans only services the caller holds a grant on": "todo",
    "§7 both endpoint shapes, one pipeline 3. §7 · the same tool called through both shapes reaches the service with identical params — the prefix is split before anything else runs": "todo",
    "§7 both endpoint shapes, one pipeline 4. §7 · role filtering bounds both surfaces: a tool outside the granted patterns is absent from the listing and answers -32001 on call, while a matched tool lists and executes (the refusal and its allow-twin in one pair)": "todo",
    "§7 both endpoint shapes, one pipeline 5. §7 · a granted role the service has not declared yields an empty listing and -32001 — not a 404: the account still holds a grant": "todo",
    "§7 both endpoint shapes, one pipeline 6. §7 · served outputSchemas carry no `writeOnly` while inputSchemas keep theirs, and the DO's cached catalog still holds both verbatim (stripping is serving-time, never storage-time)": "todo"
  },
  "server/test/tunnel/protocol.test.ts": {
    "§6 audit of the connection lifecycle 11. §6 · a successful registration writes exactly one connect.register row": "todo",
    "§6 audit of the connection lifecycle 12. §6 · a widening re-registration writes one connect.roles_widened row naming the affected roles (widening SEMANTICS are worker/registry.test.ts's)": "todo",
    "§6 audit of the connection lifecycle 13. §6 · a re-registration whose pattern sets are a subset writes no roles_widened row (the allow-twin of case 12)": "todo",
    "§6 liveness 18. §6 · no application-level heartbeat: an idle registered socket receives no hub/* frame at all, and stays online across an idle stretch (protocol pings are the runtime's business, invisible to the DO)": "todo",
    "§6 registration 1. §6 · registration table — one case per row of `registrationRows`, driven by runRegistrationCase(row, registrationRows)": "todo",
    "§6 registration 10. §6 · the hub never forwards consumer traffic to an unregistered socket: a tools/call while a socket sits accepted-but-unregistered fails -32000": "todo",
    "§6 registration 1b. §9 rule 2 · every refusal row's `twin` resolves to a registering row present in this table — the invariant that makes case 2 the measure of cases 5-10 rather than a sentence in a comment above them": "todo",
    "§6 registration 2. §6 · a valid hub/register replies { ok: true } and the service reads online (the allow-twin every refusal below is measured against)": "todo",
    "§6 registration 3. §6 · registration warms the catalog: the fake service observes exactly one hub-originated tools/list immediately after the reply": "todo",
    "§6 registration 4. §6 · initialize never crosses the socket — the first hub-originated message of the connection's life is that tools/list, self-contained with its protocol _meta fields": "todo",
    "§6 registration 4a. §7 · a catalog answer carrying a tool whose schema trips the indirection refuse-line is reported LOUDLY and survives: the service receives a warning frame naming that tool's violations, the registration still succeeds, the service reads online, and that tool is cached schema-unsound (what unsoundness then costs a call — sensitivePaths null, -32001, no recorded bodies — is the worker project's)": "todo",
    "§6 registration 4b. §7 · the same catalog with a walkable schema draws no warning frame and caches the tool sound — the allow-twin without which a warm that warns about everything, or that refuses to register at all, passes case 4a": "todo",
    "§6 registration 4c. §7 · one unsound tool does not contaminate its siblings: the other tools in the same catalog answer are cached sound, so the refusal is per tool and never per service": "todo",
    "§6 registration 5. §6 · a socket silent past limits.REGISTRATION_DEADLINE_MS is closed 4004 when the pending alarm fires (fired, never slept)": "todo",
    "§6 registration 6. §6 · the same alarm is a no-op once registration succeeded: the socket is still open and still online afterwards": "todo",
    "§6 registration 7. §6 · a non-hub/register frame before registration gets a JSON-RPC error reply and then close 4004 — twin of case 2, where the same frame sent after registration is ordinary MCP traffic": "todo",
    "§6 registration 8. §6 · a refused declaration gets an error reply and close 4004, and D1's stored roles are unchanged — twin: the accepted declaration of case 2 stores them": "todo",
    "§6 registration 9. §6 · a service row deleted between upgrade and register closes 4003 — twin: case 2's surviving row registers": "todo",
    "§6 replacement — newest wins at acceptance 14. §6 · a second socket for the same service: the first receives hub/replaced and is then closed 4000": "todo",
    "§6 replacement — newest wins at acceptance 15. §6 · eviction happens at acceptance, before the newcomer registers — at the moment the newcomer is accepted the DO holds exactly one socket": "todo",
    "§6 replacement — newest wins at acceptance 16. §6 · the eviction stands even when the newcomer's own registration is then refused: the service is offline, not healed back to the old socket (§6's accepted consequence, stated as a test so it can never be \"fixed\" silently)": "todo",
    "§6 replacement — newest wins at acceptance 17. §6 · every replacement writes a connect.replaced audit row — with a stolen service token, impersonation looks exactly like this, so the row is the signal": "todo",
    "§6 the published wire vocabulary — the behavior↔table lock 19. §6 · observed close codes and hub/* method names equal the exported vocabulary: every code this file's cases observe on a real socket is the VALUE of its CLOSE_* export (CLOSE_REPLACED, CLOSE_ROW_GONE, CLOSE_PROTOCOL) and every hub-originated control frame's method is a value of HUB_METHODS — asserted against the exports, never against a literal, so the fixtures contracts.test.ts emits from them describe this socket rather than a parallel opinion of it": "todo",
    "§6 the published wire vocabulary — the behavior↔table lock 20. §6 · totality the other way: no hub-originated control frame carries a method outside HUB_METHODS, so a new control frame cannot enter the protocol without entering the published table — the half of the lock that a growing vocabulary would otherwise slip past": "todo"
  },
  "server/test/tunnel/smoke.test.ts": {
    "A1 — a SQLite-backed Durable Object under workerd 1. platform A1 · a fresh ServiceConnection stub answers an RPC method call": "todo",
    "A1 — a SQLite-backed Durable Object under workerd 2. platform A1 · a row written to DO SQLite reads back on a second, separate call into the same instance": "todo",
    "A1 — a SQLite-backed Durable Object under workerd 3. platform A1 · DO SQLite is per-instance: a stub for a second service id sees none of the first's rows (the isolation this whole directory leans on)": "todo",
    "A2 — evictDurableObject with hibernating sockets 4. platform A2 · a socket accepted with ctx.acceptWebSocket still round-trips a text frame after evictDurableObject({ webSockets: \"hibernate\" }) — hibernated, not closed": "todo",
    "A2 — evictDurableObject with hibernating sockets 5. platform A2 · eviction really discards in-memory state: an instance field set before the boundary is gone after it (the negative twin of case 4, and the premise under §6's in-memory pending map)": "todo",
    "A2 — evictDurableObject with hibernating sockets 6. platform A2 · THE GATE — a value written with serializeAttachment before eviction deserializes intact after it, so ConnectionAttachment can carry identity through hibernation (§6)": "todo",
    "A2 — evictDurableObject with hibernating sockets 7. platform A2 · DO SQLite survives eviction untouched — the cached catalog's durability premise, stated beside its in-memory twin in case 5": "todo",
    "A2 — evictDurableObject with hibernating sockets 8. platform A2 · a storage alarm set before eviction is still pending after it": "todo",
    "project sanity — the serial tunnel project 10. platform · two DO ids created inside one file stay independent under --no-isolate: state written through one stub is invisible through the other": "todo",
    "project sanity — the serial tunnel project 9. platform · runDurableObjectAlarm fires a pending alarm immediately, so limits.REGISTRATION_DEADLINE_MS is a constant this suite reads, never a duration it waits out": "todo"
  },
  "server/test/unit/canonical.test.ts": {
    "§7 step 4 · canonicalJson — equivalence classes (table) §7 step 4 · across classes · no two representatives share a string (catches over-normalization)": "passed",
    "§7 step 4 · canonicalJson — equivalence classes (table) §7 step 4 · the classes must cover · key order at depth · undefined ≡ {} · undefined-valued properties omitted": "passed",
    "§7 step 4 · canonicalJson — equivalence classes (table) §7 step 4 · within a class · every member canonicalizes to the representative's string": "passed",
    "§7 step 4 · canonicalJson — laws §7 step 4 · law · array order is significant · [1,2] and [2,1] never canonicalize alike": "passed",
    "§7 step 4 · canonicalJson — laws §7 step 4 · law · idempotent · canonicalJson(JSON.parse(canonicalJson(v))) === canonicalJson(v)": "passed",
    "§7 step 4 · canonicalJson — laws §7 step 4 · law · key order is irrelevant at every depth · a shuffled-keys copy canonicalizes identically": "passed",
    "§7 step 4 · canonicalJson — laws §7 step 4 · law · keys sort by code unit, not by locale — the same string in workerd and in Node": "passed",
    "§7 step 4 · canonicalJson — laws §7 step 4 · law · no insignificant whitespace · no space appears outside a string literal": "passed",
    "§7 step 4 · canonicalJson — laws §7 step 4 · law · retry identity IS string equality · two argument objects match iff their canonical strings match": "passed",
    "§7 step 4 · canonicalJson — laws §7 step 4 · law · scalars render as JSON.stringify renders them · escapes, negative zero, exponents": "passed",
    "§7 step 4 · canonicalJson — what it refuses §7 step 4 · a BigInt throws (allow-twin: the same value as a number, and as a string, canonicalize)": "passed",
    "§7 step 4 · canonicalJson — what it refuses §7 step 4 · a cycle throws (allow-twin: the same object graph with the back-edge removed canonicalizes)": "passed",
    "§7 step 4 · canonicalJson — what it refuses §7 step 4 · a repeated non-cyclic reference is not mistaken for a cycle — it appears twice, expanded": "passed"
  },
  "server/test/unit/filter.test.ts": {
    "§7 step 2 · buildToolFilter — laws §7 step 2 · law · allow beats approval under every permutation of the grant entries (rules out order-dependent precedence)": "passed",
    "§7 step 2 · buildToolFilter — laws §7 step 2 · law · buildToolFilter is pure · neither the entries array nor the declaration is mutated": "passed",
    "§7 step 2 · buildToolFilter — laws §7 step 2 · law · check() is total · any string answers with a mode, never throws": "passed",
    "§7 step 2 · buildToolFilter — laws §7 step 2 · law · filterList keeps exactly the tools check() does not deny — the two answers can never disagree": "passed",
    "§7 step 2 · buildToolFilter — laws §7 step 2 · law · filterList preserves input order and element identity — it filters, it never rebuilds": "passed",
    "§7 step 2 · buildToolFilter — laws §7 step 2 · law · roleNames is the granted names verbatim · `all` stays literal, undeclared names stay present": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §2 · `all` granted against an empty declaration still allows — reserved, yet grantable": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · a declaration holding an uncompilable pattern · its role matches nothing, the sibling role still resolves": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · allow-mode and approval-mode roles both matching a tool · allow wins": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · approval-only match → approval; no match → deny": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · granted-but-undeclared role · present in roleNames, matches nothing (twin: the declared scenario below)": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · one grant on the service · roleNames is non-empty even when no tool matches": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · owner grant · `all` allows every tool and the declaration is never consulted": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · the same role once declared · matches its patterns, denies the rest": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · union across roles · a tool matched by either granted role is reachable": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §7 step 2 · zero grants · roleNames is empty — the gateway's scoped-404 signal (twin: the one-grant scenario below)": "passed",
    "§7 step 2 · buildToolFilter — the scenario table §9 rule 2 · the table itself · every all-deny scenario names its allow-twin": "passed"
  },
  "server/test/unit/pattern.test.ts": {
    "§6 + §7 step 2 · the two exports agree §6 · law · validateRoles is pure · the declaration handed in is not mutated": "passed",
    "§6 + §7 step 2 · the two exports agree §6+§7 · law · every pattern validateRoles accepts is usable by matchesPattern without throwing": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · {} is valid — \"no roles declared\" is a declaration, not an omission": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · a pattern at ROLE_PATTERN_MAX_LENGTH validates, one character over is a violation": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · a pattern that cannot compile is a violation (twin: the same pattern with the syntax error fixed)": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · a role at ROLE_PATTERNS_MAX patterns validates, one pattern over is a violation": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · a role name at ROLE_NAME_MAX_LENGTH validates, one character over is a violation": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · a role name outside [a-z0-9_-] is a violation; its in-charset twin is not": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · a well-formed declaration yields no violations — the allow anchor every refusal row is read against": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · every boundary row is synthesized from the limits.ts constant it names — no cap number is spelled in this file": "passed",
    "§6 hub/register · validateRoles — the declaration gate (table) §6 · the role name `all` is reserved and can never be declared (twin: `all_tools` is an ordinary name)": "passed",
    "§7 step 2 · matchesPattern — laws §7 step 2 · law · matchesPattern is pure · repeated calls with the same arguments agree (no compiled-regex lastIndex leak)": "passed",
    "§7 step 2 · matchesPattern — laws §7 step 2 · law · matchesPattern is total · every table pattern against every table tool returns a boolean, never throws": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · .* is not double-expanded — a * already preceded by . stays one wildcard": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · a bare * matches every tool name — the `all` role's pattern": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · a pattern's match is exact-length · search_ does not match search_docs": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · an uncompilable pattern matches nothing and never throws (allow-twin: the same pattern, fixed)": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · compilation carries no flags · get_.* does not match GET_NEWS": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · foo|bar matches foo and bar and never foox — the anchor wraps the alternation": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · get_* and get_.* accept and reject exactly the same tools": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §7 step 2 · literal get.news matches get.news and never getXnews — the dot is not a metacharacter": "passed",
    "§7 step 2 · matchesPattern — the pattern language (table) §9 rule 2 · the table itself · every row with no matches names its allow-twin": "passed"
  },
  "server/test/unit/redact.test.ts": {
    "§7 · applyRedaction — masking, direction-blind (table) §7 · a masked non-string value becomes registry.REDACTED — the type changes, the field never survives": "passed",
    "§7 · applyRedaction — masking, direction-blind (table) §7 · a nested path is masked, its parent object otherwise intact": "passed",
    "§7 · applyRedaction — masking, direction-blind (table) §7 · a path absent from the body is ignored — no key is invented (twin: the same path present is masked)": "passed",
    "§7 · applyRedaction — masking, direction-blind (table) §7 · a path meeting an array masks every element": "passed",
    "§7 · applyRedaction — masking, direction-blind (table) §7 · a path naming an object masks the whole subtree, never just its leaves": "passed",
    "§7 · applyRedaction — masking, direction-blind (table) §7 · a top-level path is masked and its siblings are untouched": "passed",
    "§7 · applyRedaction — masking, direction-blind (table) §7 · an empty path list returns an unchanged, deep-equal copy": "passed",
    "§7 · the two halves agree — laws §7 · law · direction-blind · the same (body, paths) masks identically whether it came from params.arguments or structuredContent": "passed",
    "§7 · the two halves agree — laws §7 · law · idempotent · masking a masked body changes nothing further": "passed",
    "§7 · the two halves agree — laws §7 · law · pure · the input body is never mutated, and the result is a new object even when nothing matched": "passed",
    "§7 · the two halves agree — laws §7 · law · round-trip · every path writeOnlyPaths finds is masked when applied to a body shaped by that schema": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · `$anchor` is refused (twin: the same target addressed by JSON Pointer is accepted)": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · `$dynamicRef` is refused (twin: the static `$ref` to the same subschema is accepted)": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · `$id` is refused — re-basing changes what a ref resolves to (twin: the same schema without it is accepted)": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · a recursive cycle CARRYING a writeOnly is refused — its path set is infinite, so no finite dot-path list can express the mask (twin: the same cycle with no mark inside it is accepted and cut)": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · a walkable schema — no indirection at all — yields no violations (the anchor every refusal is read against)": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · an external or non-`#/` ref is refused, naming the construct (twin: the same shape via `#/$defs/…` is accepted)": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · every accepted row is then walkable: writeOnlyPaths returns the row's paths and never throws — what the validator admits, the walk resolves, so the two can never drift into separate opinions": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §7 · law · pure · the schema handed in is not mutated, and repeated calls agree": "passed",
    "§7 · validateSchemaIndirection — the refuse-line (table) §9 rule 2 · the table itself · every refusing row names an allowTwin using the same construct legally": "passed",
    "§7 · writeOnlyPaths — indirection the walk RESOLVES (table) §7 · a JSON Pointer with escapes (~0, ~1) resolves to the same subschema a plain key would": "passed",
    "§7 · writeOnlyPaths — indirection the walk RESOLVES (table) §7 · a mark in ONE anyOf branch masks the property — over-masking is safe, so the union is taken, not the intersection (twin: no branch marks it and no path is yielded)": "passed",
    "§7 · writeOnlyPaths — indirection the walk RESOLVES (table) §7 · a ref chain (a property → $defs/A → $defs/B) resolves through to B's marks": "passed",
    "§7 · writeOnlyPaths — indirection the walk RESOLVES (table) §7 · a ref reaching an outputSchema shape resolves exactly as its inputSchema twin does — indirection is direction-blind too": "passed",
    "§7 · writeOnlyPaths — indirection the walk RESOLVES (table) §7 · a secret-free cycle is CUT and yields the paths outside it, terminating rather than throwing (twin: the same shape with the cycle broken yields exactly the same paths)": "passed",
    "§7 · writeOnlyPaths — indirection the walk RESOLVES (table) §7 · a writeOnly inside $defs, reached only by a property's `#/$defs/…` ref, yields that property's path — the shape a plain-SDK bot emits by default, and the reason inlining stays optional sugar": "passed",
    "§7 · writeOnlyPaths — indirection the walk RESOLVES (table) §7 · allOf and oneOf union identically to anyOf — composition is one rule, not three": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §15 · the token_issue shape · a writeOnly OUTPUT field is found like any other (the uniform rule, no pmcp special case)": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · a malformed schema yields [] and never throws (twin: the same schema repaired yields its paths)": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · a nested object yields the dotted path — credentials.token, at any depth": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · a top-level property marked writeOnly yields its name; its unmarked sibling yields nothing": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · an absent schema and a non-object schema both yield []": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · an array of objects yields the element path with no index segment — the grammar has no [] form": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · an outputSchema shape yields exactly what its inputSchema twin yields — the walk reads no direction": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · several markers in one schema yield every path, as a set, without duplicates": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §7 · writeOnly: false and an absent marker are both \"not sensitive\"": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §9 rule 1 · the table itself · every walk row's schema passes validateSchemaIndirection — the walk's contract is stated over walkable schemas only, so an unwalkable one is the table below's row": "passed",
    "§7 · writeOnlyPaths — the walk, direction-blind (table) §9 rule 2 · the table itself · every \"no path\" row sits beside the same shape with the marker set": "passed"
  },
  "server/test/worker/admin-ops.test.ts": {
    "§15 — deleting cascades are one atomic D1 batch §15 · a service_delete refused at validation leaves every row in place — no partial batch": "passed",
    "§15 — deleting cascades are one atomic D1 batch §15 · account_delete removes the account row, its grants (FK cascade), and its token rows together": "passed",
    "§15 — deleting cascades are one atomic D1 batch §15 · service_delete on a proxied service stops after the batch: no DO, no tokens": "passed",
    "§15 — deleting cascades are one atomic D1 batch §15 · service_delete removes the service row and its token rows together — both gone or neither": "passed",
    "§15 — deleting cascades are one atomic D1 batch §15 · the namespace's other services, tokens, and grants are untouched by it (the allow-twin)": "passed",
    "§8 — audit discipline, one row per mutating op §8 · an op refused for the reserved slug writes no `admin.<op>` row — no summary of a change that did not happen": "passed",
    "§8 — audit discipline, one row per mutating op §8 · approval_decide's own row sits beside approvals' `approval.approved` — two writers, one call": "passed",
    "§8 — audit discipline, one row per mutating op §8 · every mutating op writes exactly one `admin.<op>` row, in the namespace it changed": "passed",
    "§8 — audit discipline, one row per mutating op §8 · every read op writes no `admin.*` row (approval_list's lazy `approval.expired` is approvals' write)": "passed",
    "§8 — audit discipline, one row per mutating op §8 · service_disconnect writes `upstream.disconnected` beside its own row": "passed",
    "§8 — audit discipline, one row per mutating op §8 · service_update flipping `auth` writes `upstream.auth_mode_changed` beside its own row": "passed",
    "§8 — audit discipline, one row per mutating op §8 · token_issue's row names kind and referent, never the plaintext key": "passed",
    "§8 — parity direction A: one schema, three fronts §8 · adminBackend.sensitivePaths answers `{ args: [], results: [...] }` for known ops and null for an unknown name": "passed",
    "§8 — parity direction A: one schema, three fronts §8 · every ops key renders as a `pmcp` Tool whose inputSchema is that op's own schema": "passed",
    "§8 — parity direction A: one schema, three fronts §8 · every tool adminBackend lists names an ops key — totality in the other direction": "passed",
    "§8 — parity direction A: one schema, three fronts §8 · token_issue alone declares an outputSchema, and its key field carries `writeOnly`": "passed",
    "§8 — the reserved `pmcp` slug is a property of the table §8 · every slug-taking op refuses `pmcp` with the one uniform error, swept over Object.keys(ops)": "passed",
    "§8 — the reserved `pmcp` slug is a property of the table §8 · grant_set refuses `pmcp` as its service — accounts can never hold admin grants": "passed",
    "§8 — the reserved `pmcp` slug is a property of the table §8 · service_get('pmcp') refuses like every other slug-taking op — the builtin surfaces only through service_list": "passed",
    "§8 — the reserved `pmcp` slug is a property of the table §8 · the row set equals Object.keys(ops) — a new op fails the sweep instead of skipping it": "passed",
    "§8 — the reserved `pmcp` slug is a property of the table §8 · the same op accepts the fixture's real slug — the allow-twin the sweep generates per row": "passed"
  },
  "server/test/worker/admin-pipeline.test.ts": {
    "§7 — the owner is never approval-gated §7 · an owner's mutating `pmcp` call executes with no approval row created and no push sent": "passed",
    "§7 — the owner is never approval-gated §7 · the owner's roles forward literally as [\"all\"], never expanded into declared role names": "passed",
    "§7/§15 — what the caller sees versus what is stored §15 · one admin call writes two rows: the gateway's `tools/call` (with duration_ms) and the handler's `admin.<op>`": "passed",
    "§7/§15 — what the caller sees versus what is stored §15 · the same call's recorded result masks that key by the uniform body rule — no pmcp-specific rule exists (the ledger sweep is hygiene.test.ts's)": "passed",
    "§7/§15 — what the caller sees versus what is stored §7 · a HubError from a handler leaves through the ONE wire mapping — pinned code, no stack, no echo of the input": "passed",
    "§7/§15 — what the caller sees versus what is stored §7 · token_issue's plaintext key is present in the CALLER's reply: masking exists for persistence only": "passed",
    "§8 — service accounts and the builtin, structurally §7 step 2 · an account on scoped /mcp/pmcp gets 404, the same answer as a slug it holds no grants on": "passed",
    "§8 — service accounts and the builtin, structurally §8 · an account calling `pmcp_service_list` gets -32001, indistinguishable from an unknown tool": "passed",
    "§8 — service accounts and the builtin, structurally §8 · an account holding grants elsewhere sees no `pmcp_*` tool in its aggregated list": "passed",
    "§8 — service accounts and the builtin, structurally §8 · no grant can ever name `pmcp`: there is no service id to reference (the op-level refusal is admin-ops.test.ts's)": "passed",
    "§8 — service accounts and the builtin, structurally §8 · the same account's aggregated list does carry its granted service's tools (the allow-twin)": "passed",
    "§8 — service_list's virtual row §8 · service_get('pmcp') is refused here too — the builtin surfaces only through service_list": "passed",
    "§8 — service_list's virtual row §8 · service_list includes `pmcp` flagged `builtin: true` while no `service` row exists for it": "passed",
    "§8 — service_list's virtual row §8 · the real services in the same listing carry their row fields and no builtin flag (the twin)": "passed",
    "§8 — the builtin rides the ordinary pipeline §15 · the builtin's log_bodies is fixed ON: its schemas are the hub's own": "passed",
    "§8 — the builtin rides the ordinary pipeline §7 · a scoped `pmcp` call runs the same filter → archived → approval → availability order as any service": "passed",
    "§8 — the builtin rides the ordinary pipeline §8 · `pmcp` is never archived and never reports a connection status — the virtual row has neither": "passed",
    "§8 — the builtin rides the ordinary pipeline §8 · the owner's aggregated tools/list carries a `pmcp_<op>` tool for every ops key — the endpoint face of parity direction A": "passed",
    "§8 — the builtin rides the ordinary pipeline §8 · the scoped /mcp/pmcp list carries the same tools with bare names, no prefix": "passed"
  },
  "server/test/worker/approvals.test.ts": {
    "§13/§15 · notifying the owner §13 · a 404/410 from the push service prunes that subscription row; any other failure leaves it (a flaky push service must not unsubscribe the owner)": "passed",
    "§13/§15 · notifying the owner §13 · subscribePush upserts on endpoint — the same browser re-subscribing replaces its row, never duplicates it": "passed",
    "§13/§15 · notifying the owner §13 · the push payload decrypts in-test: the VAPID ES256 JWT verifies against the configured key and subject, and the RFC 8291 body decrypts to service, tool and approval id — and carries no arguments, redacted or otherwise": "todo",
    "§13/§15 · notifying the owner §15 · notifyOwner never throws: a push endpoint that rejects, hangs or 500s still leaves the pending row created and the -32003-shaped result returned": "passed",
    "§7 · decide, list, and lazy expiry §7 · a past-expiry pending row reads as expired on every path and is flipped once — a second read writes no second approval.expired": "passed",
    "§7 · decide, list, and lazy expiry §7 · decide flips a pending row and writes its audit row; approving starts nothing — the agent's identical retry is what executes": "passed",
    "§7 · decide, list, and lazy expiry §7 · decide refuses an unknown id, another namespace's row, an already-decided row and a past-expiry row identically · twin: the owner's live pending row decides": "passed",
    "§7 · decide, list, and lazy expiry §7 · list reports post-redaction arguments and applies the same lazy expiry as the gate, newest first": "passed",
    "§7 · decide, list, and lazy expiry §7/§15 · sweepExpired flips only what lazy expiry has not, then prunes past config.retentionDays — read from the injected config, never a literal": "passed",
    "§7 · settle restores, never consumes §7 · a complete result leaves the pass used — the ordinary end of an exchange": "passed",
    "§7 · settle restores, never consumes §7 · an error response leaves the pass used — only input_required restores": "passed",
    "§7 · settle restores, never consumes §7 · an input_required leg restores the pass to approved — one approval covers the whole MRTR exchange": "passed",
    "§7 · settle restores, never consumes §7 · dispatch that produced no response at all skips settle and leaves the pass consumed — at-most-once outranks sparing the owner a re-approval": "passed",
    "§7 · what the pass is bound to §7 · a call differing in a visible argument binds to a different row · twin to the case above": "passed",
    "§7 · what the pass is bound to §7 · absent arguments and {} bind identically — canonicalJson's undefined ≡ {}": "passed",
    "§7 · what the pass is bound to §7 · MRTR inputResponses/requestState enter neither the binding nor the stored arguments · twin: the same call's params.arguments do": "passed",
    "§7 · what the pass is bound to §7 · post-redaction hashing, observed: two calls differing only in a redacted argument match the same row, and the stored arguments show the mask": "passed",
    "§7 step 1 · the claim CAS §7 step 1 · a row that expired between check and claim answers 'lost', never a stale dispatch": "passed",
    "§7 step 1 · the claim CAS §7 step 1 · two claims on one approved row: exactly one ApprovalClaim, the other 'lost' — the changed-row count is the sole authority": "passed",
    "§7 step 1 · the claim CAS §7 step 2 · two concurrent identical first calls: one pending row, one approvalId, one approval.requested, one push — the constraint kills the race": "passed",
    "§7 step 1–2 · check decides without consuming §7 · a past-expiry approved row reads as expired but is never flipped — no approval.expired for a row that was not pending": "passed",
    "§7 step 1–2 · check decides without consuming §7 · a past-expiry pending row is flipped first, then re-requested — approval.expired before approval.requested": "passed",
    "§7 step 1–2 · check decides without consuming §7 · a retry differing in a visible argument is a different binding: its own pending row, id, audit row and push": "passed",
    "§7 step 1–2 · check decides without consuming §7 · a retry differing only in a redacted argument dedups onto the stored pending row — the hash is taken after masking": "passed",
    "§7 step 1–2 · check decides without consuming §7 · a used pass is no pass: the next identical call opens a fresh pending row": "passed",
    "§7 step 1–2 · check decides without consuming §7 · an approved pass answers ok for a retry differing only in a redacted argument — the owner approved the visible arguments": "passed",
    "§7 step 1–2 · check decides without consuming §7 · an approved pass does not cover a retry differing in a visible argument — that call opens its own pending row": "passed",
    "§7 step 1–2 · check decides without consuming §7 · an approved pass on a DIFFERENT service never answers ok for the same tool name — one approval binds one service": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 1 · an approved pass belonging to ANOTHER account never answers ok — the lookup is keyed by the account, not by tool and args": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 1 · an approved unexpired pass answers ok, writing nothing": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 1 · check leaves an approved row claimable — the pass survives a check that never reached claim (the offline-retry case, from this module's side)": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 2 · a pending row belonging to ANOTHER account is not this call's row: its own pending row, id, audit row and push": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 2 · no row at all: one fresh pending row, one approval.requested, one push": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 2 · pending dedup returns same approvalId": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 4 · a rejected row is spent: a fresh pending row, its own approval.requested and push": "passed",
    "§7 step 1–2 · check decides without consuming §7 step 4 · a row already stored `expired` re-requests without a second approval.expired": "passed"
  },
  "server/test/worker/auth-matrix.test.ts": {
    "§12 — the bootstrap route exists only while its secret does §12 · with BOOTSTRAP_SECRET unset, every request 404s — indistinguishable from an unknown path": "passed",
    "§12 — the bootstrap route exists only while its secret does §12 · with the secret set, a wrong secret is refused": "passed",
    "§12 — the bootstrap route exists only while its secret does §12 · with the secret set, the correct secret is admitted (the twin)": "passed",
    "§4/§13 — session-scope guards on /account §13 · a password session without recent auth is forced through a fresh sign-in": "passed",
    "§4/§13 — session-scope guards on /account §4 · a device-flow (bearer-sourced) session replayed as a cookie is sent to /login instead": "passed",
    "§4/§13 — session-scope guards on /account §4 · a password session with recent auth reaches /account (the twin)": "passed",
    "§7 step 1 — carrier and transport hygiene §7 step 1 · a foreign Origin is refused 403 — the one 403 on this surface": "passed",
    "§7 step 1 — carrier and transport hygiene §7 step 1 · a non-JSON Content-Type is refused; `application/json` is admitted": "passed",
    "§7 step 1 — carrier and transport hygiene §7 step 1 · a valid key in the query string is refused; the same key as a bearer is admitted (its twin)": "passed",
    "§7 step 1 — carrier and transport hygiene §7 step 1 · a valid session cookie with no Authorization header is refused on /mcp — cookies are never consulted": "passed",
    "§7 step 1 — carrier and transport hygiene §7 step 1 · an absent Origin is admitted — every legitimate consumer sends none": "passed",
    "§7 step 1 — carrier and transport hygiene §7 step 1 · an Origin equal to the hub's origin is admitted": "passed",
    "§7 step 1 — namespaces never leak existence §7 step 1 · a resolved account on a foreign namespace gets 404, not 403": "passed",
    "§7 step 1 — namespaces never leak existence §7 step 1 · a resolved account on its own namespace is admitted": "passed",
    "§7 step 1 — namespaces never leak existence §7 step 1 · a resolved owner on a foreign namespace gets the same 404": "passed",
    "§7 step 1 — namespaces never leak existence §7 step 1 · an unauthenticated request gets the same 401 whether `<user>` exists or not": "passed",
    "§7 step 1 — namespaces never leak existence §7 step 1 · any resolved principal on a nonexistent username gets 404 — byte-identical to route-not-found": "passed",
    "§7 step 1 — namespaces never leak existence §7 step 2 · a service account on a scoped slug it holds no grants on gets 404 — identical to a nonexistent slug": "passed",
    "§7 step 1 — namespaces never leak existence §7 step 2 · a service account on a scoped slug that does not exist gets the same 404 — the pair's other half, byte-compared against it": "passed",
    "§7 step 1 — namespaces never leak existence §8 · a live service-account key on scoped /mcp/pmcp gets 404 — the same answer as any slug it holds no grants on, never a 401": "passed",
    "§7 step 1 — namespaces never leak existence §8 · the owner reaches scoped /mcp/pmcp — admin (user) tokens are exactly who may": "passed",
    "§7 step 1 — resolution dispatches on prefix §4/§8 · a device-flow (bearer-sourced) session is admitted on /<user>/mcp — the §4 guard limits its SCOPE, it does not reject the session": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · a `pmcp_sa_` key whose account row is gone is refused identically again": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · a bearer matching no prefix and no session is refused": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · a better-auth session token as Bearer resolves to the owner (the user allow-twin)": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · a live `pmcp_sa_` key resolves to its service account (the anchor allow-twin)": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · a live `pmcp_svc_` key is refused and never falls through to session lookup": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · a revoked `pmcp_sa_` key is refused 401 + WWW-Authenticate": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · an expired `pmcp_sa_` key is refused identically to a revoked one — issued at a backdated now() and resolved past its expiry, never slept for": "passed",
    "§7 step 1 — resolution dispatches on prefix §7 step 1 · no Authorization header at all is refused with the same 401": "passed",
    "§8 — /api/whoami mirrors step 1 exactly §8 · a `pmcp_sa_` key resolves to the service-account principal in the owner's namespace": "passed",
    "§8 — /api/whoami mirrors step 1 exactly §8 · a `pmcp_svc_` key is refused 401, never a session lookup": "passed",
    "§8 — /api/whoami mirrors step 1 exactly §8 · a session resolves to the user principal": "passed",
    "§8 — /api/whoami mirrors step 1 exactly §8 · no credential is refused 401 + WWW-Authenticate": "passed",
    "the table's own invariants §7 step 1 · rows pinned as indistinguishable answer byte-identically, not merely with equal status": "passed",
    "the table's own invariants §9 rule 2 · every refusal row names an allow-twin present in this table": "passed"
  },
  "server/test/worker/contracts.test.ts": {
    "§4 · admin op names and schemas §15 · admin-ops.json records token_issue's outputSchema with its key field writeOnly, and no other op declares one — the uniform-body rule's whole footprint": "todo",
    "§4 · admin op names and schemas §8 · admin-ops.json input schemas deep-equal what adminBackend renders per op — one zod schema, one rendering, so the tool and the web form cannot drift": "todo",
    "§4 · admin op names and schemas §8 · admin-ops.json op-name set equals Object.keys(ops), total in both directions — a new op that forgets its fixture fails here": "todo",
    "§4 · audit body stubs — the spelling §15 defers to this directory §15 · `bytes` is pinned as a KEY with a number type and never as a value, per this directory's nothing-that-varies-per-run rule — the fixture stays stable while the cap and the fixture bodies move": "todo",
    "§4 · audit body stubs — the spelling §15 defers to this directory §15 · an under-cap structured body records intact, so the two stub rows are pinned beside the shape that must NOT be stubbed — the allow-twin without which \"stub everything\" satisfies this family": "todo",
    "§4 · audit body stubs — the spelling §15 defers to this directory §15 · audit-body-stubs.json's `blob` row deep-equals the stub a real tools/call records for an unstructured result block: the discriminator, the content type, and `bytes` present as a number — §15 names the stub but not its keys, so this row IS the naming": "todo",
    "§4 · audit body stubs — the spelling §15 defers to this directory §15 · audit-body-stubs.json's `oversize` row deep-equals the stub that replaces a whole over-cap body, driven against a shrunk AUDIT_BODY_CAP_BYTES rather than a megabyte fixture, and carrying no fragment of the body it replaced — refusal, twinned with the row below": "todo",
    "§4 · audit body stubs — the spelling §15 defers to this directory §15 · CONSUMER NOTE — hygiene.test.ts transcribes this fixture into BodyColumnShape's `stubbed` and `oversize` members (its rows spell stubs as `Omit<BodyStub, \"bytes\">`), and it is the family's only reader today. A disagreement between that type and this fixture is the drift this row exists to catch; hygiene.test.ts asserts BEHAVIOR against the shape, this file asserts the shape itself, and neither may respell the other's half.": "todo",
    "§4 · audit body stubs — the spelling §15 defers to this directory §15 · the fixture's stub kinds are exactly the two the spec allows, total in both directions against audit.ts's BodyStub union — a third stub cannot enter the recorder without entering the fixture": "todo",
    "§4 · bootstrap §12 · bootstrap.json request rows deep-equal scripts' BootstrapRequest for all four ops": "todo",
    "§4 · bootstrap §12 · bootstrap.json response rows deep-equal the live route's bodies, with `password` present on create and reset-password and absent from list and delete — allow": "todo",
    "§4 · bootstrap §12 · bootstrap.json's disabled row pins the 404 the route returns while BOOTSTRAP_SECRET is unset, beside its wrong-secret 401 twin — refusal, twinned with the row above": "todo",
    "§4 · error vocabulary §10 · cli HUB_ERRORS names map onto errors.json codes, total in both directions": "todo",
    "§4 · error vocabulary §7 · a granted call in the same seeded namespace returns a result and no error member — the allow-twin of the -32001 and -32000 rows": "todo",
    "§4 · error vocabulary §7 · errors.json -32000 entry carries no data key, and no upstream status, header, or body fragment appears anywhere in it — refusal, twinned with the row above": "todo",
    "§4 · error vocabulary §7 · errors.json -32001 entry is ONE shape for both causes: an ungranted tool and an unknown tool are byte-identical — refusal": "todo",
    "§4 · error vocabulary §7 · errors.json -32003 entry's data keys deep-equal a real approval-required error's data — approvalId, approvalUrl, expiresAt, nothing else": "todo",
    "§4 · error vocabulary §7 · errors.json code set equals the five codes the pipeline emits (-32000/-32001/-32002/-32003/-32601) and admits no sixth": "todo",
    "§4 · planner-facing rows §8 · account-list.json rows carry grants inline, so the planner's entire current-state read is these two families and nothing else": "todo",
    "§4 · planner-facing rows §8 · service-list.json row keys equal a live service_list row's, for tunnel, proxy, and the builtin pmcp entry (builtin: true, no D1 row behind it)": "todo",
    "§4 · planner-facing rows §9 · plan.CurrentService's keys equal service-list.json's minus exactly the runtime facts (status, oauth connection state, last seen) — a plan can never turn on status, and the two rows sit side by side so the omission is visible": "todo",
    "§4 · tunnel frames and close codes §6 · close-codes.json covers every code in the 4000–4004 vocabulary and the upgrade statuses, each with exactly one required client behavior": "todo",
    "§4 · tunnel frames and close codes §6 · close-codes.json's 4001 and 4002 entries equal the exported CLOSE_REVOKED and CLOSE_ARCHIVED, and the SeverCode union admits exactly those two": "todo",
    "§4 · tunnel frames and close codes §6 · close-codes.json's other three entries equal the exported CLOSE_REPLACED, CLOSE_ROW_GONE and CLOSE_PROTOCOL — the fixture is emitted from the vocabulary, so a renumbering cannot reach the wire without reaching the fixture": "todo",
    "§4 · tunnel frames and close codes §6 · every entry carries a `schedule` attribute — exponential or max_only — and only where it means something: a reconnect entry names its schedule, a stopping entry names none, so \"retry at max backoff\" is a schedule of reconnect and never a behavior of its own": "todo",
    "§4 · tunnel frames and close codes §6 · every stop_fatal entry (close 4001, upgrade 401) sits beside a keep-connecting twin (close 4002 reconnect at max_only, upgrade 403 the same, close 4000 stop_quiet) — no deny-only close-code table": "todo",
    "§4 · tunnel frames and close codes §6 · the fixture's behavior vocabulary is exactly three words — stop_fatal, stop_quiet, reconnect — and admits no fourth: the ONE spelling both client reconnect tables transcribe": "todo",
    "§4 · tunnel frames and close codes §6 · tunnel-frames.json hub/register ack and hub/replaced notification deep-equal the frames the DO emits, and their method names are the values of the exported HUB_METHODS — the frames' agreement with a live socket is tunnel/protocol.test.ts's lock, not this file's": "todo",
    "§4 · tunnel frames and close codes §7 · tunnel-frames.json's forwarded-call _meta names are exactly hub/principal, hub/roles, and the mirrored clientCapabilities key — no other hub/* name exists": "todo",
    "§4 · whoami — the CLI↔server contract §8 · cli WhoamiResponse's key set equals the fixture's, both directions — the copy is pinned on the consumer side too, not just the emitting side": "todo",
    "§4 · whoami — the CLI↔server contract §8 · whoami.json \"service_account\" row deep-equals GET /api/whoami under a live pmcp_sa_ key": "todo",
    "§4 · whoami — the CLI↔server contract §8 · whoami.json \"unauthorized\" row deep-equals the 401 body and WWW-Authenticate header for a pmcp_svc_ bearer — refusal, twinned with the two rows above": "todo",
    "§4 · whoami — the CLI↔server contract §8 · whoami.json \"user\" row deep-equals GET /api/whoami under a device-flow session token": "todo",
    "§4 direction C · planner steps → ops §4 · every emitted step's args cover its op schema's required fields": "todo",
    "§4 direction C · planner steps → ops §4 · every PlanStep.tool the planner can emit is a key of ops": "todo",
    "§4 direction D · CLI subcommands → ops §4 · every non-auth CLI subcommand maps to an ops key": "todo",
    "§4 direction D · CLI subcommands → ops §4 · every ops key is reachable from some CLI subcommand — the reverse direction, so an op nobody can run fails here": "todo",
    "§4 direction D · CLI subcommands → ops §8 · the pinned parity exceptions (auth/credential family, the OAuth consent redirect, the JSONL export) are the ONLY unmapped names, listed explicitly rather than skipped": "todo",
    "§9 · fixture governance §9 · every contracts/*.json on disk is claimed by exactly one CONTRACT_FAMILIES row — an orphan fixture nobody produces fails here": "todo",
    "§9 · fixture governance §9 · no fixture contains a sentinel secret, a pmcp_ token prefix, or a value that varies between two runs of this suite": "todo"
  },
  "server/test/worker/cron.test.ts": {
    "§15 · one run, all three effects 3. §15 · a single scheduled run flips past-expiry pending approvals, prunes past-retention audit and approval rows, and drops stale upstream-OAuth state — all from one invocation": "todo",
    "§15 · one run, all three effects 4. §7 · each flip writes exactly one approval.expired row · a second run over the same rows writes none (the exactly-once law, one case, two runs)": "todo",
    "§15 · one run, all three effects 5. §7 · the expiry sweep precedes the prune — a pending row past BOTH windows is audited expired before it is deleted, never silently dropped": "todo",
    "§15 · one run, all three effects 6. §15 · a run over an empty namespace still completes and still writes its cron.swept row (the allow-twin of every \"N rows were touched\" case)": "todo",
    "§15 · the cron leg table 1. §15 · <row.spec> · <row.title> — one case per CRON_LEG_ROWS row via runCronLegRow": "todo",
    "§15 · the cron leg table 2. §15 · every leg of the composition root's named list appears in at least one row — driven over the leg list itself, so a fourth leg cannot be added without a row": "todo",
    "§15 · the run is observable 10. §15 · the cron.swept row is itself queryable through audit_query — /audit is the cron's only monitoring, so an unqueryable row is no monitoring at all": "todo",
    "§15 · the run is observable 7. §15 · every run writes exactly one cron.swept audit row, and it names every leg": "todo",
    "§15 · the run is observable 8. §15 · a leg that throws never starves its siblings: the other two effects are observable and the swept row reports that leg failed (legOutcome ok:false)": "todo",
    "§15 · the run is observable 9. §15 · scheduled() resolves even when every leg rejects — the cron never throws into the platform, because a thrown cron is an invisible cron": "todo",
    "§15 · the schedule itself 13. §15 · the wrangler cron expression equals the expected daily constant — HONESTLY LABELLED: this asserts configuration only. Nothing in-process can prove the platform fires it; production answers that through the cron.swept rows (§10)": "todo",
    "§15 · the windows are configuration, not literals 11. §15 · retention comes from AUDIT_RETENTION_DAYS when the var is set and from limits.RETENTION_DAYS when it is not — parsed once at the composition root, so both runs differ only in what survives": "todo",
    "§15 · the windows are configuration, not literals 12. §15 · request paths never prune: a tools/call made with past-retention rows present deletes nothing · the scheduled run that follows deletes them (the twin — proving the first assertion is about restraint, not about absent rows)": "todo"
  },
  "server/test/worker/hygiene.test.ts": {
    "§15 · log_bodies defaults by kind and flips both ways 3. §15 · tunneled create with log_bodies absent stores it ON": "todo",
    "§15 · log_bodies defaults by kind and flips both ways 4. §15 · proxied create with log_bodies absent stores it OFF": "todo",
    "§15 · log_bodies defaults by kind and flips both ways 5. §15 · log_bodies flipped OFF on a tunneled service · its tools/call records no bodies": "todo",
    "§15 · log_bodies defaults by kind and flips both ways 6. §15 · log_bodies flipped ON for a proxied service · its tools/call records both bodies (the allow-twin of 5 — the flip is proven in both directions, not just off)": "todo",
    "§15 · log_bodies defaults by kind and flips both ways 7. §15 · the builtin pmcp service records bodies with no row to configure (logBodies fixed ON)": "todo",
    "§15 · the audit body table 1. §15 · <row.spec> · <row.title> — one case per AUDIT_BODY_ROWS row via runAuditBodyRow": "todo",
    "§15 · the audit body table 2. §15 · the table covers both kinds in both log_bodies states (structural: the row set spans {tunnel, proxy} × {default, flipped}, so a new default can't go untested)": "todo",
    "§15 · the sweep 23. §15 · after the full exercise no column of any table holds token material or a sentinel secret (sweepForSentinels, with its control value found — an unreachable sweep fails)": "todo",
    "§15 · the sweep 24. §15 · bodies live only in approval.args_json and the audit body columns · the visible argument IS found in exactly those columns and in no other (the twin that makes 23 non-vacuous)": "todo",
    "§15 · what may reach the two body columns 10. §15 · unstructured result blocks persist as blob stubs · the structuredContent beside them persists masked (never \"all or nothing\")": "todo",
    "§15 · what may reach the two body columns 11. §15 · a body over the cap in force is replaced WHOLE by one oversize stub · an under-cap body of the same shape persists intact": "todo",
    "§15 · what may reach the two body columns 12. §15 · an over-cap column still parses as JSON — replaced, never truncated": "todo",
    "§15 · what may reach the two body columns 13. §7 · MRTR inputResponses/requestState never enter args_json while the same leg's params.arguments do": "todo",
    "§15 · what may reach the two body columns 14. §15 · tools/list writes no audit row at all · the tools/call beside it writes exactly one": "todo",
    "§15 · what may reach the two body columns 14a. §7 · a call on a SCHEMA-UNSOUND tool — one whose schema tripped §7's indirection refuse-line at catalog warm, so it has no derivable redaction map and sensitivePaths answers null — records no body in either column · the same call on the same tool with a walkable schema records both, masked (the allow-twin, and the reason \"no redaction map\" can never quietly degrade into \"record it raw\"). Which schemas are refused is unit/redact.test.ts's; that the warm stays loud and registration survives is tunnel/protocol.test.ts's; this row owns only what reaches D1.": "todo",
    "§15 · what may reach the two body columns 8. §7 · args recorded post-redaction: every union path masked, every sibling path verbatim": "todo",
    "§15 · what may reach the two body columns 9. §7 · result structuredContent recorded post-redaction under the results union (schema writeOnly ∪ config redact_results) · a non-marked field beside it verbatim": "todo",
    "§7 · redaction precedes hashing 20. §7 · the stored args_hash equals SHA-256(canonicalJson(applyRedaction(args, union))), recomputed in-test from the raw arguments": "todo",
    "§7 · redaction precedes hashing 21. §7 · the stored args_hash does NOT equal the hash of the raw arguments — the proof that the order is redact-then-hash and not the reverse": "todo",
    "§7 · redaction precedes hashing 22. §7 · two calls differing only in a redacted field share one approval row · two differing in a visible field never do": "todo",
    "§7 · served outputSchemas carry no writeOnly 17. §7 · scoped tools/list strips writeOnly from every outputSchema · leaves inputSchema writeOnly intact (the input keyword is standard usage)": "todo",
    "§7 · served outputSchemas carry no writeOnly 18. §7 · aggregated tools/list strips it identically — one strip, both shapes": "todo",
    "§7 · served outputSchemas carry no writeOnly 19. §7 · stripping the served copy does not disarm redaction: the same tool's result is still masked at the marked path": "todo",
    "§8/§15 · the uniform rule needs no pmcp special case 15. §15 · token_issue's recorded result masks the key by the writeOnly rule · the row id and display prefix in the same result are recorded verbatim": "todo",
    "§8/§15 · the uniform rule needs no pmcp special case 16. §7 · the CALLER receives the plaintext key unredacted while the recorded body is masked (masking exists for persistence, never for the response)": "todo"
  },
  "server/test/worker/identity-tokens.test.ts": {
    "§5 · minting and plaintext-once §5 · an explicit expiresIn and an explicit 'never' each override the per-kind default, in both directions": "passed",
    "§5 · minting and plaintext-once §5 · expiry defaults by kind — a service_account token carries an expiry, a service token carries none (the bot on a home server must not silently die). The window's value is incidental (§7); its presence is not": "passed",
    "§5 · minting and plaintext-once §5 · issueToken returns the plaintext once; listTokens shows the prefix and no surface returns the secret again": "passed",
    "§5 · minting and plaintext-once §5 · the issued token authenticates on its own credential surface · twin to the row above: sealed, not lost": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · a better-auth session token in the bearer resolves null — /connect never falls through to session lookup": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · a live pmcp_svc_ token resolves to its bound service id — the allow-twin every refusal in this table is measured against": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · a pmcp_sa_ bearer resolves null — a service-account credential means nothing on /connect": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · a revoked token resolves null": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · an expired token resolves null — expiry is judged at the upgrade": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · an unknown pmcp_svc_ secret resolves null": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · kind is read from the column, never inferred from the prefix: a pmcp_svc_ secret stored as kind 'service_account' resolves null": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · no Authorization header at all resolves null": "passed",
    "§6 · resolveServiceToken: one null for every failure §6 · the union is exhausted: every ServiceTokenDefect has a row, and every refusing row answers the identical value": "passed",
    "§6 · resolveServiceToken: one null for every failure §6/§7 · the valid secret in a query string resolves null — the credential is the Authorization header or nothing": "passed",
    "§7 · listing and use §7 · a successful resolve stamps last_used_at and a refused one stamps nothing — the stamping cadence itself is incidental (§7) and is not asserted": "passed",
    "§7 · listing and use §7 · listTokens spans live, expired and revoked rows in one namespace, and never shows a token whose referent row is gone": "passed",
    "§8 · revoke versus delete §5 · deleteTokensFor is keyed by opaque id: recreating a deleted service's slug resurrects no credential": "passed",
    "§8 · revoke versus delete §6 · a revoked token still appears in listTokens with revokedAt stamped — rotation state is what the listing is for · twin: the live token lists too": "passed",
    "§8 · revoke versus delete §8 · a revoked token is refused immediately on the next resolve · twin: the same token resolved before the revoke": "passed",
    "§8 · revoke versus delete §8 · deleteTokensFor over zero matching rows succeeds": "passed",
    "§8 · revoke versus delete §8 · deleteTokensFor removes the rows from the listing entirely — deletion, not revocation, is what the service_delete cascade does": "passed",
    "§8 · revoke versus delete §8 · revokeToken answers false identically for an unknown id and for another namespace's token — one uniform not-found, so the op layer cannot leak existence": "passed",
    "§8 · revoke versus delete §8 · revokeToken is idempotent — revoking a revoked token succeeds and changes nothing": "passed"
  },
  "server/test/worker/migrations.test.ts": {
    "§10 · applying the set §10 · every migration applies to an empty database in order (the fresh-install path)": "passed",
    "§10 · applying the set §10 · forward migration over live data: 1..N−1 applied, rows inserted, N applied — N does not fail on existing rows and the inserted rows survive it": "passed",
    "§10 · applying the set §10 · re-application is a no-op: a second run applies nothing and leaves the schema identical": "passed",
    "§5 · cascades \"§5 · foreign keys are enforced, not decorative: a child row naming an absent parent is refused\" (the pragma-off failure mode looks identical in the DDL)": "passed",
    "§5 · cascades §5 · deleting the user cascades service, service_account, grant_, approval and push_subscription · token and audit rows survive": "passed",
    "§5 · cascades §5 · token rows survive every parent delete — ref_id has no FK, so deletion stays admin's cascade where it is audited": "passed",
    "§5 · cascades §5/§8 · deleting a service cascades its grant_ and approval rows · its token rows survive — ref_id has no FK, so deletion stays admin's cascade": "passed",
    "§5 · cascades §5/§8 · deleting a service_account cascades its grant_ and approval rows · its token rows survive, and the service it was granted on is untouched": "passed",
    "§5 · constraints bite \"§5 · the table is exhaustive: every CHECK, UNIQUE and NOT NULL in the applied schema appears in schemaConstraintRows\" — coverage derived from the migration SQL, so a constraint added without a row fails here instead of going unpinned": "passed",
    "§5 · constraints bite §5 · approval.args_hash NOT NULL refuses null · twin stores a hash": "passed",
    "§5 · constraints bite §5 · approval.args_json NOT NULL refuses null — the shown arguments are never absent · twin stores '{}'": "passed",
    "§5 · constraints bite §5 · approval.created_at NOT NULL refuses null · twin stores a timestamp": "passed",
    "§5 · constraints bite §5 · approval.id PRIMARY KEY refuses a duplicate id · twin stores a distinct id": "passed",
    "§5 · constraints bite §5 · approval.owner_id FK refuses an absent user · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · approval.owner_id NOT NULL refuses null · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · approval.service_account_id FK refuses an absent account · twin stores the seeded account": "passed",
    "§5 · constraints bite §5 · approval.service_account_id NOT NULL refuses null · twin stores the seeded account": "passed",
    "§5 · constraints bite §5 · approval.service_id FK refuses an absent service · twin stores the seeded service": "passed",
    "§5 · constraints bite §5 · approval.service_id NOT NULL refuses null · twin stores the seeded service": "passed",
    "§5 · constraints bite §5 · approval.status CHECK refuses 'cancelled' · twin stores 'used'": "passed",
    "§5 · constraints bite §5 · approval.status NOT NULL refuses null · twin stores 'pending'": "passed",
    "§5 · constraints bite §5 · approval.tool NOT NULL refuses null · twin stores 'get_news'": "passed",
    "§5 · constraints bite §5 · audit.event NOT NULL refuses null · twin stores 'tools/call'": "passed",
    "§5 · constraints bite §5 · audit.id PRIMARY KEY refuses a duplicate id · twin stores a distinct id": "passed",
    "§5 · constraints bite §5 · audit.outcome NOT NULL refuses null · twin stores '-32001'": "passed",
    "§5 · constraints bite §5 · audit.principal NOT NULL refuses null · twin stores 'bootstrap'": "passed",
    "§5 · constraints bite §5 · audit.ts NOT NULL refuses null · twin stores a timestamp": "passed",
    "§5 · constraints bite §5 · grant_ PRIMARY KEY (service_account_id, service_id, role) refuses the same role twice · twin stores a second role": "passed",
    "§5 · constraints bite §5 · grant_.mode CHECK refuses 'deny' — deny is a resolver answer, never a stored grant · twin stores 'approval'": "passed",
    "§5 · constraints bite §5 · grant_.mode NOT NULL refuses null · twin stores 'allow'": "passed",
    "§5 · constraints bite §5 · grant_.role NOT NULL refuses null · twin stores 'reader'": "passed",
    "§5 · constraints bite §5 · grant_.service_account_id FK refuses an absent account · twin stores the seeded account": "passed",
    "§5 · constraints bite §5 · grant_.service_account_id NOT NULL refuses null · twin stores the seeded account": "passed",
    "§5 · constraints bite §5 · grant_.service_id FK refuses an absent service · twin stores the seeded service": "passed",
    "§5 · constraints bite §5 · grant_.service_id NOT NULL refuses null · twin stores the seeded service": "passed",
    "§5 · constraints bite §5 · push_subscription.created_at NOT NULL refuses null · twin stores a timestamp": "passed",
    "§5 · constraints bite §5 · push_subscription.endpoint NOT NULL refuses null · twin stores an endpoint": "passed",
    "§5 · constraints bite §5 · push_subscription.endpoint UNIQUE refuses a second row for one endpoint · twin stores a different endpoint": "passed",
    "§5 · constraints bite §5 · push_subscription.id PRIMARY KEY refuses a duplicate id · twin stores a distinct id": "passed",
    "§5 · constraints bite §5 · push_subscription.keys_json NOT NULL refuses null · twin stores the browser's key pair": "passed",
    "§5 · constraints bite §5 · push_subscription.user_id FK refuses an absent user · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · push_subscription.user_id NOT NULL refuses null · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · service UNIQUE (owner_id, slug) refuses a second 'twice-over' for one owner · twin stores 'twice-over-2'": "passed",
    "§5 · constraints bite §5 · service_account UNIQUE (owner_id, slug) refuses a second 'twice-over' for one owner · twin stores 'twice-over-2'": "passed",
    "§5 · constraints bite §5 · service_account.created_at NOT NULL refuses null · twin stores a timestamp": "passed",
    "§5 · constraints bite §5 · service_account.id PRIMARY KEY refuses a duplicate id · twin stores a distinct id": "passed",
    "§5 · constraints bite §5 · service_account.name NOT NULL refuses null · twin stores a name": "passed",
    "§5 · constraints bite §5 · service_account.owner_id FK refuses an absent user · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · service_account.owner_id NOT NULL refuses null · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · service_account.slug NOT NULL refuses null · twin stores 'account-twin'": "passed",
    "§5 · constraints bite §5 · service.created_at NOT NULL refuses null · twin stores a timestamp": "passed",
    "§5 · constraints bite §5 · service.forward_identity NOT NULL refuses null · twin stores 0": "passed",
    "§5 · constraints bite §5 · service.id PRIMARY KEY refuses a duplicate id · twin stores a distinct id": "passed",
    "§5 · constraints bite §5 · service.kind CHECK refuses 'websocket' · twin stores 'proxy'": "passed",
    "§5 · constraints bite §5 · service.kind NOT NULL refuses null · twin stores 'tunnel'": "passed",
    "§5 · constraints bite §5 · service.name NOT NULL refuses null · twin stores a name": "passed",
    "§5 · constraints bite §5 · service.owner_id FK refuses an absent user · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · service.owner_id NOT NULL refuses null · twin stores under the seeded owner": "passed",
    "§5 · constraints bite §5 · service.redact_json NOT NULL refuses null · twin stores '{}'": "passed",
    "§5 · constraints bite §5 · service.redact_results_json NOT NULL refuses null · twin stores '{}'": "passed",
    "§5 · constraints bite §5 · service.roles_json NOT NULL refuses null · twin stores '{}'": "passed",
    "§5 · constraints bite §5 · service.slug NOT NULL refuses null · twin stores 'slug-twin'": "passed",
    "§5 · constraints bite §5 · service.upstream_auth_mode CHECK refuses 'basic' · twin stores 'oauth'": "passed",
    "§5 · constraints bite §5 · token.created_at NOT NULL refuses null · twin stores a timestamp": "passed",
    "§5 · constraints bite §5 · token.hash NOT NULL refuses null · twin stores a hash": "passed",
    "§5 · constraints bite §5 · token.hash UNIQUE refuses a second row with the same hash · twin stores a different hash": "passed",
    "§5 · constraints bite §5 · token.id PRIMARY KEY refuses a duplicate id · twin stores a distinct id": "passed",
    "§5 · constraints bite §5 · token.kind CHECK refuses 'user' — session tokens are better-auth's, never rows here · twin stores 'service_account'": "passed",
    "§5 · constraints bite §5 · token.kind NOT NULL refuses null · twin stores 'service'": "passed",
    "§5 · constraints bite §5 · token.prefix NOT NULL refuses null — the listing has nothing to show without it · twin stores a prefix": "passed",
    "§5 · constraints bite §5 · token.ref_id NOT NULL refuses null · twin stores an id no row carries — ref_id points at two tables and so carries no FK": "passed",
    "§5 · constraints bite §5/§15 · audit.owner_id NOT NULL refuses null · twin stores a namespace id no user row carries — audit carries no FK, so the record outlives the namespace": "passed",
    "§5 · constraints bite §5/§15 · service.log_bodies NOT NULL refuses an explicit null · twin stores 1": "passed",
    "§5 · constraints bite §5/§7 · approval.expires_at NOT NULL refuses null — lazy expiry has nothing to interpret without it · twin stores a timestamp": "passed",
    "§5 · constraints bite §7 step 2 · approval UNIQUE (service_account_id, service_id, tool, args_hash) WHERE status='pending' refuses the second pending row for one binding · twin stores a different args_hash": "passed",
    "§5 · constraints bite §7 step 2 · the same pending binding under a DIFFERENT tool opens its own row — `tool` is a key column of the index, so one approval can never answer for another tool": "passed",
    "§7 step 2 · the pending partial unique index §7 step 2 · same (account, service, tool) with a different args_hash opens a second pending row — the binding, not the tool, is what dedups": "passed",
    "§7 step 2 · the pending partial unique index §7 step 2 · the index is partial: the same binding inserts freely once the first row leaves `pending` — total over the ApprovalStatus vocabulary": "passed",
    "§7 step 2 · the pending partial unique index §7 step 2 · two identical pending bindings: the second insert is refused by the constraint, so the losing first call re-reads the winner's row": "passed"
  },
  "server/test/worker/order.table.test.ts": {
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 · an owner is never routed into the gate at all — [\"all\"] resolves allow, whatever the grants say": "passed",
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 · approval-mode, approved pass, available service → dispatch, pass consumed exactly once": "passed",
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 · approval-mode, approved pass, known-offline service → -32000 with the pass NOT consumed": "passed",
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 · approval-mode, no pass, available service → -32003 carrying approvalId + approvalUrl + expiresAt": "passed",
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 · approval-mode, no pass, known-offline service → -32000: no pending row, no push, nothing read": "passed",
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 step 2 · approval-mode, pending pass, retried → -32003 with the same approvalId and no new row": "passed",
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 step 4 · approval-mode, expired pass → -32003 with a fresh pending row": "passed",
    "§7 — availability-first inside the approval gate (decided 2026-08-25) §7 step 4 · approval-mode, rejected pass → -32003 with a fresh pending row": "passed",
    "§7 — name splitting and methods §7 · `server/discover` is answered by the hub on both endpoint shapes, no service resolved": "passed",
    "§7 — name splitting and methods §7 · `server/discover` is answered by the hub on the aggregated shape too — the other half of the pair": "passed",
    "§7 — name splitting and methods §7 · an aggregated name splits at the FIRST `_` — a tool whose own name contains `_` survives intact": "passed",
    "§7 — name splitting and methods §7 · an aggregated name with no `_` at all → -32001, indistinguishable from not-permitted": "passed",
    "§7 — name splitting and methods §7 · an aggregated prefix matching no visible service → the same -32001": "passed",
    "§7 — name splitting and methods §7 · any other method → -32601": "passed",
    "§7 — name splitting and methods §7 · any other method → -32601 on the scoped shape too, before the slug is ever resolved": "passed",
    "§7 — name splitting and methods §7 · the scoped endpoint takes the bare name and binds the same approval row as the prefixed call": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 2 · granted-undeclared role · the scoped tools/list is EMPTY and succeeds — never a 404, never a refusal code": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · a scoped tools/list against an archived service → -32002, like every other request to it": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · granted-allow + archived → -32002 (archived runs before the approval gate)": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · granted-allow + archived + known-offline → -32002, never -32000 (archived runs before availability)": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · granted-allow + available → dispatch (the anchor allow-twin of the whole table)": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · granted-approval + archived → -32002 with no pending row (same ordering, gate never entered)": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · granted-undeclared role → -32001, and an empty tools/list, not a 404": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · ungranted + archived → -32001, never -32002 (filter runs before archived)": "passed",
    "§7 step 3 — the fixed order, filter first §7 step 3 · ungranted + known-offline → -32001 (filter runs before availability)": "passed",
    "the table's own invariants §7 · the three -32001 sources answer identically to one another; `data` is present only on -32003": "passed",
    "the table's own invariants §9 rule 2 · every refusal row names an allow-twin present in this table": "passed",
    "the table's own invariants §9 rule 3 · every row names the stage that produced its answer — swapping two stages fails naming the row": "passed"
  },
  "server/test/worker/registry.test.ts": {
    "§5 · slugs and identity §2 · account slug charset refuses the underscore in `cron_bot` · twin `cron-bot` creates": "passed",
    "§5 · slugs and identity §2 · service slug charset refuses the dot in `news.feed` · twin `newsfeed` creates": "passed",
    "§5 · slugs and identity §2 · service slug charset refuses the empty slug · twin `n` creates": "passed",
    "§5 · slugs and identity §2 · service slug charset refuses the underscore in `news_feed` — §7's first-`_` split depends on it · twin `news-feed` creates": "passed",
    "§5 · slugs and identity §2 · service slug charset refuses the uppercase `News` · twin `news` creates": "passed",
    "§5 · slugs and identity §5 · a second account on the same slug is refused for one owner · twin `dup-account-2` creates": "passed",
    "§5 · slugs and identity §5 · a second service on the same slug is refused for one owner · twin `twice-created-2` creates": "passed",
    "§5 · slugs and identity §5 · createService mints an opaque id: deleting a slug and recreating it yields a different id, so no recreated service can be rebound to a stale DO": "passed",
    "§5 · slugs and identity §5/§8 · the reserved slug is virtual in both directions — createService refuses `pmcp` and getService answers null for it · twin: any other slug creates and reads back": "passed",
    "§5 · slugs and identity §8 · service slug `pmcp` is reserved — the builtin is virtual and no row may shadow it · twin `pmcp-tools` creates": "passed",
    "§5/§9 · create and update invariants §15 · log_bodies resolves at create from the kind when the draft omits it — tunnel on, proxy off — and an explicit value overrides either way": "passed",
    "§5/§9 · create and update invariants §15 · updateService flips log_bodies in both directions on either kind, changing nothing else about the row": "passed",
    "§5/§9 · create and update invariants §2 · allow beats approval when two granted roles match the same tool — the stored pair resolves allow": "passed",
    "§5/§9 · create and update invariants §2 · an approval-mode grant stores as approval · resolveAccess answers approval on a pattern-matched tool": "passed",
    "§5/§9 · create and update invariants §2 · the built-in `all` is grantable without ever being declared — it resolves every tool against an empty declaration": "passed",
    "§5/§9 · create and update invariants §5 · kind/field mismatch: a proxy draft without upstreamUrl and a tunnel draft carrying a declaration are both refused · twins: the well-formed draft of each kind creates": "passed",
    "§5/§9 · create and update invariants §5 · the same role in both modes is refused — one grant row per (account, service, role), so a mode collision stores nothing": "passed",
    "§5/§9 · create and update invariants §7 · flipping upstreamAuthMode clears the credential envelope in the same write — connectionStatus reads not_connected immediately after, so no read can observe a mode and an envelope kind disagreeing": "passed",
    "§5/§9 · create and update invariants §8 · an empty entry list is a legal grant set — it stores nothing and the account resolves deny": "passed",
    "§5/§9 · create and update invariants §9 · a declared role on a proxied service stores with no warnings · resolveAccess answers allow on a matched tool": "passed",
    "§5/§9 · create and update invariants §9 · an undeclared role on a PROXIED service is a hard error — its declaration is complete by construction, so nothing stores": "passed",
    "§5/§9 · create and update invariants §9 · an undeclared role on a TUNNELED service warns and stores — the file may be ahead of the first connection · the grant resolves to no tools until the service declares": "passed",
    "§5/§9 · create and update invariants §9 · the warned set still stores whole — a declared role granted beside an undeclared one keeps its tools": "passed",
    "§5/§9 · create and update invariants §9/§2 · the built-in `all` is grantable on a PROXIED service too — the exemption survives the undeclared-role hard error its neighbour states": "passed",
    "§6 · archived is a pipeline stage, not a filter §6 · archive and unarchive are idempotent and preserve roles, grants and redaction config across the round trip": "passed",
    "§6 · archived is a pipeline stage, not a filter §6 · getService returns an archived row — the -32002 answer needs the row that a filtering lookup would have hidden": "passed",
    "§6 · archived is a pipeline stage, not a filter §6 · listServicesFor keeps archived rows for the owner, and an account still sees only services it holds a grant on": "passed",
    "§6 · declaration drift §6 · a subset re-declaration is not drift, and an added pattern string is, even when the regex language is unchanged (comparison is textual by design)": "passed",
    "§6 · declaration drift §6 · a successful registration stamps last_connected_at — the one moment a tunnel comes online": "passed",
    "§6 · declaration drift §6 · drift is reported only for roles holding a live grant — widening a role nobody was granted is silent": "passed",
    "§6 · declaration drift §6 · upsertDeclaredRoles refuses a proxied service, refuses an invalid declaration without partially writing, and throws on a row that vanished (the caller's close-4003 signal) · twin: a valid declaration on a live tunneled row stores and reports no drift": "passed",
    "§7 · config-declared redaction paths §7 · redactPathsFor keeps the directions apart: `redact` answers 'args' only and `redact_results` answers 'results' only, on the same tool": "passed",
    "§7 · config-declared redaction paths §7 · redactPathsFor unions every matching key (literal and pattern alike) and answers [] — never an error — for a tool nothing matches": "passed",
    "§7 step 2 · resolution at request time §7 · an owner resolves to roleNames ['all'] on every service in the namespace": "passed",
    "§7 step 2 · resolution at request time §7 step 2 · a granted role absent from the declaration stays in roleNames and matches nothing (empty listing, not an absent grant)": "passed",
    "§7 step 2 · resolution at request time §7 step 2 · a role widened by upsertDeclaredRoles takes effect on the very next resolveAccess — the declaration is re-read, never cached across calls": "passed",
    "§7 step 2 · resolution at request time §7 step 2 · a zero-grant account resolves to empty roleNames — the gateway's scoped-404 signal, distinct from the row above · twin: one grant makes it non-empty": "passed"
  },
  "server/test/worker/routes.test.ts": {
    "§2 · the router walk ≡ the reserved usernames 1. §2 · every segment the running worker serves is in RESERVED_ROUTES — walked, not listed": "todo",
    "§2 · the router walk ≡ the reserved usernames 2. §2 · every RESERVED_ROUTES entry except \"mcp\" is served by the running worker (the other direction: a reservation with no mount is drift too, just the harmless-looking kind)": "todo",
    "§2 · the router walk ≡ the reserved usernames 3. §2 · \"mcp\" is reserved by fiat though served only as a second segment — the one member of the set the walk cannot produce, named here so the asymmetry is deliberate": "todo",
    "§2 · the router walk ≡ the reserved usernames 4. §2 · the walk can tell served from unserved: a control name that is not a route classifies \"fallthrough\", never \"served\" (without this, case 1 passes vacuously)": "todo",
    "§2 · the router walk ≡ the reserved usernames 5. §12 · with BOOTSTRAP_SECRET unset, /internal is indistinguishable from an unknown path — so the walk runs with it SET; this case pins both halves and the reason": "todo",
    "§2 · the router walk ≡ the reserved usernames 6. §2 · RESERVED_ROUTES is ROUTES ∪ {\"mcp\"} — derived, so adding a route reserves its name with no second edit anywhere": "todo",
    "§2 · usernames may not collide with routes 7. §12 · provisioning a user named for a reserved segment is refused · the same request with a non-reserved name of the same charset succeeds (the twin)": "todo",
    "§2 · usernames may not collide with routes 8. §2 · a username outside [a-z0-9-] is refused at the route, before any namespace lookup": "todo",
    "§2 · usernames may not collide with routes 9. §2 · manifest.webmanifest and sw.js are reserved even though the dot already puts them outside the username charset — belt and braces, and the case says so": "todo",
    "§2/§7 · what the fallthrough serves 10. §7 · /<reserved>/mcp never reaches the gateway — the mounted group owns the segment, so a user could not shadow a page even if the reservation were missed": "todo",
    "§2/§7 · what the fallthrough serves 11. §2 · an unmatched top-level segment falls through to the namespace route · an unmatched deeper path 404s (the fallthrough is one segment wide, not a catch-all)": "todo",
    "§2/§7 · what the fallthrough serves 12. §7 · /<user>/mcp and /<user>/mcp/<slug> are the only consumer shapes — a third segment 404s rather than resolving a slug with a suffix": "todo",
    "§2/§7 · what the fallthrough serves 13. §8 · /api/whoami answers under the reserved \"api\" segment and is never read as a username's namespace (the CLI contract's one non-MCP route)": "todo"
  },
  "server/test/worker/upstream-credentials.test.ts": {
    "§5 · the envelope at rest §5 · a sentinel header stored through setHeaders appears nowhere in the service row — the column holds ciphertext": "todo",
    "§5 · the envelope at rest §5 · the envelope leads with a version byte, so ciphertext written under today's key is self-describing before any key is applied": "todo",
    "§5 · the envelope at rest §5 · twin to the case above: the fake upstream observes that exact header on the next dial — sealed, not lost": "todo",
    "§7 · connectionStatus is total §7 · the cross-product is exhausted and the read dials nothing": "todo",
    "§7 · connectionStatus is total one case per connectionStatusRow — title as authored": "todo",
    "§7 · hygiene around the flow §7 · cleanupStaleState deletes past-TTL rows and only those — and a past-TTL row was already dead to the callback before the sweep ran (hygiene, not correctness)": "todo",
    "§7 · hygiene around the flow §7 · clientMetadata serves a static secret-free document whose client_id is its own URL under the injected origin": "todo",
    "§7 · one credential path per mode §7 · disconnect wipes the envelope, writes upstream.disconnected, leaves roles, grants and config untouched, and is idempotent": "todo",
    "§7 · one credential path per mode §7 · setHeaders refuses an oauth-mode service and beginConnect refuses a headers-mode or tunneled one · twins: each mode's own path stores": "todo",
    "§7 · the callback refusal matrix §7 · state is single-use by compare-and-set delete: of two concurrent callbacks on one state row, one connects and one is refused": "todo",
    "§7 · the callback refusal matrix one case per oauthCallbackRow — title as authored": "todo",
    "§7 · the connect flow against an adversarial AS §7 · beginConnect records one single-use state row bound to owner, service, issuer, token endpoint, verifier and session — and stores nothing on the service": "todo",
    "§7 · the connect flow against an adversarial AS §7 · mix-up defense: the code is redeemed only at the token endpoint recorded at initiation, even when the AS advertises a different one afterwards": "todo",
    "§7 · the connect flow against an adversarial AS §7 · the AS's real S256 check bites: a redemption carrying a verifier that does not match the recorded challenge is refused and stores nothing · twin: the bound verifier redeems": "todo",
    "§7 · the connect flow against an adversarial AS §7/§9 · the adversarial branches are the real ones: no RFC 9728 document, CIMD refused so DCR runs, no expires_in in the token response — connect still completes and the service reads `connected`": "todo"
  },
  "server/test/worker/upstream-proxy.test.ts": {
    "§10 — subrequest budgets asserted explicitly (workerd enforces none locally) §10 · an aggregated list over N proxied services is N dials, and a second list dials again (no proxied catalog cache in v1)": "todo",
    "§10 — subrequest budgets asserted explicitly (workerd enforces none locally) §10 · an upstream redirect is not followed — `redirect: \"manual\"` keeps a bearer from walking off to another origin": "todo",
    "§10 — subrequest budgets asserted explicitly (workerd enforces none locally) §10 · one tools/call is exactly one resource dial — never pooled, never retried": "todo",
    "§7 — aggregated fan-out vs the scoped surface §15 · none of these paths writes an audit row — `tools/list` is out of the vocabulary": "todo",
    "§7 — aggregated fan-out vs the scoped surface §7 · a tunneled service in the same fan-out answers from DO cache and is unaffected by either deadline": "todo",
    "§7 — aggregated fan-out vs the scoped surface §7 · both slugs are named in `_meta[\"pmcp/unavailable\"]`, and the healthy services' tools are all present, slug-prefixed": "todo",
    "§7 — aggregated fan-out vs the scoped surface §7 · one failing plus one hanging upstream: the aggregate still succeeds": "todo",
    "§7 — aggregated fan-out vs the scoped surface §7 · the fan-out honors AGGREGATED_LIST_DEADLINE_MS, not CALL_TIMEOUT_MS — a hang cannot hold the listing past it": "todo",
    "§7 — aggregated fan-out vs the scoped surface §7 · the scoped list against the same failing service → -32000 — where the aggregate's silent omission surfaces": "todo",
    "§7 — caller identity forwarding, off by default §7 · `forward_identity: true` → `X-Pmcp-Principal` and `X-Pmcp-Roles`, the wildcard forwarded literally as `all`": "todo",
    "§7 — caller identity forwarding, off by default §7 · `forward_identity` absent (default false) → no `X-Pmcp-*` header on the upstream request": "todo",
    "§7 — caller identity forwarding, off by default §7 · consumer headers (Authorization, Cookie) are never copied upstream under either setting": "todo",
    "§7 — caller identity forwarding, off by default §7 · the consumer's declared clientCapabilities are mirrored onto the per-request client, `{}` when none were declared": "todo",
    "§7 — credentials at call time §7 · a failed refresh flips needs_reconnect, writes `upstream.oauth_refresh_failed`, and never dials the resource": "todo",
    "§7 — credentials at call time §7 · a fresh bundle triggers no refresh at all — zero token dials (the twin)": "todo",
    "§7 — credentials at call time §7 · a stale oauth bundle is refreshed BEFORE the forward: the token endpoint is dialed first, the resource second": "todo",
    "§7 — credentials at call time §7 · headers-mode: the stored headers ride the dial and appear in no response and no audit row": "todo",
    "§7 — proxied redaction has no schema half §7 · so no proxied call is ever refused for a catalog miss — the tunneled twin (-32001) is approval-e2e's": "todo",
    "§7 — proxied redaction has no schema half §7 · upstreamBackend.sensitivePaths resolves `{ args: [], results: [] }` and never null": "todo",
    "§7 — the failure table: one code out, the class in §15 · a sentinel planted in the upstream's status text, WWW-Authenticate, and body reaches no consumer-visible byte": "todo",
    "§7 — the failure table: one code out, the class in §7 · `data` is unset on every failure row; nothing upstream-derived survives the gateway mapping": "todo",
    "§7 — the failure table: one code out, the class in §7 · a 2xx body that is not a JSON-RPC message → -32000, class bad_body": "todo",
    "§7 — the failure table: one code out, the class in §7 · a non-2xx answer → -32000, `detail` records upstream_status with the status": "todo",
    "§7 — the failure table: one code out, the class in §7 · a service already flagged needs_reconnect → -32000 with zero dials, class needs_reconnect": "todo",
    "§7 — the failure table: one code out, the class in §7 · a transport/TLS failure before any response → -32000, class unreachable": "todo",
    "§7 — the failure table: one code out, the class in §7 · a well-formed upstream result is relayed verbatim — the allow-twin every failure row names": "todo",
    "§7 — the failure table: one code out, the class in §7 · no answer inside CALL_TIMEOUT_MS → -32000, class timeout": "todo"
  },
  "server/test/worker/web-pages.test.ts": {
    "§13 · CSRF on every mutating POST 1. §13 · a mutating POST with no CSRF field is 403 AND the substituted ops handler was never invoked (a rejected-but-executed mutation is the bug this case exists for)": "todo",
    "§13 · CSRF on every mutating POST 2. §13 · the same POST carrying the token the page rendered succeeds and the handler ran exactly once (the allow-twin of 1 — without it, `throw 403` passes)": "todo",
    "§13 · CSRF on every mutating POST 3. §13 · a token minted under a different cookie session is 403, handler not invoked": "todo",
    "§13 · CSRF on every mutating POST 4. §13 · every mutating form the pages render carries a CSRF field — walked out of the rendered HTML, never listed, so a new form cannot forget one": "todo",
    "§13 · CSRF on every mutating POST 5. §13 · /audit renders no mutating form and needs no token (no mutations, no CSRF surface)": "todo",
    "§4/§13 · cookie sessions are the only page credential 6. §4 · a bearer-sourced (device-flow) session is refused on /account · a browser session renders it (the twin — the guard is about provenance, not about being logged out)": "todo",
    "§4/§13 · cookie sessions are the only page credential 7. §7 · an Authorization: Bearer header with no cookie opens no page — bearer tokens are never consulted on page routes": "todo",
    "§4/§13 · cookie sessions are the only page credential 8. §13 · /approvals/<id> for another namespace's approval refuses · the owner's own id renders (owner-only, and indistinguishable from a nonexistent id)": "todo",
    "§4/§13 · cookie sessions are the only page credential 9. §13 · /manifest.webmanifest and /sw.js are served without a session — installability is not gated, and the PWA shell holds nothing to gate": "todo",
    "§7/§13 · the OAuth callback shell 20. §7 · /oauth/upstream/callback without an owner session is refused before any upstream code runs and stores nothing · with the session and a live single-use state it completes (the twin; every other state failure is upstream-credentials.test.ts's table)": "todo",
    "§8 · parity direction B — forms and schemas are one source 16. §8 · every form rendered on /services and /approvals names an ops key that exists in admin.ops (no form fronts a tool that is gone)": "todo",
    "§8 · parity direction B — forms and schemas are one source 17. §8 · each form's field set equals schemaKeysOf(ops[name]) — both sides derived, so a schema change with no form change fails here rather than at a user's keyboard": "todo",
    "§8 · parity direction B — forms and schemas are one source 18. §8 · /account renders no ops-backed form at all — the pinned parity exception: credentials ride better-auth's endpoints and are never reachable from a pmcp tool": "todo",
    "§8 · parity direction B — forms and schemas are one source 19. §8 · every page mutation reaches an ops handler (or better-auth): no page route mutates D1 on its own — the no-web-only-capability invariant, checked by substituting handlers across the ops table rather than by reading web.ts": "todo",
    "§8/§13 · one paging contract, two presentations 10. §8 · the page's \"N events match\" line is audit.query's `total`, not the rendered row count — they differ whenever a page is not the last one": "todo",
    "§8/§13 · one paging contract, two presentations 11. §13 · desktop page numbers and mobile \"Load more\" walk the same offset/limit contract to the same final row set": "todo",
    "§8/§13 · one paging contract, two presentations 12. §13 · Export JSONL emits exactly `total` lines for the current filters": "todo",
    "§8/§13 · one paging contract, two presentations 13. §13 · the export applies the page's filters verbatim — a filtered export is a strict subset of the unfiltered one over the same seed": "todo",
    "§8/§13 · one paging contract, two presentations 14. §15 · an exported row carries its recorded bodies post-redaction, with stubs rendered as typed placeholders · never the bytes a blob stub stands for": "todo",
    "§8/§13 · one paging contract, two presentations 15. §13 · a row's client session id links back to this same view as ?session=… and that link returns exactly the rows sharing the session": "todo"
  }
}
