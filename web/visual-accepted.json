{
  "agent-detail__activityApprovalSelected__390x844": "Reject and Approve are a mutation, not two form POSTs, so they are direct children of .actions and the narrow rule .actions .btn { flex: 1 } finally reaches them; the SSR baseline escaped it only because each button sat inside its own <form>.",
  "agent-detail__filterOffer__390x844": "SSR fixture kept the entire unfiltered Tools group under q=admin_*; the filter matches on substring, so the real render keeps only the Patterns group (never filtered) plus the pattern offer.",
  "agent-detail__longData__1280x900": "Same fixture contradiction as the 390 pair: the rail claims the 40-character slug while the pane, its counts and its Reachable-by line are news's.",
  "agent-detail__longData__390x844": "SSR fixture relabels only the rail's first entry to the 40-character slug and leaves the pane on news, so its header, scoped endpoint and TypeScript path are the short app's; the long slug's own pane is what a rail entry for it opens.",
  "agent-detail__narrowLevel1__1280x900": "SSR fixture's rail is hand-ordered news/linear/home and lands on news; held apps sort ascending, so the landing pane is home — the baseline's own rail contradicts its own pane.",
  "agent-detail__newGrant__390x844": "SSR fixture wrote groups: [] while its own header counts 4 tools on gh; a real new grant lists the app's whole catalog with every row at none.",
  "agent-detail__noEffect__390x844": "SSR fixture hand-wrote one Tools group holding one row while its own header counts 5 tools, 1 prompt and 2 resources; the derivation lists every family, so no catalog produces that baseline.",
  "agent-detail__unconnected__390x844": "SSR fixture wrote one never-connected note under Tools; familyNote returns that sentence for all three families, and the baseline also omits the Roles group and the undeclared lights row the set carries.",
  "agent-detail__undeclaredRole__390x844": "SSR fixture listed only Roles, the undeclared row and Tools; grantEditorOf always also emits Prompts, Resources and Patterns, so the baseline is three groups short of any real render.",
  "app-detail__agentsEditing__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__agentsRefused__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__agents__390x844": "Baseline artefact: the fixture claude row typed allowed: [reader, tool/paper_fetch] while its own grant-editor fixture carries the resource/news://feed/* pattern that grant holds; the live pane lists every allow-mode entry, so React third badge is right and the baseline two-badge row is the fixture.",
  "app-detail__catalogPrompt__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__catalogResource__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__catalogTool__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__confirmRemoveAgent__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__longAgents__390x844": "Baseline artefact: the same hand-typed reach as longCatalog, plus a reach panel listing the short app three tools where the long app has four; the live page counts the catalog it is given, so React four-row panel is right.",
  "app-detail__longCatalog__390x844": "Baseline artefact: the long-data fixture typed per-row reach by hand — it omits the claude that the reader role search_.* pattern grants on the long tool, and spells pi triage:approval grant as no-ask; the live page derives both from the grants, so React extra badge and pi · ask are right.",
  "app-detail__longRoles__1280x900": "Baseline artefact: the fixture spliced the long role in front of the SHORT app role rows, so reader keeps matches 2 and all keeps matches 6 against a four-tool catalog (3 and 7 live), it credits the long role to an agent whose grant its own longAgents fixture spells as literals, and it credits all to claude who never holds it; every one of those four is derived live, so React is right.",
  "app-detail__longRoles__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had. Its listing also carries the long-data count artefacts named on the 1280 pair.",
  "app-detail__mobileLevel3__390x844": "Baseline artefact: catalogToolDetails.redaction typed only the arguments half while the same file masks out.stored on secret_push; the live builder appends \" · results out.stored\", so React two-line Redaction value is right.",
  "app-detail__overviewProxied__390x844": "Baseline artefact: the fixture set the proxied app alias view to tools: [] while its sibling fixtures list that app catalog; aliasViewOf unions the configured keys with every listed tool and aliasRowsFor pads by ALIAS_SPARE_ROWS, so the live editor draws 3 prefilled rows plus 3 spares — React is right, the baseline three bare spares are the fixture.",
  "app-detail__recordingExpanded__390x844": "Baseline artefact: mcpToolsRecording hand-typed 3 argument paths while the same file catalogToolDetails declares credentials.user on secret_push, and it hand-ordered the per-tool sub-rows; the live pane and the live item card both walk one schemaLeaves(inputSchema), so the two baselines contradict each other and only one is reproducible — the seed keeps the schema the catalog baseline documents, and React 4 paths in listing order is what the live page renders.",
  "app-detail__recordingOff__390x844": "Baseline artefact: mcpToolsRecording hand-typed 3 argument paths while the same file catalogToolDetails declares credentials.user on secret_push, and it hand-ordered the per-tool sub-rows; the live pane and the live item card both walk one schemaLeaves(inputSchema), so the two baselines contradict each other and only one is reproducible — the seed keeps the schema the catalog baseline documents, and React 4 paths in listing order is what the live page renders.",
  "app-detail__recordingProxiedWarning__390x844": "Baseline artefact: the fixture helper hardcoded \"body logging on · proxied default\", which logDefault = logBodies === (kind === tunnel) cannot produce for a proxied app whose logging is ON; the live summary says \"set explicitly\", which is what React renders.",
  "app-detail__recordingRefused__390x844": "Baseline artefact: mcpToolsRecording hand-typed 3 argument paths while the same file catalogToolDetails declares credentials.user on secret_push, and it hand-ordered the per-tool sub-rows; the live pane and the live item card both walk one schemaLeaves(inputSchema), so the two baselines contradict each other and only one is reproducible — the seed keeps the schema the catalog baseline documents, and React 4 paths in listing order is what the live page renders.",
  "app-detail__recording__390x844": "Baseline artefact: mcpToolsRecording hand-typed 3 argument paths while the same file catalogToolDetails declares credentials.user on secret_push, and it hand-ordered the per-tool sub-rows; the live pane and the live item card both walk one schemaLeaves(inputSchema), so the two baselines contradict each other and only one is reproducible — the seed keeps the schema the catalog baseline documents, and React 4 paths in listing order is what the live page renders.",
  "app-detail__rolesEditing__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__rolesOffer__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__rolesRefused__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__rolesShadowed__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__rolesUnread__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__tokenRevealed__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-detail__tokenSelected__390x844": "Baseline artefact: the SSR fixture set pane.details directly while appDetail() derived the level from an EMPTY query, so it drew narrow level 2 (the listing) for a state that has a row selected; the live appLevel makes any ?sel= level 3, so the React level-3 card is right — the fixture own mobileLevel3 state is this same render at the level it should have had.",
  "app-new__blank__1280x900": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__blank__390x844": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__default__1280x900": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__default__390x844": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__errors__1280x900": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__errors__390x844": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__longValues__1280x900": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__longValues__390x844": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__slugInvalid__1280x900": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__slugInvalid__390x844": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__slugReserved__1280x900": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__slugReserved__390x844": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__tunneled__1280x900": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "app-new__tunneled__390x844": "Baseline artefact: the SSR app-new fixtures carry ZERO alias rows while the live form composes aliasRowsFor(aliasDraftOf(query)), which pads to ALIAS_SPARE_ROWS (3); React three blank rows are what the live page produced and the empty baseline is the fixture.",
  "apps__archivedNotice__390x844": "Baseline artefact: the fixture typed a bespoke sentence (\"home is archived. Its roles, grants, and tokens are kept.\") where the live noticeOf answers ?done=app_archive with `${humanize(done)} done.`; React \"App archive done.\" is the live copy."
}
